# CLAUDE.md — waterfall-viewer

Рабочая конвенция этого проекта. Действует для всех будущих сессий Claude Code здесь.

> ⚠️ Репозиторий **публичный** (`VibeEngineering-LLC/waterfall-viewer`). В коммиты и
> любые файлы репо — **никаких** секретов/PII: device ID, серийники, GUID, IP/MAC, GPS-координаты,
> пути с именем оператора, e-mail, токены, пароли. Этот файл держать обезличенным.

## Конвенция замечаний (зафиксировано оператором 2026-06-26 — «Запомни навсегда»)

Дословно: «Фиксируй все замечания, присваивай номера. Отслеживай выполнение. Запомни навсегда».

1. **Фиксировать каждое замечание.** Любая правка/баг/пожелание из чата или со скриншота —
   записывается, не теряется. Источник истины — `TASKS.md` (раздел «Дефекты/Доработки …»).
2. **Присваивать сквозной номер** `#NN`. Нумерация непрерывна по всему проекту и **не сбрасывается**
   между фазами. Следующий свободный номер берётся за максимумом существующих в `TASKS.md`.
3. **Отслеживать выполнение** через список задач (TaskCreate/TaskUpdate): `pending` → `in_progress`
   → `completed`. Заголовок задачи начинается с `#NN — …`.
4. **При закрытии замечания** — дописать в `TASKS.md` запись `**#NN — суть** (файлы): что сделано`,
   обновить счётчик тестов («Полный pytest — N passed»), отметить задачу `completed`.
5. **Номер виден в коде:** в комментариях у изменённого кода ссылаться на `Задача #NN` —
   чтобы будущая сессия связала строку с замечанием.

История нумерации: `#1–#36` — исходные задачи/дефекты (см. `TASKS.md`); `#37–#40` — доработки
сечений 3D + раскладка окон; `#41–#44` — графики среза (подсветка/синк/лог-лин/counts-cps);
`#45–#46` — контраст профиля сечения (белый) + затенение/освещение рельефа 3D;
`#47–#48` — профиль сечения = проекция суммы между плоскостями + цвет профиля как рама/ось;
`#49–#50` — суммарные линии поверх рельефа (depthValue) + проекция считает узкий пик (log(Σ counts));
`#51` — кнопка «Сброс» настроек отображения к умолчанию;
`#52` — профиль сечения = спектр верхнего-правого окна (sum-LOD вместо max-LOD; меняет семантику #50);
`#53` — дефолт всех графиков → cps (рельеф = cps/канал/момент max-LOD, голубая = сумма cps на срезе sum-LOD);
`#54` — истинный log10 Z-шкалы с авто-порогом (floor = перцентиль ненулевых) вместо log10(1+x): для cps<1 старый лог вырождался в линейный, новый масштаб-инвариантен;
`#55` — 5 регулировок отображения вынесены из тулбара в отдельный док-панель в стиле «рукоятки» (`awf/ui/knobs.py`: `Knob`/`KnobRow`/`AdjustPanel`); per-knob и общий вкл/выкл+сброс; bypass = стандартный вид как без регулировки (дефолты ручек нейтральны, позиции сохраняются);
`#56` — 6-я ручка «Окно t» на ту же панель: ширина выборки по времени = число временны́х бинов LOD-прорежки 3D-водопада (`max_time` в `downsample`), не косметическое растяжение; v/100 = относит. ширина w → `max_time=round(400/w)`, дефолт ×1.00 нейтрален; `Waterfall3DView.set_time_bins`, `set_spectrogram(max_time=None)` сохраняет значение при загрузке файла;
`#57` — регуляторы панели перерисованы из круглых крутилок в вертикальные движки-фейдеры (скриншот iZotope «Time Structure»): `Knob.paintEvent` рисует паз+зелёный уровень+риски-шкалу+металлическую каретку (`_draw_cap`), размер 44×92; QSlider-совместимый API и bypass-семантика без изменений (имя `Knob`/алиасы `_*_slider` целы);
`#58` — движки повёрнуты в горизонтальные, панель собрана в одну колонку (скриншот сетки 3×2): `Knob` — горизонтальный паз (`x0..x1`), зелёный уровень слева до каретки, риски сверху/снизу, каретка-планка `_draw_cap` на `hx`; низкий тянущийся (`sizeHint 120×24`, Expanding по ширине), drag/курсор по X (`_drag_x`/`SizeHorCursor`); `KnobRow` → `QHBoxLayout` (подпись│движок│значение│вкл│⟲), `AdjustPanel` → `grid.addWidget(row, i, 0)` (одна колонка); QSlider-API и bypass без изменений.
`#59` — панель сечений (`SectionControls` в `view3d.py`) оформлена в том же knob-стиле: `objectName("adjustPanel")` + заголовки `knobTitle`/значения `knobValue`; ряд слота `#N│Knob│значение│вкл-выкл` (`QSlider`→`Knob` #58, `QCheckBox`→`QToolButton knobToggle`). По умолчанию все слоты выкл (кнопки не нажаты, движки погашены `setEnabled(False)`); вкл слота оживляет движок и переиздаёт `planeChanged`. Публичный API (`planeChanged`/`_rows`/`_frac`/`_emit`/`set_value_label`/`emit_all`) без изменений — проводка и прежние тесты валидны.
`#60` — дефолтные положения рукояток/переключателей приведены к эталон-скриншоту обеих панелей: панель регулировок стартует с общим выключателем **ВЫКЛ** (`AdjustPanel._global` не нажат, «Регулировки: ВЫКЛ») и всеми рядами **выкл** (`KnobRow._chk` не нажат, ручка/значение погашены), ручки на нейтральных дефолтах (bypass = стандартный вид); панель сечений — слот **#1 в минимуме** (frac 0, левый край), слот **#2 в максимуме** (frac 1, правый край), все слоты выкл (с #59); `reset_all()` приведён к «сброс == дефолт» (всё выкл). Bypass-тесты включают нужные ряды перед проверкой passthrough.
`#61` — убраны все отображения НА секущих плоскостях (профиль/проекция `line` всегда скрыт, построение кривой и вызов `_section_projection` удалены — снят визуал #41/#47/#48/#49/#50/#52/#53 на плоскости; `_section_projection` оставлен мёртвым кодом на случай возврата) + грань плоскости (контур `border`) приглушена: цвет оси сохранён, alpha понижена 1.0→`_BORDER_ALPHA`=0.30, width 2.0→1.5; заливка `mesh` остаётся выключенной (#37). Значения плоскостей (`plane_value`) не затронуты. Удалены 7 тестов снятой фичи профиля → счётчик 278→271.
`#62` — тулбар «Вид» и строка статуса были скучены: в `style.py` QSS подняты spacing/padding/font-size тулбара и добавлены правила потомков `QToolBar QComboBox/QPushButton` (`min-height:28px`, контролы 22→40–42px, тулбар ~58px), `QStatusBar` получил `font-size:14px`; высоту бара QSS `min-height` не применяет — в `main_window.py` `statusBar().setMinimumHeight(28)` + `pointSize+1`. Селекторы по потомкам `QToolBar`, доки не раздуты. Счётчик 271→273.
`#63–#68` — координатная сетка 3D-водопада (одна связная правка `view3d.py`/`main_window.py`): `#63` рамка поля в 1 клетку со всех сторон (`_rebuild_grid`/`_draw_grid_lines`, `_grid_items`, `_GRID_BORDER_RGBA`); `#68` клетки на круглых делениях шкал (`_time_ticks`/`_energy_ticks` через `_nice_ticks`); `#64` единицы оси времени с/мин/ч (`_TIME_UNIT_SCALE`, `set_time_unit`, комбобокс «Время» в тулбаре); `#66` единица на каждой клетке («30 с»/«200 кэВ»); `#65` удалён блок вертикальной шкалы счёта (Z) из `_rebuild_axis_labels`. Счётчик 273→280.
`#67`/`#69` — маркеры выбранных линий нуклидов на гранях видимых плоскостей Времени (`_rebuild_plane_nuclides`/`_draw_plane_nuclide_lines`/`_add_plane_nuclide_line`, `_plane_nuclide_items`): `#67` цветной вертикальный отрезок на позиции энергии каждой линии; `#69` высота ∝ интенсивности (`GammaLine.intensity` 4-м элементом кортежа линии, нормировка `_max_line_intensity`, ярчайшая = полная высота `zmax`), несколько линий семейства = по маркеру на линию; 3-кортежи без интенсивности → полная высота (совместимость, толерантная распаковка `_energy_lines`). Счётчик 280→285.
`#70` — отступ рамки 3D-сетки от данных убавлен с целой клетки (#63) до полклетки (`_rebuild_grid`: `mx,my=0.5*cellx,0.5*celly`). Тест `…extends_one_cell…` → `…half_cell…` (+проверка «отступ < 0.75 клетки»). Счётчик без изменений — 285.
`#71`/`#72` — фиксированные «человеческие» шаги делений осей 3D: время по 15 мин для длинных водопадов, энергия по 200 кэВ для широкого спектра (`view3d.py`: helper'ы `_step_ticks`/`_pick_ticks`, константы `_TIME_STEP_15MIN_S=900`/`_ENERGY_STEP_KEV=200`/`_PREF_STEP_MIN_TICKS=3`/`_PREF_STEP_MAX_TICKS=40`; `_time_ticks`/`_energy_ticks` используют предпочтительный шаг при 3..40 делениях, иначе откат на `_nice_ticks`). Счётчик 285→289.
`#73` — шрифт подписей делений осей 3D уменьшен `QFont("Helvetica",10)`→`7` (`_rebuild_axis_labels`). Тест `test_axis_label_font_reduced`. Счётчик 289→290.
`#74` — комбобоксы тулбара заменены на «перебор» кнопкой/колесом (`awf/ui/cyclebutton.py` NEW `CycleButton(QPushButton)`: клик = след. по кругу, колесо = вперёд/назад; QComboBox-совместимый API `addItem`/`count`/`currentIndex`/`setCurrentIndex`/`currentData`/`currentText`/`itemData`/`itemText`/`currentIndexChanged`); 4 контрола («Z-шкала»/«Палитра»/«Единицы»/«Время») в `main_window.py` переведены `QComboBox`→`CycleButton`, наследник QPushButton сохраняет QSS #62. Тесты `test_cyclebutton_click_cycles`/`test_cyclebutton_wheel_scrolls`. Счётчик 290→292.
`#75` — каркас верхних выпадающих меню (`main_window.py`: `_build_stub_menus` из `_build_menu`): к «Файл» добавлены Изотопы/Анализ/Сервис/Помощь/О программе, в каждом один disabled-пункт-заглушка «— наполняется позже —», меню в `self._menus` (ключи `isotopes/analysis/service/help/about`) под будущее наполнение действиями. Тест `test_top_menus_skeleton_present`. Счётчик 292→293.
`#76` — отключение «подложки» (фиолетового прямоугольника = плоского дна рельефа) (`awf/ui/view3d.py` + `main_window.py`): подложка — не отдельный объект, а ячейки дна единственной `GLSurfacePlotItem` с `zn≈0` (фиолетовый минимум палитры). При выключении такие ячейки (высота `≤ _FLOOR_FRAC·height_scale`, `_FLOOR_FRAC=0.02`) делаются прозрачными (`alpha=0`) + поверхность в `translucent`; остаётся только рельеф. Чекбокс «Подложка» в тулбаре (дефолт вкл, «Сброс»→вкл) → `_on_floor_toggled` → `set_floor_visible()` (alpha-only ре-рендер, без ре-LOD). Тесты `test_floor_visible_by_default`/`test_floor_hidden_zeroes_base_cells`/`test_floor_toolbar_checkbox_drives_view`. Счётчик 293→296.
`#77` — подписи делений осей «гасить за массивом, зажигать со стороны взгляда» (`awf/ui/view3d.py`): подписи единиц (время «15 мин», энергия «200 кэВ») и зубцы оси энергии теперь висят на ближнем к зрителю крае оси, а не жёстко на `ymin`/`xmax`. Край выбирается по знаку XY-направления камеры на зрителя (`_viewer_sides()`, камера→центр сцены, центр в (0,0)): время — `ymax`/`ymin`, энергия+зубцы — `xmax`/`xmin`. Перевешивание при вращении мышью — override `mouseMoveEvent` → `_maybe_reorient_labels()`, ребилд только при смене квадранта (сигнатура `_label_side_sig`). Дефолт камеры (azimuth=−60) сохраняет прежнее размещение (время `ymin`, энергия `xmax`). Тест `test_axis_labels_follow_viewer_side`. Счётчик 296→297.
`#78` — вывод и сетку 3D ограничить 3000 кэВ (`view3d.py`): каналы выше `_MAX_ENERGY_KEV=3000` отсекаются в `set_spectrogram` после LOD-прорежки (срез prefix'а `z_counts`/`_z_counts_sum`/`ch_centers`, калибровка монотонна); `ch_centers` задаёт и рельеф, и `_energy_ticks`, и `_rebuild_grid` — обрезка данных обрезает и сетку, отдельной логики не нужно. Тест `test_energy_axis_capped_at_3000`. Счётчик 297→298.
`#79` — в меню «Изотопы» ссылка на окно с изотопами (`main_window.py`): заглушка #75 заменена на `self._ndock.toggleViewAction()` (док «Нуклиды» сохранён в `self._ndock`), текст «Окно изотопов (нуклиды)» — открывает/прячет панель, галочка = видимость; прочие меню остаются заглушками. Тест #75 обновлён (исключает «Изотопы»), добавлен `test_isotopes_menu_opens_nuclides_dock`. Счётчик 298→299.
`#80` — убрать вертикальные жёлтые отрезки-зубцы шкалы энергий (`view3d.py`): оливковые зубцы (IV-R2/#77) убраны, на оси энергии остались только текстовые подписи. В `_rebuild_axis_labels` снят вызов `_add_line` (и расчёт высоты зубца от `zmax`); как мёртвый код удалены метод `_add_line` и константа `_ENERGY_TICK_RGBA` (использовались только под зубцы, проверено grep). Тесты наличия зубцов перепрофилированы на отсутствие (`teeth == 0` в `test_view3d_axes.py`; `test_energy_scale_has_no_vertical_teeth` в `test_group4_remarks.py`). Счётчик без изменений — 299.
`#81` — объяснение (без кода) физического смысла 6 регуляторов «Регулировки отображения» (`knobs.py:242-249` `_SPECS`, применение `main_window.py:346-362`, контраст `zscale.py:37-77`): Усиление/Гамма/Отсечка/Сглаживание/Освещение/Окно t — все только отображение, дефолты = bypass. Изменений кода/тестов нет, счётчик 299.
`#82` — отключить жёлтое окно ROI-выделения на 2D-карте (`panels.py`): `HeatmapPanel._roi` (`pg.RectROI`, жёлтый pen) больше не показывается — в `set_spectrogram` `setVisible(True)`→`setVisible(False)`; дефолтное окно (центр. четверть) и `_emit_roi()` сохранены, панель срезов получает «полосу ROI» как раньше. Тест `test_heatmap_roi_hidden`. Счётчик 299→300.
`#83` — объяснение (без кода) вкладки «Аналитика» (`analytics_panel.py`, бэкенд `decomposition.py`/`cluster.py`): проекция спектров временны́х срезов в 2D + кластерная раскраска. Контролы — Проекция (pca/tsne/umap), Кластеры (kmeans/dbscan/hdbscan), k (для kmeans), Нормировка (L1 формы спектра), Пересчитать. Точка=срез, клик→переход к срезу; «дисперсия PCA X%/Y%»=объяснённая дисперсия PC1/PC2. Изменений кода/тестов нет, счётчик 300.
`#87` — 2D-карта «уезжала» при отдалении/панораме (`panels.py`): добавлен `HeatmapPanel._lock_view_to_data()`, в `set_spectrogram` на ViewBox ставится `setLimits(xMin=0,xMax=cols,yMin=0,yMax=rows,maxXRange=cols,maxYRange=rows)` — панорама не выходит за окно данных, минимальный зум = полное окно. Тест `test_heatmap_view_limited_to_data`. Счётчик 300→301. (#84–#86 из той же серии — в очереди.)
`#84` — секущие плоскости 3D режут данные от своего края до текущей позиции (`view3d.py` `_clip_windows`/`_rebuild_surface`): каждый слот режет независимо односторонне — слот 0 = нижняя граница (от минимума оси), слот 1 = верхняя (от максимума); одной видимой плоскости теперь достаточно (раньше IV-R3/#22 требовала пару). Две плоскости оси = окно между ними (обратная совместимость). Семантика «от края, прятать пройденное» (выбор оператора). Встречные плоскости (`i0>i1`/`j0>j1`) → пустое окно, поверхность снимается. Тесты: `test_single_plane_clips_from_edge`, `test_single_plane_slot1_clips_upper`, `test_single_counts_plane_activates_clip`, `test_crossed_planes_empty_surface`, `test_disable_one_plane_keeps_other_clip`. Счётчик 301→304.
`#85` — маркеры изотопов только на секущих плоскостях (`view3d.py`): рёберные лучи энергий у края данных (Задача 15/IV-R2) убраны — `_rebuild_energy_rays()` теперь лишь снимает старые `_ray_items`, ничего не рисует. Единственный механизм — `_rebuild_plane_nuclides()` (маркеры на гранях ВИДИМЫХ плоскостей Времени, #67/#69); без видимой плоскости маркеров нет. Режим подсветки поверхности (Задача 18) не затронут. Тесты `test_view3d_rays.py`/`test_highlight.py` переписаны под новую семантику. Счётчик без изменений — 304.
`#89` — графики дока «Срезы/Сечения/Выборки» уезжали при зуме (`panels.py::SlicePanel._lock_views_to_data`): аналог #87 для двух графиков среза — после `set_spectrogram` X-домен каждого ViewBox ограничен экстентом данных (`setLimits` xMin/xMax/maxXRange): верх — энергия [emin..emax], низ — время [tmin..tmax]; панорама не выходит за домен, мин. зум = полный домен. Y не трогаем (лог-шкала спектра #43, меняющийся cps #44/#53). Тест `test_slice_views_locked_to_data`. Счётчик 304→305.
`#88` — объяснение (без кода): панель «Срезы/Сечения/Выборки» (`panels.py::SlicePanel`). Верх — γ-спектр выбранного фрагмента по времени (срез/окно/выборка), не интегральный по всему файлу → видны короткие линии; «Энергоокно» — узкая ROI у фотопика (пресеты Cs-137 662/K-40 1461/U 609/U 1764/Th 2614 или вручную); низ — скорость счёта по времени: малиновая «полоса ROI» (широкая полоса с 2D-карты) + жёлтая «энергоокно» (узкая у линии). Даёт: временной профиль линии (внос/вынос/спад), линия vs фон, накопление статистики по «горячей» выборке. Кода/тестов нет, счётчик 304.
`#86` — догрузка γ-линий оставшихся изотопов из списков, медицинские в первую очередь (`scripts/fetch_missing_nuclides.py`, `awf/data/nuclides.json`): воспроизводимый скрипт тянет недостающие нуклиды (карта категорий − библиотека) из IAEA LiveChart/ENSDF (`decay_rads`, `rad_types=g`, офлайн-кэш), фильтр интенсивности ≥0.1 %, кэп 30 линий; метастабильные (Tc-99m/Ba-137m/Ag-110m) — через основное состояние по `p_energy` уровня изомера, T½ из `start_level_hl`. Порядок medical→natural→technogenic→fission, `--merge` идемпотентен. Итог: **+62 нуклида (21→83), 124→871 линия**, все 4 категории заполнены (medical 10/natural 21/technogenic 33/fission 19). 8 чистых α/β без γ≥0.1 % честно не добавлены (Po-210, Ni-63, Pu-241, Pm-147, Ru-106, Sr-89, Sr-90, Y-90; вместо Ru-106 — дочерняя Rh-106). Все значения из источника, не из памяти (§23). Тесты панели обновлены под новый состав (ветка «Медицинские» → `topLevelItemCount` 4; lifetime-фильтр разделяет long/short; `add_nuclide` на Po-210). Счётчик без изменений — 304.
`#90` — сглаживание SMA по каналам в каждом временно́м срезе (`view3d.py:237`, `zscale.py::smooth_counts`): проверка-фиксация, запрошенное уже реализовано — `smooth_counts(z_counts, self._smooth, axis=1)` на матрице `(nt,nc)`=(срезы×каналы), `axis=1` = канальная ось, box-SMA окном `2·r+1` к каждому ряду-срезу независимо (по времени не смешивает); регулятор 0–15, дефолт 0 = bypass (`knobs.py:246`, `DEFAULT_SMOOTH==0`). Логика не менялась, добавлен регрессионный тест `test_smooth_per_time_slice_independent_axis1`. Счётчик 305→306.
`#91` — убрать нефункциональную кнопку «Регулировки: ВКЛ/ВЫКЛ» (`knobs.py`, `style.py`): мастер-выключатель `AdjustPanel._global` удалён — он был нефункционален (по дефолту ВЫКЛ (#60), и в этом состоянии `values()` глушил все ряды, так что включение отдельной регулировки не давало эффекта без флипа мастера). Bypass теперь только per-row: `values()`=`{k: r.effective_value()}`, ряд выкл → дефолт. Удалены `_on_global`/`is_global_on`/`KnobRow.set_global_enabled` + QSS `#knobGlobal`, `reset_all` упрощён. Тесты переписаны под per-row. Счётчик без изменений — 306.
`#92` — рукоятки Регулировок сбрасывали зум 3D (`view3d.py`): любой ход ручки перерисовывал поверхность через `set_spectrogram(self._sg, …)`, а тот безусловно звал `setCameraPosition(distance=span*1.6)` — сбрасывая пользовательский зум/панораму. Теперь в начале `set_spectrogram` фиксируем `is_new = sg is not self._sg` (ре-рендеры передают тот же объект, новый файл — другой) и кадрируем камеру только `if is_new`. Зум держится при регулировках/смене палитры/Z-шкалы/единиц; новый спектр по-прежнему кадрируется. Тест `test_adjust_setters_preserve_camera_zoom`. Счётчик 306→307.
`#93` — шрифт тулбара «Вид» приведён к размеру главного меню (`style.py`): после #62 тулбар был 15px, крупнее `QMenuBar`/`QMenu` (14px); `font-size: 15px`→`14px` во всех 5 правилах тулбара (`QToolBar`+потомки), высота контролов (#62 `min-height`/`padding`) не тронута. Тест #62 обновлён (проверяет 14px есть, 15px нет). Счётчик без изменений — 307.
`#94` — убран нерабочий пункт «— вручную —» из комбо «Энергоокно» (`panels.py`): пункт был пустышкой (выбор → ранний `return`). Удалён, комбо `_ewin_preset` держит только пресеты `DEFAULT_WINDOWS` (индексы сдвинуты −1: `_on_ewin_preset` → `DEFAULT_WINDOWS[idx]`); «ручное» состояние = пустой выбор `setCurrentIndex(-1)` (старт и сброс при правке спинбоксов). Тест `test_ewin_preset_has_no_manual_item` + правки 2 тестов слайса. Счётчик 307→308.

`#95` — маркеры изотопов больше не просвечивают сквозь рельеф 3D (`view3d.py`): `_add_plane_nuclide_line` строил `GLLinePlotItem` с дефолтными glOptions, а дефолт pyqtgraph 0.14.0 — `'additive'` (depth-тест выкл) → маркеры рисовались поверх массива. Передал `glOptions="opaque"` (depth-тест вкл) + `antialias=False` → рельеф перекрывает маркеры сзади; профиль сечения (#41) намеренно остаётся поверх. Тест `test_plane_marker_depth_tested_not_additive`. Счётчик 308→309.

`#96` — фон и вычитание; меню «Анализ»: выбор/наложение/вычет фона (новые `awf/model/background.py`, `awf/ui/background_dialog.py`; `panels.py::SlicePanel`, `main_window.py`). Фон — поканальный cps (нормировка по живому времени); источник — ЛЮБОЙ: диапазон срезов текущего файла (`background_from_range` = Σcounts/Σlive_time) ИЛИ отдельный файл (`background_from_spectrogram`: при иной калибровке — через спектральную плотность cps/кэВ, `np.interp` по энергии, вне диапазона→0, ×ширину каналов цели). Наложение — оранжевый пунктир `_bg_curve` поверх спектра среза в текущих единицах. Вычет = весь водопад (3D+2D+срез): `subtract_background` → производная `Spectrogram` `clip(counts−bg_cps·live_time,0)`, раздаётся через `_redistribute`/`_active_spectrogram`; вкладка «Аналитика» — всегда на исходных данных; новый файл сбрасывает фон (`_reset_background`). Тесты `tests/test_background.py` (10) + `tests/test_bg_ui.py` (10) + правка #75. Счётчик 309→329.

`#97` — стиль диалога «Выбор фона» под тёмную тему (`awf/ui/style.py`): `BackgroundDialog` (#96) — дочерний к `MainWindow`, тёмный фон `APP_QSS` каскадировал, но в теме не было правил `QRadioButton` (дефолтный синий индикатор Windows) и кнопок-стрелок `QSpinBox::up/down-button` (светлые системные). Добавлены в `APP_QSS`: `QDialog` (явный тёмный фон top-level), `QRadioButton`+`::indicator` (тёмный кружок, checked = зелёный `#4a7d4a`), `QSpinBox/QDoubleSpinBox ::up/down-button` (тёмные `#45484d`+hover) и `::up/down-arrow` (CSS-треугольники `#cfd2d6`). Правила общие — чинят радиокнопки/спинбоксы во всём приложении. Тесты `test_bg_dialog_theme_covers_radio_and_spinbox` + `test_bg_dialog_inherits_window_theme`. Счётчик 329→331.

`#99` — наложение фона в окне среза выглядит странно на лог-шкале (`awf/ui/panels.py`): оранжевая кривая фона `_bg_curve` в ВЭ-хвосте имеет много каналов с фоном `==0` (нулевая сумма в фоновом окне); на лог-Y `log10(0)→−∞` → pyqtgraph рисовал «частокол» вертикальных пунктиров до ~1e-10. В `_render_background` при лог-шкале (`_spec_log`) неположительный фон → `nan`, кривая `connect="finite"` рвётся по сегментам (на линейной маскирования нет). `set_spectrum_log` теперь зовёт `_render_spectrum` (пересчёт маски при переключении). Тест `test_slice_bg_log_masks_nonpositive`. Счётчик 331→332.

`#100` — сброс зума на окнах среза и времени (`awf/ui/panels.py`): в `SlicePanel` добавлены кнопка «Сброс зума» (рядом с «лог Y») и метод `reset_zoom()` — `autoRange()` на обоих ViewBox (спектр среза + временной ряд) возвращает полный вид; X-домен не выходит за окно данных (ограничен `_lock_views_to_data`, #89). Тесты `test_slice_has_reset_zoom_button` + `test_slice_reset_zoom_restores_full_view`. Счётчик 332→334.

`#98` — фоновый массив не отображается на 3D-спектрограмме (`awf/ui/view3d.py` + `awf/ui/main_window.py`): выбранный фон (поканальный cps) рендерится на 3D-водопаде как «тусклая постоянная простыня» (решение оператора через AskUserQuestion) — полупрозрачный сине-серый `GLSurfacePlotItem` (alpha 0.16, translucent), натянутый по всей оси времени на высоте рельефа, отвечающей `bg(энергия)`. Высота — интерполяция `bg` в реализованное отображение значение→высота текущего рельефа (`z_counts`→`z_surface` через `np.interp` по отсортированным значениям), согласовано с любой Z-шкалой/контрастом без повторного лог-преобразования с data-зависимым полом; cps→counts через медианное живое время среза (`_bg_unit_factor`). Один пункт меню «Наложение фона» теперь правит и кривую среза (1D), и простыню (3D): `set_background_sheet`/`set_background_sheet_visible`, разводка в `_on_bg_select`/`_on_bg_overlay_toggled`/`_reset_background`. Тест `test_view3d_bg_sheet_toggle`. Счётчик 334→335.

`#101` — «0 по оси Y не зафиксирован» в окне среза/выборки (`awf/ui/panels.py`): `_lock_views_to_data` (#89) ограничивал только X, нижняя граница Y была свободна — при зуме/панораме «0» (лин.) и дно лог-оси уезжали. Добавлен `_lock_spectrum_y()` (зовётся из `_render_spectrum`, т.е. на каждую перерисовку/смену единиц/сглаживание/переключение лог↔лин/фон): лин. → `setLimits(yMin=0)` (счёт ≥ 0, ноль прибит к низу); лог → `setLimits(yMin=log10(min>0))` — пол по минимальному положительному отображаемому значению (вид pyqtgraph в лог-режиме = координаты log10, поэтому floor через `log10`). Убирает пустой провал лог-оси до ~1e-10 в ВЭ-хвосте. Тесты `test_slice_spectrum_y_floor_linear` + `test_slice_spectrum_y_floor_log`. Счётчик 335→337.

`#102` — добавить палитры в окно «Цветовая палитра» (`awf/ui/colormaps.py` + новый `awf/ui/palette_dialog.py` + `awf/ui/main_window.py`): было 3 палитры, стало 15 — Insight (дефолт) + 14 со скриншота-макета (Inferno/Magma/Plasma/Viridis/Cividis/Turbo/Jet/Hot/Ocean/Parula/Cubehelix/Spectral/Cool/Grayscale). `COLORMAPS` → `(ключ, подпись, описание)`. `get_colormap` чинён: pyqtgraph-источник, затем `source="matplotlib"` (jet/hot/ocean/cubehelix/cool/gray/Spectral резолвятся только так — иначе молчаливый fallback на Insight); `parula` нет нигде (лицензия MathWorks) → кастомные контрольные точки; `Spectral` только с заглавной. Новый модальный `PaletteDialog` (тёмная тема): строки с превью-градиентом (`getLookupTable` → скруглённый QPixmap) + название + описание, клик = выбор + подсветка + сигнал `selected(key)` для живого применения. Кнопка-цикл палитр (#74) в тулбаре заменена на кнопку-открытие окна; `_apply_colormap` единообразит 2D/3D + переразмещает сечения. Тесты `test_registry_has_screenshot_palettes` + `test_added_palettes_resolve_distinct_from_insight` (нет молчаливого fallback) + `test_palette_dialog_lists_all_and_selects`. Счётчик 337→340.

`#103` — `.rcspg` RadiaCode не загружался (`JSONDecodeError`): `load_rcspg` (`awf/io/rcspg_loader.py`) делал `json.load`, но **Android-экспорт** RadiaCode — табулированный ТЕКСТ, не JSON (JSON — формат **iOS-экспорта**; оператор подтвердил, что нужны оба). Загрузчик переписан в диспетчер по формату: первый непустой символ `{` → JSON-ветка `_load_rcspg_json` (iOS, прежняя логика сохранена), иначе → новая текст-ветка `_load_rcspg_text` (Android). Текст-формат: заголовок `Spectrogram:…` (tab: `Channels`/`Timestamp`=Windows-FILETIME/`Accumulation time`/`Device serial`); строка `Spectrum: <hex>` → калибровка `[a0,a1,a2]` из 3×float32 LE после 4 служебных байт (преднакопленный спектр как срез НЕ берётся); строки-интервалы `<FILETIME>\t<живых_сек>\t<отсчёты>` с опущенными хвостовыми нулями → паддинг до `Channels`. `live_time_s` = поле живых секунд (Σ = Accumulation time ТОЧНО), `time_offsets_s`/`real_time_s` из разностей FILETIME-меток (целочисленно по тикам). FILETIME = **UTC** (заголовочный `Time` — локальное время прибора). Проверено на реальном файле: 1236×1024, калибровка [3.94, 2.37, 3.8e-4], live_total 1624 c. Заголовок диалога ошибки «Ошибка загрузки N42» → «Ошибка загрузки» (`main_window.py:550`). 9 Android-тестов (`tests/test_rcspg_android_text.py`), iOS/JSON-тест не тронут. Счётчик 340→349.

`#104` — спектрограмма с мощностью дозы из `.rcspg` RadiaCode (новый `awf/model/dose.py` + `awf/ui/panels.py` + `awf/ui/main_window.py` + тесты `test_dose.py`/`test_dose_ui.py`): в `.rcspg` мощности дозы НЕТ (только отсчёты — доказано `scripts/inspect_rcspg.py`), поэтому доза ВЫЧИСЛЯЕТСЯ из спектра. `dose_rate_series(sg)` = энерговклад `P[k]=Σ_i cps[k,i]·E_i` (кэВ/с, посл. канал отброшен IV-R5) × `DOSE_CAL_RC103`=3.345e-7 (мЗв/ч)/(кэВ/с). Константа ЭМПИРИЧЕСКИ подогнана под эталон приложения RadiaCode-103 по пику (`P_peak`=2.24e8 кэВ/с на срезе 632 → 75 мЗв/ч); вторая опорная точка ≈2 мЗв/ч согласована той же линейной `k` (пик/полка ≈37× ≈ 75/2). Провенанс размечен в docstring (P измерено / `k_cal` подогнано / линейность предположена); оговорка сатурации (на пике ~1.9e5 1/с детектор в мёртвом времени, «75+» — нижняя оценка). Физ-сверка `absorbed_dose_rate_in_crystal` Ø8×8 мм CsI(Tl) → 71.3 мЗв/ч (тот же порядок). UI: вторая ось Y (мЗв/ч, авто-мкЗв/ч если max<1) справа на нижнем графике отсчётов (`SlicePanel._series_plot`, второй `ViewBox` + `sigResized`-синк), переключатель в меню «Анализ»; §23-гейт — оверлей только для RadiaCode-источника (`source_path` оканчивается на `.rcspg`), для прочих детекторов калибровка RC-103 неприменима (пункт disabled). Валидация на реальном файле (`scripts/dose_overlay_smoke.py`): пик 75.0 / медиана 3.94 / min 0.016 мЗв/ч, 1236 точек. PII (serial) в код не зашит. Счётчик 349→373.

`#105` — легенда мощности дозы на нижнем графике отсчётов (`awf/ui/panels.py` + `tests/test_dose_ui.py`): кривая дозы #104 живёт в отдельном правом `ViewBox`, поэтому `addLegend()` основного графика её не подхватывал (в легенде были только «полоса ROI»/«энергоокно»). Объект легенды захвачен в `self._legend`; новый `_set_dose_legend(on, unit_lbl)` add/remove записи «мощность дозы, <ед>» через `LegendItem.addItem/removeItem`, вызывается из `_draw_dose_overlay` синхронно с кривой → для `.aswf`/`.n42` (дозы нет) записи нет, при `set_dose_overlay(False)` исчезает. Единицы (мЗв/ч либо мкЗв/ч) из `_dose_unit`. +3 теста (наличие для `.rcspg`, отсутствие для `.n42`, тоггл). Счётчик 373→376.

`#106` — переключение языка интерфейса RU↔EN, MVP (новый `awf/ui/i18n.py` + `awf/ui/main_window.py` + новый `tests/test_i18n.py` + правка `tests/test_group4_remarks.py`). Дизайн: словарь `TRANSLATIONS[en][ru] -> en`, ключ перевода = русская строка, `tr(ru)` возвращает en-перевод либо сам ru-fallback — позволяет переводить инкрементально без поломок (необёрнутое остаётся русским). Глобальный `_Signals.changed = Signal(str)` эмитится `set_language(code)` только при реальной смене; `MainWindow` ловит → проходит по реестру `self._i18n_widgets: list[(setter, ru_key)]` и переприменяет `tr(key)`. Регистрация: `self._register_i18n(setter, ru_key)` — сразу применяет `tr(ru_key)` и запоминает пару (для виджетов с индексом — `lambda s: tabs.setTabText(idx, s)`). Покрыто: заголовок окна, подписи вкладок (3D Waterfall / 2D Карта / Аналитика), меню Файл (+Открыть…/Выход), Изотопы/Анализ/Сервис/Помощь/О программе, действия в «Анализ» (Выбор фона.../Наложение/Вычет/Мощность дозы + tooltip), заголовок тулбара «Вид» и метка «Z-шкала», статусбар «Готов…», заголовки `QFileDialog`/`QMessageBox.critical`. Новое подменю «Сервис → Язык» с `QActionGroup` (exclusive checkable): Русский/English, клик → `i18n.set_language(code)`. Сохранение в `QSettings` ключ `interface/language`, загрузка в начале `__init__` (до построения меню; снят дублирующий `self._settings = QSettings(...)` ниже по `__init__`). Покрытие словаря — менюшки/тулбар/вкладки/диалоги; остальные UI-строки (`panels.py`/`view3d.py`/диалог фона) остаются русскими fallback'ом — добавление переводов туда не меняет код. +8 i18n-тестов (default ru, tr ru-mode, tr en-mode для 6 ключей, fallback на неизвестный, сигнал только при смене, неизвестный код игнорируется, en-переводы вкладок, ключ настроек). Регрессия `test_top_menus_skeleton_present` (#75) скорректирована: «Сервис» исключён из проверки disabled-каркаса, добавлена проверка наличия пункта «Язык». Счётчик 376→384.

`#107` — поиск пиков (Задача 2 MVP), новый Qt-free модуль `awf/analysis/peaks.py` + тесты `tests/test_peaks.py` + экспорт в `awf/analysis/__init__.py`. Порт алгоритма Mariscotti (1967) + критерий значимости Currie L_C из SpectraVibe `gamma-spectrum-analysis/scripts/gamma/peaks/search.py` `mariscotti_search()` (стр. 261–520). Публичная `find_peaks(counts_1d, fwhm_channels, *, sigma_threshold=3.0, spike_min_fwhm_frac=0.3, min_separation_factor=1.0, band_ratio=1.2, energies=None) -> list[FoundPeak]`. Пайплайн: фильтр −G'' (вторая производная гауссиана, L2-норма) свёрткой со спектром, нормировка на локальную дисперсию `var=conv(max(seg,1),kernel²)` → массив значимости; полосовая сегментация по адаптивной FWHM (геом. среднее, BUG-56 половина ядра 1.0·FWHM при FWHM≥15, иначе 1.5·FWHM); маска краёв `⌈2·FWHM⌉`; локальные максимумы значимости > порога; жадный отбор по убыванию значимости с мин. расстоянием `factor·max(fwhm)`; континуум из боковых окон [2·FWHM..3·FWHM] с **лог-линейной интерполяцией baseline (BUG-21)**; спайк-фильтр по полувысоте (F-139, порог `spike_min_fwhm_frac`); площадь `2.507·σ·net_height`. Адаптация под проектный `FoundPeak` (frozen): `channel=float`, добавлено `energy` (из опц. массива `energies`, иначе = номер канала), ROI `sigma_area_estimate` SpectraVibe **не переносился** (поля нет). Кодоген Ollama `qwen3-coder:30b` из спек `scripts/ollama/_spec_peaks.md`/`_spec_test_peaks.md` (хелпер `scripts/ollama/_gen.py`); анти-галлюцинационная сверка с `search.py` построчно. **2 правки после ревью/тестов:** (1) `_partition_into_bands` — порт сравнивал каждый канал с `band_hi·ratio` вместо `max/min`-отношения растущей полосы (расходится при убывающей FWHM); (2) `_enforce_separation_adaptive` — инвертированный оператор `<` вместо `>=` (принимал кандидат, если он БЛИЗОК ко всем принятым) → находился только сильнейший пик; синтетический оракул это поймал. Тест: 3 гауссианы (h=400/1200/2500, FWHM=8) на наклонном фоне + 1-канальный спайк → ровно 3 пика, центры ±2, значимость монотонна по высоте, спайк отсеян (контраст on/off), energies→energy, n<50→[], callable==scalar. Счётчик 384→392.

`#108` — поиск пиков заведён в UI (подзадача 2.6 Задачи 2): `awf/ui/panels.py` + `awf/ui/main_window.py` + новый `tests/test_peaks_ui.py`. Меню «Анализ» → новый чекбокс-пункт «Поиск пиков» (`self._act_peaks`, после сепаратора за «Мощность дозы»; i18n-регистрация text+tooltip; обработчик `_on_peaks_toggled` → `SlicePanel.set_peak_search(on)`). В `SlicePanel`: состояние `self._peak_lines`/`self._peaks_visible`; `set_peak_search(on)` хранит флаг и зовёт `_draw_peaks()`; `_draw_peaks()` снимает прежние маркеры, при `_peaks_visible` гоняет `find_peaks(self._raw_spec[1], PEAK_FWHM_CHANNELS, energies=self._energies)` по **сырым отсчётам кэшированного спектра** и рисует зелёные пунктирные `InfiniteLine` (angle=90) с подписью энергии. Пересчёт привязан к `_plot_spectrum` (хук в конце) → маркеры обновляются при смене ROI/среза/файла; единицы (counts/cps) и сглаживание на положения пиков не влияют (find_peaks по сырым counts, метки в кэВ из калибровки). Модельная FWHM (Задача 8) ещё не готова → константа `PEAK_FWHM_CHANNELS=8.0` (модуль `panels.py`) как приемлемая стартовая точка. Пункт всегда доступен; до загрузки файла `_draw_peaks` безопасно выходит (нет `_raw_spec`). +6 offscreen-тестов (скрыты по умолчанию, ровно 3 пика на 3 гауссианах, маркеры на калиброванных энергиях ±3 каналов, off снимает маркеры, пересчёт при смене спектра, безопасность без спектра). Счётчик 392→398.

`#109` — тулбар «Вид»: поправлен цвет и размер шрифта кнопки палитры (`awf/ui/style.py`, только QSS). Симптом по скриншоту: текст «Viridis» в кнопке палитры мельче и тусклее соседних подписей/кнопок («Палитра:», «Единицы:»). Причина: кнопка палитры — `QtWidgets.QToolButton` (`main_window.py` `self._cmap_btn`), а в `APP_QSS` тулбар-правила были только для `QToolBar QLabel/QCheckBox/QComboBox/QPushButton` — для `QToolButton` правила НЕ было, поэтому она рендерилась дефолтным (мельче, тусклее). CycleButton'ы (Z-шкала/Единицы) наследуют `QPushButton` → уже стилизованы. Добавлено правило `QToolBar QToolButton` (font-size 14px, color #d8dade, фон/рамка/радиус как у соседей, padding 6px 14px, min-height 28px) + `:hover`/`:pressed`. Чистая QSS-правка, кода/логики не трогает. Тестов не добавлял (offscreen не рендерит шрифты — px не резолвится; визуально проверяет оператор). Полный pytest — 398 passed (без регрессий).

`#110` — поиск пиков перенесён со спектра среза на **3D-водопад**, маркер пика рисуется **на поле рельефа** (`awf/ui/view3d.py` + `awf/ui/panels.py` + `awf/ui/main_window.py` + `awf/ui/i18n.py` + переписан `tests/test_peaks_ui.py`). Оператор: «Поиск пиков нужно делать на 3Д спектрограмме, а не в окне среза» → первая реализация (зелёные **вертикальные лучи у ребра** оси энергии) отвергнута со скриншотом: «не понял в чём идея. Пики должны на поле спектрограммы находиться и выделяться». Итог: фотопик в водопаде — это **гребень рельефа вдоль оси времени** на постоянной энергии, поэтому каждый пик выделяется зелёной линией **по гребню рельефа на канале своей энергии вдоль всей оси времени** (на ПОЛЕ спектрограммы, не у ребра). Откат UI-части #108: из `SlicePanel` удалены `_peak_lines`/`_peaks_visible`/`set_peak_search`/`_draw_peaks` и импорт `find_peaks`/константа `PEAK_FWHM_CHANNELS` (хук в `_plot_spectrum` снят). В `Waterfall3DView`: импорт `find_peaks`, константы `PEAK_FWHM_CHANNELS=8.0`/`_PEAK_RAY_RGBA` (зелёный), состояние `_peaks_on`/`_peak_ridge_items`; `set_peak_search(on)` → `_rebuild_peak_ridges()`. `_found_peak_energies()` гоняет `find_peaks` по сырым counts **полноразрешённого** `sg.total_spectrum()` (`energies=sg.energies()`) → список энергий пиков (LOD/единицы на положение не влияют). `_rebuild_peak_ridges()` для каждой энергии находит индекс канала LOD `jc=round(np.interp(e, ch_centers, idx))` и зовёт `_add_peak_ridge(jc)`: полилиния `GLLinePlotItem` (`mode="line_strip"`) по всем индексам времени `i` — точка мира `(i−nt/2, jc−nc/2, z_surface[i,jc]+lift)`, где `lift=0.01·height_scale` (анти-z-fighting); пики выше отображаемого диапазона (#78, 3000 кэВ) отсекаются по `[cc[0],cc[-1]]`; `glOptions="opaque"` даёт depth-occlusion (#95 — линия за более высоким рельефом перекрывается, не просвечивает). Хук перестройки — только `set_spectrogram` (хребет идёт вдоль всей оси времени и от стороны камеры не зависит, в отличие от лучей-у-ребра → вызов в `_maybe_reorient_labels` убран). `main_window._on_peaks_toggled` → `self._view3d.set_peak_search(on)`; комментарий/tooltip пункта меню «Поиск пиков» → «…на 3D-спектрограмме», добавлены EN-переводы (`Peak search` / `Mark detected photopeaks on the 3D spectrogram`). Модельная FWHM (Задача 8) не готова → константа 8.0 как стартовая точка (как в #108). `tests/test_peaks_ui.py` переписан под `Waterfall3DView` (7 offscreen-тестов: скрыты по умолчанию, 3 хребта на 3 гауссианах, `_found_peak_energies` на калиброванных энергиях ±3 каналов, off снимает хребты, перестройка при новом спектре, безопасность без спектра, геометрия хребта — линия вдоль всей оси времени при постоянном Y, Z по `z_surface[:,jc]+lift`, depth-tested). Счётчик 398→399.

`#133` — **порт цепочечного (decay-chain) равновесия из SpectraVibe** (новый Qt-free `awf/analysis/chain_equilibrium.py` + `tests/test_chain_equilibrium.py` + экспорт в `awf/analysis/__init__.py`; спецификации §16 `scripts/ollama/_spec_chain_equilibrium.md`/`_spec_test_chain_equilibrium.md`). Порт из `gamma-spectrum-analysis/scripts/gamma/identification/chain_equilibrium.py` (зона `spectravibe-dev` — **read-only** справочник, §12), переписан под структуры вьюера (`IdentResult`/`LineMatch`/`FoundPeak`), а НЕ скопирован (оригинал зависит от `gamma.identification.proportionality` и полей `library_E_keV`/`significance_currie`/`peak_area`, которых во вьюере нет). Усиливает #130/#131: после идентификации линий собирает природную распадную цепочку и проверяет пропорциональность интенсивностей **ВНУТРИ групп** (различает настоящую цепочку и одиночный ложный нуклид). Группы по имени нуклида: Ra-226 — A `Ra-226` (intrinsic 186), B `Pb-214`+`Bi-214` (дочки Rn-222), C `Pb-210` (свинец защиты, информационно, НЕ валидирует); Th-232 — `Ac-228`/`Pb-212`/`Bi-212`/`Tl-208` (Tl-208 с ветвлением Bi-212→Tl-208 **0.36**, цит. оригинал стр.104). Пропорциональность **эфф-корректная** — переиспользует `relative_efficiency` из `identify.py` (НЕ дублирует кривую); попарно `expected=(I_i·eff_i·br_i)/(I_j·eff_j·br_j)`, допуск по умолчанию 5.0× (широкий — линии разнесены 186↔2614 кэВ), PASS при ≥60 % прошедших пар, <2 линий → отложено. Удержание Rn = `rn_retention_ratio` = B/A по эфф-нормированным опорным линиям Ra-226 186 / Bi-214 609 (1.0=равновесие, <1=утечка Rn) — эфф-нормировка скорректирована против оригинала (оригинал делил только на I, стр.204/212) для семантики «1.0=равновесие». Публичный API: `analyze_chains(ident_results, found_peaks)` → `list[ChainResult]` (только цепочки с найденными членами) + аддитивная обёртка `identify_with_chains(found_peaks, library, …)` = `identify_peaks(...)` + `analyze_chains(...)` (НЕ трогает `identify_peaks`, защищает прежние тесты). Кодоген Ollama `qwen3-coder:30b` из спецификаций (§16), py_compile + 8 синтетических тестов (Th-232 консистентна, ветвление Tl-208 значимо при узком допуске, Ra-226 равновесие retention≈1.0, утечка Rn retention≈0.3, Pb-210 информационна, неродственный спектр→[], одиночная линия→отложено, smoke). UI-пометка линий «член цепочки X» (часть шага 3 плана) — отдельная UI-задача под визуальное подтверждение оператора. **Полный pytest — 530 passed.**

`#131` — **авто-сегментация по времени + поканальная идентификация нуклидов** (`awf/model/segment.py` + `awf/ui/segments_panel.py` + `awf/ui/main_window.py` + `awf/ui/i18n.py` + `tests/test_segment.py` + `tests/test_segments_ui.py`). Мотивация: спектрограмма структурирована по времени — торированный вольфрамовый TIG-электрод (цепочка Th-232) доминирует во временно-интегральном суммарном спектре, а урановое стекло (U-238 + немного U-235) и K-40 проявляются в ПОЗДНЕМ временно́м сегменте и тонут в сумме. Авто-сегментация изолирует каждый временно́й режим, и слабый источник всплывает в своём сегменте. Бэкенд: `segment_by_time()` — top-down best-first Пуассон-change-point по макро-бинам (BIC-штраф `pen_factor·K·ln(max(2,ns))`, дефолты `pen_factor=2.0`, `max_segments=12`, `min_slices=3`, `n_bands=32`); `identify_segments()` переиспользует `find_peaks`+`identify_peaks`, модель FWHM(E) строится ОДИН раз из суммарного спектра (разрешение от времени не зависит) и шарится между сегментами. UI: `SegmentsPanel` (дерево «Сегмент → Нуклид», контрол `_pen_box` 0.5–8.0, кнопка «Сегментировать», статус «Сегментов: N»), док `dock_segments` табифицирован с «Найденные пики», пункт в меню «Анализ» (`_act_segments`) и в меню «Инструменты». Кодоген UI-теста через Ollama qwen3-coder:30b (спецификация `scripts/ollama/_spec_test_segments_ui.md`, §16). Тесты: 10 backend (`test_segment.py`) + 6 UI offscreen (`test_segments_ui.py`) — наличие виджетов/действия, безопасный пустой пересчёт, построение дерева ≥2 сегментов, обновление статуса, физика «K-40 в более раннем сегменте, чем Cs-137», пункт в меню «Инструменты»; все язык-независимы (assert через `i18n.tr(...)`, т.к. MainWindow восстанавливает сохранённый язык из QSettings). **Полный pytest — 522 passed.** (Примечание: записи журнала `#111`–`#130` велись в `TASKS.md` и в этот журнал не дублировались — отсюда скачок счётчика 399→522.)

`#134` — **вычет фона некорректен, когда фон = самому спектру (должен уходить в 0)** (`awf/model/background.py` — `subtract_background`; `tests/test_background.py`, `tests/test_bg_ui.py`). Симптом оператора: при выборе фоном того же файла (или всего временно́го диапазона) вычет обязан сокращать интегральный спектр в ноль, но давал заметный положительный остаток. Диагностика на реальном файле (256 срезов × 8191 канал, Σсчёт 2 522 742, Σlive 15360 с; путь через argv, gitignored `scripts/diag/bg134_selfsub.py`): поканальный фон — это скорость `bg_cps[ch]=total_counts[ch]/total_lt`, и per-slice остаток `counts−bg_cps·lt` суммируется по времени в **точный ноль** (`total_counts−bg_cps·total_lt=0`), НО `np.clip(sub,0,None)` в `subtract_background` **асимметрично** разрушал сокращение — срезы выше средней скорости (положительный остаток) выживали, ниже средней (отрицательный) прибивались к 0 → ложный **+18.60 %** интеграл (469219 из 2522742) вместо 0; клип брал лишь «положительную половину» шумовых флуктуаций вокруг средней. **Фикс:** убран `np.clip(sub,0.0,None,out=sub)` — вычтенная спектрограмма хранит **знаковый** остаток. Безопасно для видов: `zscale._base_transform` уже делает `np.maximum(a,0)` для всех режимов (lin/sqrt/log), т.е. 3D-рельеф и 2D-карта гасят отрицательные ячейки на этапе ОТОБРАЖЕНИЯ и визуально не меняются; интегральный спектр среза считается из сырых counts и теперь уходит в ~0. Grep подтвердил — иной код на неотрицательность вычтенных counts не опирался; знаковый остаток и статистически корректнее в общем случае (отдельный лёгкий фон-файл) — снимает восходящее смещение клипа. Overlay (наложение кривой фона) при фон=весь файл совпадает со спектром по построению (`bg_cps·Σlt≡Σcounts`) — правки не требовал, только тест-замок. Тесты: `test_subtract_clips_negatives_to_zero`→`test_subtract_keeps_signed_net` (знаковый −2, не клип к 0), +`test_self_subtraction_integrates_to_zero` (диапазон-фон: интеграл≈0, отрицательные присутствуют), +`test_self_subtraction_via_file_path_integrates_to_zero` (`background_from_spectrogram`: интеграл≈0), +`test_overlay_equals_spectrum_when_bg_is_whole_file`; в `test_bg_ui.py::test_mainwindow_subtract_reduces_waterfall` старый assert `(counts>=0).all()` заменён на инвариант #134 (интеграл≈0 + знаковость). Диагностика после фикса: остаток **−0.00 %** (обе ветки). **Полный pytest — 535 passed** (530 + 3 нетто-новых; 2 теста переписаны на месте). Закоммичено `20c6d4d` вместе с `5de21e3` (#129, см. `TASKS.md`) отдельными коммитами при разборе рабочего дерева после #REL-очистки — оба фикса были готовы в дереве, но не закоммичены.

`#REL-1..#REL-4` — **гигиена релиза на GitHub** (по внешнему план-ревью «4 падающих теста + дефекты упаковки/CI»; оператор уточнил: анализ состояния **чистого GitHub-релиза**, НЕ локального дерева). Ключевое расхождение: на чистом клоне репо `scipy` отсутствует в `requirements.txt`, а `sample_data/*.n42` gitignored → 4 падения (`peaks.py` `curve_fit`, `deconvolve.py` `lsq_linear`) + 3 skip (нет sample). Локально (scipy 1.17.1 + sample) — 535 passed. **#REL-1:** `scipy>=1.13` добавлен в `requirements.txt` (жёсткая production-зависимость: `peaks.py:88` `curve_fit`, `deconvolve.py:61` `lsq_linear`), + смягчён fallback-ассерт `tests/test_fwhm_calibration.py:104` (`model.b < 0.99*_DEF_B` — страхует ветку без scipy, где авто-калибровка не сходится и `b==_DEF_B`). **#REL-2:** новый `pyproject.toml` (PEP 621, setuptools>=68): `name`/`dynamic version` из `awf.__version__`, зависимости-зеркало requirements, `dev=[pytest]`, gui-script `atomspectra=awf.ui.main_window:main`, `packages=[awf,awf.model,awf.io,awf.ui,awf.analysis]`, `package-data awf=[data/*.json]` + `include-package-data=false`. Разгадка «73 CSV в wheel»: setuptools при сборке pip из НЕ-git временной копии не читает `.gitignore` и сметает физически лежащий `data/iaea_cache/*.csv`; но `git ls-files` = **0 отслеживаемых CSV** (gitignored, в репо не попадал) → wheel из чистого клона их не содержит. Проверено сборкой из staging только с git-tracked файлами: `iaea csv in wheel: 0`, `nuclides.json`+`nuclide_categories.json` на месте, entry-point `[gui_scripts] atomspectra=awf.ui.main_window:main`, 41 py-модуль. **#REL-3:** `.github/workflows/tests.yml` (ubuntu-latest, matrix py 3.12/3.13 + 3.14 experimental `continue-on-error`; apt `libegl1 libgl1 libxkbcommon0 libdbus-1-3` для headless-PySide6; `pip install .[dev]`; `pytest -q` с `QT_QPA_PLATFORM=offscreen`, `PYTHONIOENCODING=utf-8`). `.gitignore` дополнен `*.egg-info/`. **#REL-4:** выполнен перенос AI-артефактов в `docs/dev/` через `git mv` (оператор выбрал расширенный scope — **и `KNOWN_ISSUES.md` тоже**): `CLAUDE.md`→`docs/dev/CLAUDE_NOTES.md`, `TASKS.md`, `IMPLEMENTATION_PLAN.md`, `KNOWN_ISSUES.md`, `_specs/`. В корне — тонкая заглушка `CLAUDE.md` (автозагрузка + инварианты §24/§12/offscreen/scipy/§5). Ссылки на `KNOWN_ISSUES.md` в `README.md`/`INSTALL.md` поправлены на новый путь. `awf.__version__=0.1.0` резолвится. **Полный pytest после переноса — 535 passed.** **Push НЕ выполнялся — гейт оператора (§24).**

`#135` — **наложение фона («простыня») на 3D-водопаде некорректно при фон=образец** (`awf/ui/view3d.py` — константа `_BG_SHEET_K=3.0` + переписан `_rebuild_bg_sheet`; `tests/test_bg_ui.py` +1 тест). Тот же корень, что #136: рельеф строится через `downsample(method="max")` (max-LOD), а старая простыня стояла на СРЕДНЕМ поканальном cps (`bg[ch]`) — медиана ~3.7 % от max-пулингового рельефа (13–27× инфляция max-пула на низкосчётных данных), «тонула» у дна и не совпадала с поверхностью при фон=образец. По решению оператора (AskUserQuestion → «Критуровень Currie (реком.)») простыня переведена на **критический уровень Currie** `bg·lt + k·√(bg·lt)` (counts) / `bg + k·√(bg/lt)` (cps), тот же `k=3`, что в гашении #136, прорежённый тем же max-LOD (`downsample(method="max", data=crit)`) и отображённый через ту же карту value→height рельефа (`_z_counts`→`_z_surface`). Смысл: простыня = «детекционный пол» — уровень, ниже которого поверхность статистически неотличима от фона (ровно то, что #136 гасит в ноль); согласовано с вычетом. Реальный файл (256 срезов × 512 каналов, argv, `scripts/diag/bg135_crit_probe.py`, gitignored): медиана простыня/рельеф(max) поднялась с **0.037** до **0.483**. Тест `test_view3d_bg_sheet_currie_raised` — при фон=образец высоты простыни не ниже «среднего» уровня и строго выше в целом. `_bg_unit_factor()` стала dead-code (единственный вызов заменён) — оставлена, чтобы минимизировать churn. **Полный pytest — 540 passed** (539 + 1). Ждёт визуального подтверждения оператора.

`#136` — **вычет фона не читается как ноль на 3D/2D-водопаде (рецидив/неполнота #134)** (`awf/model/background.py` — новая `gate_significant_net`; `awf/ui/main_window.py` — helper `_gated_for_3d` + проводка в `_redistribute`; `tests/test_background.py` +4 теста). Симптом оператора: «вычет опять не нулевой» — после #134 интегральный спектр среза уходит в 0, но 3D-рельеф всё равно показывал ненулевой «остаток» при фон=весь файл. **Корень (systematic-debugging, Phase 1):** рельеф — max-LOD (`downsample(method="max")`), показывает per-slice пуассоновы МАКСИМУМЫ; знаковый нетто #134 (`net=counts−bg·lt`) сокращается только в ИНТЕГРАЛЕ по времени, а каждый отдельный срез несёт счётный шум √N (σ≈√(bg·lt)), max-пулинг которого не ноль. Т.е. #134 корректен (интеграл=0), но max-LOD по определению не может показать ноль на поканальном самовычете — это НЕ регрессия #134, а отдельный слой отображения. **Фикс:** значимостное гашение Currie — `gate_significant_net(sub_sg, bg_cps, k=3.0)` обнуляет ячейки `|net| < k·σ` (критический уровень Currie, σ=√(bg·lt)); при самовычете гаснет ~весь водопад (реальный файл: **0.13 %** ячеек переживают при k=3 → читается как ноль, как «вычет одинаковых спектров» в BecqMoni). Применяется к ОТДЕЛЬНОЙ копии для 3D+2D (`_gated_for_3d(sg)` → `view3d.set_spectrogram` + `heatmap.set_spectrogram`); срезы (`_slices`) остаются на ЗНАКОВОЙ модели #134 (интегральный спектр = точный ноль, не трогаем — иначе сломается инвариант #134). Реальный файл: знаковый интеграл вычета ≈**3.66e-13** (#134 держится). Тесты: `test_gate_zeros_subthreshold_noise`, `test_gate_keeps_significant_net`, `test_gate_returns_new_spectrogram_without_mutating`, `test_gate_length_mismatch_raises`. **Полный pytest — 539 passed** (535 + 4). Ждёт визуального подтверждения оператора.

`#137` — **образец и фон отображаются по-разному (образец шумный, фон сглажен) → вычет разный; усреднять образец и фон посегментно** (`awf/model/background.py` — новая `region_averaged_net`; `awf/ui/main_window.py` — переписан `_gated_for_3d` + импорт; `tests/test_background.py` +4 теста). Симптом оператора (2 скриншота окна среза): голубая кривая «образец» на выборке срезов [64:192] визуально шумнее оранжевой «фон»; «Вычитать можно только если способ отображения одинаков. Нужно определять на образце участки стабильности и усреднять — образец и фон внутри каждого участка отдельно». **Корень (systematic-debugging, Phase 1):** обе кривые используют один `smooth_counts`, но «образец» — это ПОДвыборка (меньше срезов → счётный шум √N на кривой), а «фон» — среднее по всему файлу (гладкое). Разная гладкость = «разный способ отображения» → и per-slice вычет несёт пуассонов шум, self-subtract не читается нулём без гейта #136. **Решение (выбор оператора через AskUserQuestion: «Режим усреднённый вычет»):** образец усредняется ТАК ЖЕ, как фон, но внутри каждого УЧАСТКА СТАБИЛЬНОСТИ (переиспользован `segment_by_time`, #131): в участке Ri `avg_smp_i[ch]=Σ_{t∈Ri}counts/Σ_{t∈Ri}lt` (cps/канал), `net_rate_i=avg_smp_i−bg_cps` (обе стороны — скорости, «одинаковый способ»), каждый срез t∈Ri получает `net_rate_i·lt[t]`. Применяется ТОЛЬКО при включённом «Вычет фона» (без вычета — данные как есть). Свойства (доказаны тестами): (1) образец и фон сглажены одинаково → гейт 3σ (#136) в этом режиме НЕ нужен и отключён; (2) интеграл нетто по каждому участку сохранён точно = Σ_{Ri}(counts−bg·lt) — знаковая модель среза #134 не нарушена; (3) при фон=весь файл и одном стационарном участке `avg_smp≡bg_cps ⇒ out≡0` поканально: самовычет одинаковых спектров = точный ноль (ровно «вычет одинаковых спектров = ноль» из BecqMoni, но честно — шум усреднён, а не погашен). Проводка (`_gated_for_3d`): при вычете зовёт `region_averaged_net(self._sg, self._bg_cps, segment_by_time(self._sg))` для 3D+2D-водопада; срезы (1D, `_active_spectrogram`) остаются на знаковой модели #134 — интегральный спектр среза = точный ноль, инвариант не трогаем. Реальный файл (256 срезов × 8191 канал, argv, gitignored `scripts/diag/bg137_region_probe.py`): `segment_by_time` → **1 участок стабильности** `[0:256]`; усреднённый самовычет max|нетто|=**0.000**, σ=0.0000 (ВЕСЬ водопад читается нулём, без гейта) против знакового #134 max 23.1/σ 1.10; `Σ_t` нетто = 0 точно (инвариант #134 держится). Тесты: `test_region_averaged_self_subtract_is_exact_zero`, `test_region_averaged_preserves_per_region_integral`, `test_region_averaged_is_smooth_within_region`, `test_region_averaged_length_mismatch_raises`. **Полный pytest — 544 passed** (540 + 4). Ждёт визуального подтверждения оператора.

`#138` — **откат режима «усреднённый вычет» (#137): 3D/2D показывать прямой поканальный вычет сырых данных** (`awf/ui/main_window.py` — удалён `_gated_for_3d`, упрощён `_redistribute`, очищен импорт background). Оператор (скриншот 3D: ступенчатые оранжевые «плиты» от посегментного усреднения на файле с источниками, выборка срезов [64:192]): «не устраивает. Вычитай сырые данные поканально. Ничего не усредняй». Решение #137 (усреднение образца и фона внутри участков стабильности) визуально давало ровные плиты внутри каждого участка — отвергнуто. **Откат:** 3D, 2D и срезы теперь получают ОДНУ И ТУ ЖЕ активную спектрограмму — прямой знаковый поканальный вычет `subtract_background(self._sg, self._bg_cps)` (#134); посегментное усреднение (`region_averaged_net`, #137) и гейт 3σ (`gate_significant_net`, #136) **убраны из проводки** 3D/2D. `_active_spectrogram()` (знаковый #134) — единственный источник для всех трёх видов, способ отображения везде одинаков. Функции `region_averaged_net`/`gate_significant_net` и их модульные тесты оставлены в `awf/model/background.py` как исторические (не в проводке, минимум churn — как ранее с #136 после #137). **Следствие (озвучено оператору):** самовычет фон=образец на 3D-рельефе (max-LOD) снова показывает per-slice пуассонов шум (σ≈√(bg·lt); на реальном файле max|нетто| 23.1/σ 1.10), а НЕ читается точным нулём, как давали #136/#137 — это прямое следствие «ничего не усредняй»; интегральный спектр среза по-прежнему = точный ноль (инвариант #134 держится). Развилка #136↔#138 (гасить/усреднять ради «нуля» vs показывать сырой вычет) разрешена оператором в пользу сырого поканального вычета. **Полный pytest — 544 passed** (число тестов не изменилось — правка только UI-проводки).

`#139` — **в окне срезов кривая фона гладкая (усреднённый bg_cps) на фоне «лохматого» образца — сделать фон той же статистики** (`awf/model/background.py` — новая `background_window_like`; `awf/ui/panels.py` — `SlicePanel.set_background(bg_cps, raw)` + `_render_background`; `awf/ui/main_window.py` — стеш сырого фонового блока `self._bg_raw` в `_compute_background`; `tests/test_background.py` +5 тестов). Симптом оператора (скриншот окна срезов, 1D-кривые): голубая «образец» — лохматая (шумная), оранжевая «фон» — гладкая; «зачем усреднил фон? фон должен быть таким же лохматым как образец». **Корень (systematic-debugging, Phase 1, подтверждён чтением кода — НЕ угадан):** голубая «образец» = сырые суммарные отсчёты по короткому окну выборки срезов (напр. 128 срезов [64:192]) → полный пуассонов шум √N → лохматая; оранжевая «фон» = `bg_cps` (средняя скорость по ВСЕМУ фоновому диапазону, напр. 256 срезов) → шум подавлен √N → гладкая. `smooth_counts` применяется к обеим ОДИНАКОВО (один регулятор радиуса) — сглаживание НЕ причина; лохматость задаётся ЧИСЛОМ усреднённых срезов. **Решение (выбор оператора через AskUserQuestion: «Сырой фон, равный образцу по времени»):** оверлей фона в окне срезов строится как сырая сумма отсчётов фонового ПОДокна с ТЕМ ЖЕ живым временем, что у окна образца → совпадающий пуассонов шум. `background_window_like(bg_counts, bg_lt, target_lt)` суммирует сырые фоновые срезы (от начала) до накопления живого времени `target_lt` (= live-time окна образца), затем масштабирует ровно к `target_lt` (коэффициент ≈1) → та же статистика; при фоновый-диапазон=окно-образца кривые совпадают точно. Проводка: `_compute_background` для **range-источника** сташит сырой блок `self._bg_raw=(counts[lo:hi], lt[lo:hi])`, `set_background(bg, self._bg_raw)` передаёт его в `SlicePanel`; `_render_background` при наличии `_bg_raw` строит `background_window_like(cnt, lt, lt_total)` (в counts — как есть, в cps — /lt_total, единично с образцом), иначе падает на гладкий `bg_cps`. **file-источник → `_bg_raw=None`** (иная калибровка, сырой матч не строим — оверлей = гладкий `bg_cps`). **Область #139 — ТОЛЬКО 1D-оверлей окна срезов** (по скриншоту); эталон вычитания водопада (#134/#138) остаётся гладкой средней скоростью `bg_cps` (физически обязательно: один фон, много срезов — усреднение по диапазону корректно как эталон вычитания, шумить его нельзя). Тесты: `test_bg_window_full_range_equals_raw_sum` (полный диапазон = Σ сырых), `test_bg_window_scales_to_target_live_time` (масштаб к tgt), `test_bg_window_matches_sample_window` (фон=образец при совпадении окна → совпали), `test_bg_window_zero_target_returns_raw_sum`, `test_bg_window_bad_shape_raises`. **Полный pytest — 549 passed** (544 + 5). Ждёт визуального подтверждения оператора.

`#140` — **простыня фона 3D строится иначе, чем рельеф-образец — строить одинаково** (`awf/ui/view3d.py` — переписан `_rebuild_bg_sheet` + `set_background_sheet(bg_cps, raw=None)` + удалены `_BG_SHEET_K` (#135 отменён) и dead-code `_bg_unit_factor`; `awf/ui/main_window.py` — проводка `self._bg_raw`; `tests/test_bg_ui.py` — тест #135 заменён). Директива оператора: «сравнивай и построение простыни фона и образца. Они должны строиться одинаково». **Корень (Phase 1):** `_rebuild_bg_sheet` расходился с пайплайном рельефа (`set_spectrogram`) в 4 местах: (1) вход — ГЛАДКИЙ усреднённый `bg_cps` вместо сырых отсчётов; (2) критуровень Currie `+k·√(bg·lt)` (#135) — систематическая надбавка, которой у рельефа нет; (3) `max(axis=0)` по времени поверх max-LOD — двойной max; (4) не применялось пользовательское сглаживание `smooth_counts`. Итог: при фон=образец простыня ПРИНЦИПИАЛЬНО не могла совпасть с рельефом — висела над ним. **Фикс:** простыня строится ТЕМ ЖЕ способом, что рельеф: сырое фоновое окно `background_window_like(bg_counts, bg_lt, lt_ref)` (#139; `lt_ref` = медианное положительное live-time среза) → cps=`win/lt_ref` | counts=`win` → постоянная по времени матрица → тот же `downsample(method="max")` → строка `[0,:nc]` → тот же `smooth_counts(·,self._smooth)` → та же карта value→height (`_z_counts`→`_z_surface`, `np.interp`). Критуровень #135 отменён решением оператора; file-источник фона (нет сырого блока — иная калибровка) — fallback на гладкий `bg_cps` (как #139). Семантика: при фон=образец простыня идёт СКВОЗЬ рельеф в пределах пуассонова шума, а не над ним. Проводка: `main_window` передаёт `self._bg_raw` (стеш #139) вторым аргументом. Тест `test_view3d_bg_sheet_currie_raised` (проверял надбавку #135) заменён на `test_view3d_bg_sheet_built_like_relief` (высоты простыни == эталонному прогону того же пайплайна, `atol=1e-5`). **Полный pytest — 549 passed** (1 тест заменён, число не изменилось). Ждёт визуального подтверждения оператора.

`#141` — **фон из файла-источника сглажен и на 3D, и в срезах — при совпадающей энергосетке брать сырой блок** (`awf/ui/main_window.py` — ветка file в `_compute_background`; `tests/test_bg_ui.py` +2 теста). Симптом оператора (2 скриншота после #139/#140): «Ничего не исправлено. Фон сглажен и на 3d и в окне срезы. Исправляй. Фон должен выглядеть так же как образец - это один и тот же исходный файл!» — простыня 3D гладкая под лохматым рельефом, в срезах оранжевый фон гладкий против лохматого образца. **Корень (Phase 1):** #139/#140 подключили сырой блок `_bg_raw` ТОЛЬКО для **range**-источника фона; ветка **file** ставила `self._bg_raw = None` безусловно (страховка «иная калибровка» из #139) → оба потребителя (`_slices.set_background(bg, self._bg_raw)` и `_view3d.set_background_sheet(bg, self._bg_raw)`) получали None → гладкий fallback `bg_cps` везде. А оператор выбирает фоном ТОТ ЖЕ файл → энергосетка идентична, сырой блок валиден поканально. **Фикс:** в file-ветке после `load_spectrogram(path).trimmed_channels(1)` сравнивается энергосетка фона с целевой (`e_bg.size == e_t.size and np.allclose(e_bg, e_t)`; цель тоже `trimmed_channels(1)` при загрузке — сетки симметричны для одного файла): совпала → `self._bg_raw = (bg_sg.counts, bg_sg.live_time_s)` (лохматый фон в срезах #139 и на простыне #140), иная → None (гладкий `bg_cps`, как раньше). Тесты: `test_compute_background_file_same_grid_stashes_raw` (monkeypatch `load_spectrogram`, файл-фон = тот же спектр → `_bg_raw` не None, форма = целевой), `test_compute_background_file_other_grid_raw_none` (иное число каналов → `_bg_raw` сброшен в None). **Полный pytest — 551 passed** (549 + 2). Ждёт визуального подтверждения оператора.

`#CI-1` — **сломан test_i18n_peaks_strings на CI (3.12/3.13), локально 3.14 прошло — восстановить голые EN-ключи `Высота`/`Площадь`** (`awf/ui/i18n.py` — 2 ключа возвращены рядом с `Высота, отсч.`/`Площадь, отсч.` #123). Оператор: скриншот GitHub Actions (3.14 ✓ / 3.13 ✗ / 3.12 ✗) + «сверь весь проект и гит. устрани проблему насовсем». **Корень (Phase 1 systematic-debugging):** вчерашний i18n-аудит (после #169) удалил 4 «orphan» ключа (без ссылок через `tr()`), но проверял ТОЛЬКО код `awf/`, НЕ тесты. `tests/test_peaks_ui.py:355 test_i18n_peaks_strings` жёстко читает `TRANSLATIONS[LANG_EN]['Высота']` и `['Площадь']` (канонические подписи колонок таблицы #111 PeaksPanel), и без них падает `AssertionError: Нет EN-перевода: 'Высота'`. Локально 3.14 прошло, потому что после Edit файла `i18n.py` полный pytest не был перезапущен (нарушение §17.4 «после каждого прогона — сразу верификация»); CI поймал регрессию сразу. **Фикс:** оба ключа восстановлены (`Высота: Height`, `Площадь: Area`) с комментарием `#CI-1: голые ключи «Высота»/«Площадь» сохраняются для test_i18n_peaks_strings`, чтобы будущие аудиты не удаляли повторно. Другие 2 ключа (`Открыть файл`, `Сглаживание`) — реально orphan, оставлены удалёнными (проверено grep-ом по `tests/` и `awf/`). **Урок:** i18n-аудит `orphan`-логики обязан покрывать И `tests/` (а не только `awf/`), либо сопровождаться обязательным полным pytest ДО коммита. Коммит `bc28519` → rebase поверх doc-правок оператора (`f0ab2cf..34685a9`) → push. **CI run `28699367367`:** 3.12 ✓ 2m44s, 3.13 ✓ 2m58s, 3.14 ✓ 2m54s. **Полный pytest (лок., 3.14) — 623 passed** (число сохранено).

`#180` — **реальный .aswf файл не читается — v2-раскладка (`row_stride`, per-row `row_time`, `saved_rows=0`) не поддерживалась** (`awf/io/aswf_loader.py` — учёт `row_stride`/`row_time` из header + fallback `saved_rows<=0 → n_rows_file`; `tests/test_aswf_loader.py` — фикстура `_build_aswf_v2` + `_header_v2` + 2 теста). Оператор: приложил локальный `.aswf` v2-файл (LOCAL-ONLY, путь не логируем) + «почему-то файл не читается». **Корень (Phase 1 systematic-debugging, header-dump):** header реального файла — `version=2`, `channels=8192`, `row_stride=16386` (= 8192·2 + 2), `saved_rows=0`, `row_time: {dtype: uint16, unit: sec, offset: 16384}`. Прежний v1-only loader: `row_bytes = n_channels*2` жёстко (игнорил `row_stride`, читал по 16384 байта на строку — сдвиг по 2 байта после каждой строки → мусор), и `saved_rows==0` шёл в ветку `if saved_rows: n_rows = ...`, где 0 трактовался как «явные ноль строк» → `ValueError: ASWF: нет строк данных`. Прошивка v2 не обновляет `saved_rows` при выгрузке (осталось 0 из pre-alloc) — надо считать по размеру файла. **Фикс (2 хунка ≤25 строк §16):** (а) `counts_bytes = n_channels*2`; `row_stride = int(hdr.get("row_stride") or counts_bytes)` (v1 — fallback на `counts_bytes`); `n_rows_file = (EOF - data_off) // row_stride`; `saved_rows is None or <= 0` → `n_rows = n_rows_file`, иначе `min(saved_rows, n_rows_file)`. (б) Чтение `n_rows*row_stride` в 2-D массив `raw_rows[n_rows, row_stride]`, counts = первые `counts_bytes` байт строки → `.view("<u2")`. При наличии `row_time` в header и `row_stride > counts_bytes` — читаем per-row `real_time_s` по `offset+dtype` (`uint16`/`uint32`); `time_offsets_s = np.cumsum(real_time_s[:-1])` с ведущим нулём; для v1 fallback на `interval_sec` (поведение сохранено). v1 backward-compat — все прежние 11 тестов проходят без правок. **Тесты v2:** `test_v2_saved_rows_zero_falls_back_to_size` (`saved_rows=0`, 3 строки counts+row_time uint16 → `sg.n_slices==3`, counts совпадают), `test_v2_row_time_per_row` (row_time `[30, 45, 90]` → `real_time_s=[30,45,90]`, `time_offsets_s=[0,30,75]`, `live_time_s==real_time_s`). Реальный файл после фикса: `shape=(132, 8192)`, `t0` восстановлен из header, `real_time_s=[60,60,...]`, суммарный счёт 2 548 398 отсчётов. **Полный pytest — 625 passed** (623 + 2 v2). Ждёт визуального подтверждения оператора.

`#182` — **пустые пункты «Помощь» и «О программе» в главном меню — наполнить контентом** (новый `awf/ui/help_dialogs.py`, ~357 строк, PowerShell heredoc §16; `awf/ui/main_window.py` — импорт + 2 `elif`-ветки в `_build_stub_menus`; `awf/ui/i18n.py` — 3 новых RU→EN ключа; `tests/test_group4_remarks.py::test_top_menus_skeleton_present` — переписан под наполненное состояние). Оператор (скриншот главного меню приложения, оба пункта пустые/disabled с #75-каркаса): «Пиши контнет в разделы помощь и о программе. заполняй разделы. Раздел помощь подробно опиши, с оглавлением». **Решение:** новый модуль `awf/ui/help_dialogs.py` инкапсулирует оба диалога — единственный публичный API `show_help(parent)` / `show_about(parent)`, всё HTML-содержимое и константы TOC внутри модуля, main_window.py остаётся чистым (только import + 2 lambda). **`show_help`:** `QDialog` 900×700 с `QTextBrowser`, `setOpenLinks(True)`+`setOpenExternalLinks(True)` (клики по внутренним якорям `#s1..#s15` и по внешним ссылкам вроде GitHub-репо работают), кнопка «Закрыть». Оглавление на 15 разделов с якорными ссылками: (s1) Быстрый старт, (s2) Форматы файлов `.aswf`/`.n42`/`.rcspg`, (s3) Главные вкладки, (s4) 3D-водопад — управление и оси, (s5) Секущие плоскости 3D, (s6) Регулировки отображения (Z-шкала/палитра/единицы), (s7) Окно «Срезы / Сечения / Выборки», (s8) Поиск фотопиков, (s9) Библиотека нуклидов и идентификация, (s10) Сегментация по времени, (s11) Фон (выбор/наложение/вычет), (s12) Нормализация по эффективности, (s13) Аналитика, (s14) Меню приложения, (s15) Известные ограничения. **`show_about`:** `QMessageBox` с RichText — версия `awf.__version__=0.1.0`, описание, ключевые возможности, стек (`Python 3.14 · PySide6 · pyqtgraph`), лицензия, ссылка на публичный репо `github.com/VibeEngineering-LLC/waterfall-viewer` (внешний `href`). **Проводка (main_window):** в цикле `_build_stub_menus` по `[..., ("help","Помощь"), ("about","О программе")]` добавлены `elif key=="help"` (QAction «Справка…» → `show_help(self)`) и `elif key=="about"` (QAction «О программе…» → `show_about(self)`); оба зарегистрированы через `_register_i18n` для RU↔EN. **i18n:** добавлены `"Справка…"→"Help…"`, `"О программе…"→"About…"`, `"— наполняется позже —"→"— coming later —"` (последнее — на случай, если stub-строку из #75 где-то ещё используют). **Регрессия теста (Phase 1 systematic-debugging):** прежний `test_top_menus_skeleton_present` (#75) требовал `all(not a.isEnabled() for a in acts)` для help/about (проверял «disabled-каркас»); после #182 меню наполнились и пункты активны → падение. Первая попытка «переключить на all `isEnabled`» тоже упала: меню «Анализ» имеет пункты «Наложение фона»/«Вычет фона», которые условно disabled до выбора фона (#96 инвариант). **Итог теста:** для КАЖДОГО меню проверяется отсутствие stub-текста `"— наполняется позже —"` (все stubs должны исчезнуть по мере наполнения) + отдельно наличие enabled-пунктов «Справка…» в `help` и «О программе…» в `about` — не ломает #96 (Analysis-меню трогает только stub-текст, disabled-условные пункты фона проходят). **Полный pytest — 625 passed** (число сохранено; 1 тест переписан на месте, ни одна из 4 правок не создала новых тестов — контентная задача проверяется визуально оператором). Ждёт визуального подтверждения оператора.

`#184` — **сборка Windows-exe (PyInstaller onedir)** (новый `waterfall-viewer.spec` в корне; сборка `py -3.14 -m PyInstaller waterfall-viewer.spec --noconfirm` → `dist/waterfall-viewer/waterfall-viewer.exe`). PyInstaller 6.21.0, PySide6 6.11.1, Python 3.14. Грабли и решения: (1) hook PySide6 на Python 3.14 НЕ подтягивает Qt-плагины — `platforms/qwindows.dll`, `styles`, `iconengines` добавлены в `datas` вручную циклом по `PySide6/plugins/`, без них exe молча не открывает окно; (2) `collect_submodules("pyqtgraph")` обязателен — `pyqtgraph.opengl` тянет `pyqtgraph.parametertree.interactive` динамически; (3) `console=False` — при `True` рядом с окном приложения вылезает чёрное окно консоли (замечание оператора со скриншотом). Smoke-протокол: `cmd /c start /min dist\waterfall-viewer\waterfall-viewer.exe` + sleep 6 + `tasklist /FI "IMAGENAME eq waterfall-viewer.exe"` + `taskkill`. Exe подтверждён оператором визуально (окно Waterfall Viewer открывается двойным кликом из `dist/waterfall-viewer/`, без чёрной консоли). В git — только spec; `build/`/`dist/` gitignored. **Полный pytest — 625 passed** (spec на тесты не влияет).

`#185` — **оптимизация размера дистрибутива exe: 308.3 → 221.3 МБ (−87.0 МБ, −28.2%)** (`waterfall-viewer.spec` — excludes, фильтр плагинов, постбилд-шаг; по read-only аудиту контура code-audit-core, находки F1–F5/F7). Дисциплина аудитора соблюдена: по одному пункту → пересборка → smoke exe → полный pytest (625 passed после каждого). Пункты в порядке применения: **F1** `opengl32sw.dll` (software-fallback OpenGL, 20.6 МБ) — не Python-модуль, excludes бессилен; удаляется постбилд-кодом в конце spec после COLLECT (hardware GL уже требование `awf/ui/view3d.py`), −19.7 МБ; **F2** `matplotlib`+`PIL` в excludes — тянулись транзитивно через `collect_submodules("pyqtgraph")` (опциональный `MatplotlibWidget`), −50.8 МБ; **F3** `pandas` в excludes — дельта 0 (ушёл вместе с веткой F2), оставлен страховкой; **F5** `PySide6.QtTest`+`PySide6.QtNetwork` в excludes — −8.0 МБ (аудитор оценивал 2.15 МБ — с зависимыми Qt-DLL вышло больше); **F4** `imageformats` убран из цикла плагинов — awf не читает файлы изображений (QPixmap/QImage только in-memory из numpy, `Format_RGB888`), заодно ушёл `Qt6Pdf.dll` (4.6 МБ, зависимость `qpdf.dll`), −7.9 МБ; **F7** `data/iaea_cache/*.csv` убран из `collect_data_files` — не вес (0.6 МБ), а bug репродуцируемости: `pyproject.toml` декларирует «iaea_cache в дистрибутив не входит; ядро идентификации = nuclides.json», сборка зависела от локального регенерируемого кэша. Итог: 323 253 939 Б (1437 файлов) → 232 068 119 Б; scipy/numpy/PySide6-core не тронуты. **Полный pytest — 625 passed** (правки только spec).

`#186` — **`waterfall-viewer.spec` попадал под `.gitignore` — рецепт сборки exe был без версионирования** (`.gitignore` — negation-строка `!waterfall-viewer.spec` после `*.spec`). Побочная находка Addendum-перепроверки аудита code-audit-core (тот же отчёт, что #185): запись #184 декларирует «в git — только spec», но `.gitignore:23` `*.spec` (шаблонная строка «Сборка PyInstaller») глушил и наш spec — файл не отслеживался git, потеря рабочей копии = потеря рецепта сборки. Верификация: `git check-ignore -v waterfall-viewer.spec` → срабатывает negation-правило, `git status` показывает spec как untracked (виден git, готов к add). pytest не гонялся — правка только `.gitignore`, кода не касается. Заодно удалены из корня пустые артефакты smoke-прогонов #185 (`smoke_err.txt`/`smoke_out.txt`, оба 0 байт).

`#181` — **единичные шипы на границах зон 3D в лог-Z без нормировки — пуассонов квант держит плоское дно, каналы 2-5 counts против нулей выглядят иглами** (`awf/ui/zscale.py` — `_base_transform` лог-ветка: детект low-stat через `p10==p25` → floor поднимается до `max(p10, p50/2)`; тесты `test_zscale_contrast.py` не менялись — новая формула инвариантна на прежних входах). Оператор (2 скриншота 3D после #167/#168 в лог+нормировка выкл, полосы 200-2000 кэВ + Ac-228/K-40/Bi-214): «Остались единичные артефакты на границах зон. Лучше, но недостаточно. Доделывай» → уточнение через AskUserQuestion: режим = **лог + нормировка выкл**, приоритет = **одиночные иглы вверх/вниз**. **Корень (Phase 1 systematic-debugging, локальный .aswf v2-файл оператора через `scripts/diag181_log_spikes.py`):** shape=(132, 8192), max=55, `pos.min=1, pos.p10=1, pos.p50=6, zero_frac=0.744` (74.4% пустых каналов). Прежняя #167-формула `floor = percentile(pos, 10.0)` при плоском дне вырождается: p10 = pos.min() = 1 = квант пуассона → каналы со счётами 2 counts дают `log10(2/1) = 0.301`, 3 counts → 0.477, 5 counts → 0.699. На фоне нулей (`log10(1/1)=0`) это визуально иглы в 30-70% высоты рельефа. Не «артефакт рендера» — сама трансформация усиливает пуассонов шум при low-stat. **Phase 2 (гипотеза + симуляция, `scripts/diag181_floor_fix.py`):** первая попытка — `floor = max(p10, √median(pos)) = √6 = 2.449` — работает на файле оператора, но **ломает #54 test_log_scale_invariant_for_units**: `√median` не масштаб-инвариантно (`√median(x/k) = √median(x)/√k`, а перцентили линейны `p10(x/k)=p10(x)/k`) → counts и cps дают разные формы рельефа. **Phase 3 (масштаб-инвариантная формула):** признак плоского дна на пуассоновом кванте — `p10 == p25` (>=25% каналов держат минимум positive). Когда сработало → `floor = max(p10, p50/2)` (медиана положительных / 2, масштаб-инвариантно). Верификация: файл оператора → `p10=p25=1, p50=6, low_stat=True, floor=3`: x=2 → z=0 (спайк убран), x=3 → z=0, x=5 → z=0.222, x=55 (пик K-40) → z=1.263 (было 1.740, потеря 27%). Тестовые массивы #54 (`[5,50,500,5000]` counts vs `[0.25,2.5,25,250]` cps) → `p10=18.5, p25=38.75, low_stat=False, floor=p10=18.5` (та же старая логика, форма 3D `[0, 0.178, 0.589, 1]` идентична для counts и cps → инвариантность держится). Тест #167 (`[0.1]*10 + [10]*90`) → `p10=9.01, p25=10, low_stat=False, floor=p10=9.01` (старая логика, тест проходит). Никакие тесты #54/#166/#167 править не пришлось — условие `p10==p25` не срабатывает на «нормальных» распределениях. **Полный pytest — 625 passed** (число сохранено). Ждёт визуального подтверждения оператора.

`#169` — **проверить и дописать все EN-переводы (i18n RU↔EN); изоляция тестов от машинного QSettings** (`awf/ui/i18n.py` — дополнен EN-словарь; `awf/ui/analytics_panel.py`, `background_dialog.py`, `cyclebutton.py`, `knobs.py`, `main_window.py`, `nuclide_panel.py`, `palette_dialog.py`, `panels.py`, `peaks_panel.py`, `segments_panel.py`, `view3d.py` — все непереведённые RU-литералы обёрнуты `tr()`, расширены `retranslate()`; `scripts/i18n_audit.py` — скрипт аудита (0 пропущенных EN-ключей); `tests/conftest.py` — изоляция утечки языка из QSettings). Оператор: «Проверь все переводы на англ. Допиши». Аудит `scripts/i18n_audit.py` выявил ~95 незавёрнутых RU-литералов в 9 UI-модулях — все обёрнуты в `tr()`, в каждой панели расширен `retranslate()`, при lang=en UI полностью переключается. **Регрессия (Phase 1 systematic-debugging):** после правок #169 13 тестов упали (610 passed). Изолированно — проходили, в полном прогоне — нет → порядковая зависимость. Диагноз: `MainWindow.__init__` читал ключ `interface/language` из РЕАЛЬНОГО QSettings машины (`HKCU:\Software\VibeEngineering-LLC\AtomSpectraWaterfallViewer`, значение `en` — языковой выбор оператора); глобальный i18n-язык процесса устанавливался в `en` и «утекал» в соседние тесты, строившие панели напрямую → RU-ассерты на строках, впервые завёрнутых `tr()` в #169, падали. **Фикс:** `tests/conftest.py` — `_preserve_operator_language` (session-scope: сохраняет ключ реестра до сессии, восстанавливает после) + `_fresh_language` (function-scope: удаляет ключ + `i18n.reset_for_tests()` перед каждым тестом). Производственное поведение (постоянство языка в живом приложении) не изменено. Sanity-run: 21 passed, ключ реестра `en` восстановлен. **Полный pytest — 623 passed** (0 fail).

`#168` — **та же проблема в лин Z-режиме — простыня приподнялась + провалы вниз** (`awf/ui/view3d.py` — module-level helper `_floor_shift_linear` + условный вызов в `set_spectrogram` после `apply_z_scale`; `tests/test_view3d_axes.py` +3 теста). Оператор (скриншот 3D с включённой нормировкой в лин Z-режиме, 900–2800 кэВ, Tl-208 2614 и K-40 ~1461): «и тоже в лин режиме» — тот же корень #167, но лин-ветка `_base_transform` возвращает `max(a, 0)` **без floor-логики** (только для лог сделан `#167`). После ε-нормировки #156 единичные каналы ВЭ-хвоста ×10.76 образуют видимую «простыню-полку», а истинные 0-count каналы остаются на Z=0 → провалы. **Корень (Phase 1 systematic-debugging, чтение `view3d.py:260-419`):** пайплайн `counts_in_unit → downsample('max') → smooth → apply_z_scale('linear') → zn=z_disp/zmax → z_surface=zn·height_scale`; в лин-режиме `_base_transform` не режет низ, и `zn` для основной массы низкосчётных каналов оказывается на ~5–15 % высоты → визуальная «простыня». **Итер.1 (zero-cut, провалилась):** `_noise_cut_linear` обнулял значения ниже percentile-10 положительных — простыня опустилась, НО «отрицательные пики» стали ЯРЧЕ (контраст «поднятая полка ↔ 0-каналы» усилился, поскольку доля Z=0 каналов выросла). Оператор (2 скриншота лин 900–2800 кэВ / 1200–1800 кэВ + Tl-208 пик): «Не получилось. Обрезай отрицательные пики». **Итер.2 (глобальный floor-сдвиг, отклонена):** `_floor_shift_linear(z_disp)` = `max(z_disp − floor, 0.0)`, `floor = percentile-10 положительных` (глобальный скаляр). Полка опустилась, но оператор прислал новый скриншот (лин, окно 1000–1800 кэВ, иглы вниз на 1200–1800): «откуда на простыне отрицательные пики? Найди причину». **Корень игл (диагностика на реальном файле, probe в скилле `atomspectra-waterfall-viewer-dev/scripts/probe168_floor_diag.py`) — 3 звена:** (1) пуассонова дискретность — 0-count display-бины остаются Z=0 даже после LOD-downsample method="max" (8.7 % бинов полосы 1200–1600 кэВ); (2) ε-фактор ×10.76 за 1408 кэВ (#156, плоская экстраполяция Гамма-1С) раздувает квант «0↔1 отсчёт»: уровень 1 отсчёта 0.43–0.54 против нуля → игла на всю высоту полки; (3) глубина иглы **зависит от энергии**, а floor итер.2 — глобальный скаляр: на реальном файле floor=0.359 < локального уровня «1 отсчёт» 0.430 в ВЭ-полосе → полка после сдвига висела на ~0.18, нули на 0 — иглы остались. **Итер.3 (поканальный floor, текущая):** `floor_col = min(min_positive_col, cap)`; `cap = max` кванта по «дырявым» колонкам (колонки с 0-бинами среди активных строк-времён); пустые временные строки хвоста файла исключаются из подсчёта «дырок» (иначе cap взрывается и все колонки «дырявые»). Семантика: 0 и 1 отсчёт садятся ровно на Z=0 при ЛЮБОЙ энергии, настоящие пики (≥2 отсчётов) поднимаются; НЭ-континуум (min >> квант) сдвигается лишь на cap — форма сохранена. Верификация на реальном файле: полоса 1200–1600 кэВ p50 полки после сдвига 0.0013 ≈ 0 (было 0.538), zero-frac 8.7 %→38.8 % (полка слилась с нулями на плоскости основания — контрастных игл нет), пик выживает (max 2.69→2.15); НЭ 300–800 p50 0.480→0.323 (сдвиг только на квант, форма цела). Тесты переписаны под 2-D семантику: `test_floor_shift_linear_baseline_and_peaks` (колонка-«игла» 0-бин+кванты → вся в 0; колонка без нулей: полка 5→0, пик 100→95), `test_floor_shift_linear_lowE_continuum_preserved` (плотная колонка сдвигается ровно на cap, не проседает; пустая строка-хвост не делает колонки «дырявыми»), `test_floor_shift_linear_degenerate_no_crash` (+1-D вход насквозь). **Полный pytest — 623 passed** (число не изменилось: 3 теста переписаны на месте). Ждёт визуального подтверждения оператора.

`#167` — **режим нормализации 3D (лог): «отрицательные» пики и приподнятая простыня** (`awf/ui/zscale.py` — `_base_transform` лог-режим: `floor = np.percentile(pos, 10.0)` вместо `pos, 1.0`; `tests/test_zscale_contrast.py` — `_expected_log` helper переведён на perc-10, добавлен `test_log_floor_uses_percentile_10_robust_to_low_outliers`). Оператор (скриншот 3D-водопада с включённой нормализацией в лог-режиме): «в режиме нормализации (лог) появились отрицательные? пики и простыня приподнялась». Аналог #166 для 3D-вида: там же плечо лог-Y в окне Срезы цеплялось за ВЭ-выбросы, здесь тот же корень бьёт по 3D-простыне. **Корень (Phase 1 systematic-debugging, чтение `awf/ui/view3d.py` строк 280-410 → `apply_z_scale` → `_base_transform`):** после ε-нормировки #156 распределение положительных значений реформируется — ВЭ-хвост ×10.76, НЭ-полка ×~0.5, единичные каналы становятся выбросами вниз. Прежний `floor = percentile(pos, 1.0)` уползал за ними вниз → знаменатель `log10(x/floor)` малел → числитель `zn = z_disp/zmax` для основной массы каналов рос → в 3D `z_surface = zn * height_scale` вся простыня поднималась над Z=0. При этом каналы с 0 отсчётов оставались на строго Z=0 (fast path `nn` при `pos.size==0`) → визуально глубокие «провалы вниз» на фоне общего подъёма (оператор описал как «отрицательные? пики»). Верхний контраст пика не менялся (max сохранялся), плоскость сдвинулась. **Фикс:** `floor = np.percentile(pos, 10.0)` (симметрично #166). Percentile-10 устойчив к 5-10% выбросов вниз ε-нормировки; при равномерных данных ≈ min, регрессий не создаёт. Применяется к общему `_base_transform` — правит и 2D-карту (`awf/ui/panels.py::HeatmapPanel`), и 3D одинаково. Fallback на `pos.min()` при вырожденном `percentile==0`. Регрессионный тест: массив [0.1]×10 + [10.0]×90, percentile-10 = 10.0 (не улетел на 0.1), лог-диапазон стабилен. **Полный pytest — 620 passed** (619 + 1). Ждёт визуального подтверждения оператора.

`#166` — **лог-Y низ в окне «Срезы» после нормализации — не цепляться на ВЭ-выбросы** (`awf/ui/panels.py` — `_expand_y_if_needed()` и `_lock_spectrum_y()` берут `floor = np.percentile(pos, 10.0)` вместо `pos.min()`; `tests/test_bg_ui.py::test_slice_spectrum_y_floor_log` обновлён под percentile-10; `tests/test_group4_remarks.py::test_slice_update_full_y_autofit_log_mode` обновлён; новый регрессионный тест `test_slice_normalization_ve_outliers_do_not_stretch_y_bottom`). Оператор (после #164 phase 2): «не получилось. При нажатии 'нормализация' спектр в окне срезы взлетает (лог режим)»; скриншот: log-Y окно ~[0.005, 1.6], плотная часть кривой ~[0.03, 1.0] в верхней половине, гэп ~0.8 dec под ней. **Корень (Phase 1 systematic-debugging):** ε-нормировка Гамма-1С ×10.76 в ВЭ-хвосте (1408+ кэВ) помножает единичные каналы (1 count) до ~0.005-0.01 cps. Абсолютный `min(pos)` цепляется за эти изолированные выбросы, тянет `setYRange`-низ (и `setLimits(yMin)` через `_lock_spectrum_y`) на 1-2 декады ниже реального пола плотной части кривой. Стандартная лог-Y-конвенция гамма-спектрометрии — окно ~2–3 dec, охватывающее плотную часть; отдельные ВЭ-выбросы не должны его растягивать. **Фикс:** заменить `pos.min()` → `np.percentile(pos, 10.0)` в обеих функциях. Перцентиль-10 устойчив к 5–10% выбросов ВЭ-хвоста; при равномерных данных percentile-10 ≈ min (регрессии на poisson-тестах не создаёт). Итог: плотная часть кривой занимает почти всё окно снизу вверх, ВЭ-выбросы обрезаны снизу (визуально сплошная линия дискретизации нижнего края окна больше не видна). Оба места (setYRange и setLimits yMin) переведены синхронно — иначе `setLimits(yMin=log10(min))` клампит `setYRange(lo=log10(percentile10))` обратно вниз. Регрессионный тест синтезирует ВЭ-хвост: 10 срезов × 50 каналов, плотная полка 1000 counts (→10 cps), 3 из 50 каналов = 6% выбросов по 1 count (→0.01 cps); без фикса Y-низ ≈ -2, с фиксом ≈ 1 (перцентиль-10 отсекает 3 из 50 выбросов, попадая в плотную часть). Полный pytest — 619 passed (618+1). Ждёт визуального подтверждения оператора.

`#165` — **спектр в окне Срезов не должен цепляться мышью, зум колесиком относительно указателя** (`awf/ui/panels.py` — новый ViewBox-подкласс `_NoPanViewBox` в module-scope (переопределён `mouseDragEvent` → `ev.ignore()`), `SlicePanel._spectrum_plot = pg.PlotWidget(viewBox=_NoPanViewBox())`; `tests/test_group4_remarks.py::test_slice_spectrum_no_mouse_pan` — регрессионный тест). Оператор: «спектр в окне срезы не должен цепляться мышью и перемещаться, нужен зум относительно точки при зажатии кнопки»; уточнение: «зум колесиком относительно указателя мыши». Корень: pyqtgraph по умолчанию ловит left-drag на ViewBox как pan → пользователь случайно уводит спектр в сторону (плоскости сечений 3D теперь двигают срез при малейшем сдвиге мыши по спектру). Фикс: перегружаем ТОЛЬКО `mouseDragEvent` (сохраняем `mouseEnabled=[True,True]` — иначе pyqtgraph выключит и wheel-zoom). Wheel-zoom по умолчанию центрирован в точке курсора (`ViewBox.wheelEvent` → `scaleBy(anchor=ev.pos())`) — как и просил оператор, дополнительная логика не нужна. Область — только верхний график `_spectrum_plot` (спектр среза); нижний `_series_plot` (профиль полосы ROI + доза) остаётся с обычным ViewBox (там pan штатно полезен). Тест проверяет: (1) тип viewbox = `_NoPanViewBox`; (2) `mouseEnabled == [True, True]`; (3) фейковый drag-event не сдвигает `viewRange`; (4) регрессия — нижний график не подменён. Полный pytest — 618 passed (617+1). Ждёт визуального подтверждения оператора.

`#164` — **режим нормализации: спектр «улетал» в окне Срезов и выглядел не нормализованным** (`awf/ui/panels.py` — `SlicePanel._refresh_after_update()` расширен вызовом `_expand_y_if_needed()`; парный `_expand_series_y_if_needed()` для нижнего графика профиля (полоса ROI + энергоокно, `yMin=0` держим по #128, X не трогаем); `tests/test_group4_remarks.py` — `test_slice_update_expands_y_after_normalization` (лин.) + новый `test_slice_update_full_y_autofit_log_mode` (лог., с ассертами под клампинг `setLimits(yMin)` в pyqtgraph)). Оператор (2 итерации): (1) «при включении нормализации улетает спектр в окне срезы. И вид спектра не соответствует нормализованному»; (2) после первой фиксации — «не сработало. Спектр в окне Срезы оторвался от линии X и его вид не соответствует нормализованному спектру» (скриншот: лог-Y кривая висит НАД осью X с заметным зазором). Корень: ε-нормировка кривой `Гамма-1С` выдаёт множитель `ε_ref/ε(E)` от ×5–7 (низкоэнерг. плато) до ×~10.76 (плоская экстраполяция выше 1408 кэВ). После #161 `update_spectrogram()` корректно применял её к `_raw_spec`, но не пересматривал yRange. **Phase 1 (недостаточная):** только расширять Y-верх при `data_hi > y_hi` — закрывало «улёт спектра за верх» в лин., но НЕ закрывало лог-режим: `_lock_spectrum_y` в `_render_spectrum` ставит `setLimits(yMin=log10(min_data))` от pre-нормировочных данных, viewRange-низ оставался на pre-нормировочном `log10(min_data)`, а после ×10 нормировки новый `log10(min_data)` был на +1 dec выше — кривая «висела» над низом окна. **Phase 2 (закрывающая):** `_expand_y_if_needed()` теперь ПОЛНОСТЬЮ переподгоняет Y обоих графиков после `update_spectrogram()` — лог: `setYRange(log10(min_pos)-0.1, log10(max_pos)+0.1, padding=0)`; лин: `setYRange(0.0, max*1.05, padding=0)`. `_lock_spectrum_y` (yMin по log10(min_data)) выполняется РАНЬШЕ и клампит запрос lo к log10(min_data) — pyqtgraph при клампе сохраняет ширину диапазона (валидировано standalone-тестом), сдвигая hi на ту же дельту вверх. Итог: низ viewRange садится ровно на log10(min_data), кривая прижата к X, гэп закрыт. X-зум пользователя не трогаем. Тест `test_slice_update_full_y_autofit_log_mode` кодирует поведение клампа: `yr_after[0] ≈ log10(min_data)`, `yr_after[1] ≈ log10(max_data) + 0.2` (запас +0.1 сверху и сдвиг +0.1 от клампа снизу). Полный pytest — 617 passed (616+1). Ждёт визуального подтверждения оператора.

`#163` — **рукоятка «Сглаживание»: дискретный выбор 0/SMA/WMA вместо непрерывного радиуса 0–15** (`awf/ui/zscale.py` — константы `SMOOTH_MODE_OFF, SMOOTH_MODE_SMA, SMOOTH_MODE_WMA = 0, 1, 2` и `SMOOTH_RADIUS = 2`; новая `weighted_moving_average()` (треугольные веса `radius+1-|смещение|`, центр окна весит больше краёв) рядом со старой `smooth_counts()` (box-фильтр, равные веса); диспетчер `smooth_by_mode(arr, mode, axis, radius=SMOOTH_RADIUS)` — 0 без изменений, 1 → `smooth_counts`, 2 → `weighted_moving_average`; `awf/ui/knobs.py` — ряд `_SPECS["smooth"]` сужен с `(0, 15, 0, "{v}")` до `(0, 2, 0, {0:"0",1:"SMA",2:"WMA"})`; `awf/ui/view3d.py` — `Waterfall3DView` мигрирован с `smooth_counts` на `smooth_by_mode` (2 места), `set_smoothing(mode)` теперь клампит `0..2` вместо произвольного радиуса; `awf/ui/panels.py` — `HeatmapPanel`/`SlicePanel` та же миграция; `tests/test_knobs_panel.py::test_adjustpanel_per_row_bypass` — диапазон ряда `smooth` теперь `0..2`). Оператор: «Рукоятку сглаживание переделай: 0-SMA-WMA». Было — непрерывный радиус box-фильтра 0–15 каналов (один алгоритм, переменная ширина окна); стало — фиксированная ширина окна (2·`SMOOTH_RADIUS`+1 = 5 каналов), рукоятка выбирает алгоритм: 0=выкл, SMA=равные веса (старое поведение), WMA=треугольные веса (новый алгоритм, сильнее сглаживает по центру окна). Полный pytest — 615 passed. Ждёт визуального подтверждения оператора.

`#162` — **La-138 определялся ошибочно — идентификация не проверяла присутствие всех значимых линий нуклида** (`awf/analysis/identify.py` — новые константы `_SIGNIFICANT_REL_FRAC = 0.20`, `_SIGNIFICANT_MIN_E_KEV = 50.0`; в `identify_peaks()` после существующего гейта `char_found`/`len(matched)<2` добавлен гейт значимых линий: для кандидата-нуклида собирается множество его линий с `energy >= _SIGNIFICANT_MIN_E_KEV` и `intensity >= _SIGNIFICANT_REL_FRAC * characteristic.intensity`; если это множество непусто, все его члены обязаны быть в matched-линиях, иначе кандидат отклоняется (`continue`); добавлен пункт 7 в докстринг модуля). Оператор: «Лантан определен ошибочно. У нуклида должны присутствовать ВСЕ значимые линии, увязанные по интенсивности». Корень: La-138 матчился только по линии 1435.8 кэВ, без требования присутствия его же линии 788.7 кэВ (интенсивность ~34.5% от характеристической) — прежний гейт проверял только «нашлась характеристическая линия + ≥2 совпадения», не требуя присутствия остальных значимых линий того же нуклида. Тесты: `tests/test_identify.py`, `tests/test_nuclide_families.py` (обновлены под новый гейт). Полный pytest — 615 passed. Ждёт визуального подтверждения оператора.

`#161` — **режим нормализации (по эффективности регистрации) ломал окно «Срезы/Сечения/Выборки»** (`awf/ui/panels.py` — `SlicePanel` получил состояние `self._view_mode` (`("integral",)` / `("slice", i)` / `("roi", t_lo, t_hi, ch_lo, ch_hi)`), обновляемое в `show_time_slice()`/`show_roi()`/`set_spectrogram()` (последний сбрасывает на integral); новый метод `update_spectrogram(sg)` — переприменяет текущий `_view_mode` к свежим данным без сброса на интегральный вид, затем вызывает новый `_refresh_after_update(sg)` (переприменяет наложение энергоокна и дозы без сброса зума); `awf/ui/main_window.py` — `_redistribute()` получил keyword-only `reset: bool = False`: `reset=True` (только в `_on_loaded`, т.е. загрузка нового файла) → `self._slices.set_spectrogram(sg)` (полный сброс); `reset=False` (дефолт, все toggle фона/нормализации) → `self._slices.update_spectrogram(sg)` (вид сохраняется)). Оператор: «В режиме нормализации ломается окно "срезы"». Корень: любой toggle (bg-select/subtract/eff-norm/eff-load) дёргал `_redistribute()` безусловно, а тот всегда звал `set_spectrogram()` на `SlicePanel`, который безусловно сбрасывал вид на интегральный спектр — пользователь терял выбранный срез/ROI при каждом переключении нормализации. Тесты: `tests/test_slice_analysis.py::test_update_spectrogram_keeps_time_slice_view`, `::test_update_spectrogram_keeps_roi_view`, `::test_update_spectrogram_keeps_integral_view`. Полный pytest — 615 passed. Ждёт визуального подтверждения оператора.

`#160` — **зона 1461–2614 кэВ всё ещё визуально пуста — оператор не видит найденных пиков (после `#159`)** (`awf/analysis/peaks.py` — новая `peak_presence_mask`; `awf/ui/view3d.py` — union в `_add_peak_ridge`; `tests/test_peak_time_mask.py` +4). Оператор: «нет, не нашел ты пики». **Корень (диагностика на реальном файле, `scripts/diag/ident160_*.py`):** `#159` починил идентификацию численно, но визуальный индикатор оператора — зелёные гребни 3D — в зоне отсутствовал: слабые-но-реальные постоянные линии (K-40 1440.6, Th-232 963.8/1573.0, escape 2095.5, 460.8/713.9/755.6) интегрально значимы (в таблице пиков по Карри на суммарном спектре), но одноканальная строгая `peak_time_mask` с пуассоновым гейтом `#152` по бинам времени пуста или почти пуста (один LOD-канал держит малую долю счётов пика: K-40 → 7/400 бинов, остальные → 0); релакс `#126` тоже пуст → гребней нет. Гипотеза «агрегация по времени ×2..×64» ОТВЕРГНУТА (спасает лишь половину линий: 460.8/501.9/713.9/755.6/2095.5 — ноль на всех факторах); принята «сумма окна пика»: sig(t)=Σ каналов jc±hw, baseline=клип(среднее плеч слева/справа за окном, gap=3, ширина 6)·ширина_окна, бин значим при net ≥ k·√(max(bg,1)); морфология opening(3) убивает одиночные шум-биты (контрольные пустые каналы 1800/2400/2900 кэВ реального файла → 0 бинов), closing(5) сшивает разрывы гребня. **Композиция в UI — UNION:** `mask = strict | peak_presence_mask(...)` (только при poisson=True); первая проводка «presence лишь при пустой strict» провалила E2E — K-40 strict=7 бинов срабатывал `.any()`, presence пропускался, гребень невидим. Union сохраняет строгую локализацию И добавляет слабые широкие линии; пустые колонки остаются пустыми (presence гейтится по-биново). Гребни 963.8/1573.0/2095.5 возвращаются в зону НАМЕРЕННО — после `#159` они идентифицированы (равновесные линии Th-232 с дрейфом шкалы, escape от 2614.5), а не «призраки» `#152`. E2E реальный файл (nt=400): 1440.6→243, 1573.0→55, 2095.5→29 (ранние бины, где Tl-208 силён — физично), 2607.2→272, 963.8→276, 713.9→41, 755.6→57, 460.8→3; исключение — 501.9 без гребня (незначим даже суммой окна, пограничный в таблице). `peak_time_mask` не тронута, релакс-fallback `#126` сохранён. Тесты: `test_160_weak_wide_line_strict_empty_presence_covers`, `test_160_empty_column_presence_stays_empty`, `test_160_transient_presence_localized`, `test_160_edge_channel_no_crash`. **Полный pytest — 603 passed** (599 + 4). Ждёт визуального подтверждения оператора.

`#159` — **пики Th-232 в зоне 1461–2614 кэВ и сам K-40 не найдены (рецидив зоны #158)** (`awf/io/nuclide_lib.py` — `USED_OVERRIDES` + `apply_used_overrides()`; `awf/analysis/identify.py` — `_energy_quality` линейный → квадратичный штраф; `awf/data/nuclides.json` — 6 used-флагов Th-232 false→true; `scripts/gen_nuclides_json.py` — регенерация применяет поправку; `scripts/fix159_th232_used.py` — идемпотентный one-shot; `tests/test_identify.py` +3). Оператор: «опять торий линии от 2614 до K40 1461 и сам калий 40 не найдены. исправляй». **Корень (диагностика на реальном файле, `scripts/diag/ident159_*.py`) — ДВА слоя:** (1) **данные** — у Th-232 в LSRM-источнике 25 линий, 19 с `used=false`, вкл. равновесные 968.971 (I=15.8), 727.33, 1588.2, 1620.5, 1630.627, 463.004; `identify_peaks` фильтрует `ln.used`, маркеры плоскостей — `major_lines(only_used=True)` → зона 911..2614 кэВ пуста для матчинга/маркеров при найденных пиках 963.8/1573.0; (2) **матчинг** — линейный штраф `1−|Δ|/окно` давил K-40: шкала файла −0.2..−1.4 % (пик 1440.6 vs 1460.8, Δ −20.2 при окне 29.7) → conf 0.32, ложный La-138 (1435.8, Δ +4.8) обгонял с 0.36. **Фикс:** (а) кураторская поправка `USED_OVERRIDES` (SSOT в `nuclide_lib.py`) — 6 равновесных линий Th-232 включены в отгружаемом JSON и при регенерации; 964.766 оставлена выключенной (мультиплет-партнёр 969, #130); (б) `_energy_quality`: `1−(|Δ|/w)²` — сцинтиллятор дрейфует по шкале ±1–2 % с температурой; квадратичный штраф терпим в середине окна, →0 у края. Реальный файл (UI-путь): Th-232 0.71→0.79 (+4 линии: 969, 1588, 727, 463 — зона закрыта), K-40 0.32→0.54 (ВЫШЕ La-138 0.42). Пик 2095.5 = single escape от 2614.5 (2103.5 кэВ) — не библиотечная линия, не идентифицируется by design; гребни 1573/2095 не рисуются (пустые пуассоновы маски — by design #126/#152). Тесты: `test_energy_quality_tolerates_calibration_drift`, `test_th232_high_energy_lines_enabled_in_shipped_library`, `test_real_file_peak_set_k40_and_th232` (TDD red→green). **Полный pytest — 599 passed** (596 + 3). Ждёт визуального подтверждения оператора.

`#158` — **идентификация перестала определять Th-232 и K-40 (регрессия #156)** (`awf/ui/main_window.py` — `_active_spectrogram()` разделён на `_analysis_spectrogram()` (вычет фона БЕЗ ε-нормализации) + `_active_spectrogram()` (то же + нормализация, только отображение), `_redistribute()` передаёт оба; `awf/ui/view3d.py` — `set_spectrogram(..., analysis_sg=None)` + `self._sg_analysis`, `_found_peaks()` и пуассонова маска `_z_counts_int` (#152) читают аналитический источник; `tests/test_efficiency.py` +1 тест). Оператор: «Алоримтм перестал определять Торий -232 и Калий-40. Это ошибка». **Корень (диагностика на реальном файле, `scripts/diag/ident158_probe.py`, НЕ угадан):** «известное взаимодействие» #156 оказалось реальной регрессией — поиск пиков и идентификация шли по ОТОБРАЖАЕМОЙ (нормализованной) спектрограмме. Два механизма: (1) умножение на ε_ref/ε(E) (до ×10.76 на ВЭ) ломает пуассонову статистику (Currie считает var=N, реальная дисперсия ∝ f²) → 10 ложных пиков >1700 кэВ (17 → 27); (2) `identify_peaks` сам ε-корректирует библиотечные интенсивности (#130) → двойная коррекция: Th-232 терял ВСЕ матчи (conf 0.71/6 линий → 0), K-40 падал 0.32 → 0.23 (ниже порога UI 0.30). **Фикс:** нормализация — строго отображение (3D/2D/срезы); анализ (пики #107/#113, маска #152, вход ID #127) — по `_analysis_spectrogram()`. Ре-рендеры view3d (`analysis_sg=None`, `sg is self._sg`) сохраняют прежний источник; новый sg без `analysis_sg` — сам sg. Проверка через реальный UI-путь (`scripts/diag/ident158_ui_check.py`, нормализация ВКЛ): 17 пиков, Th-232:0.71, Cs-137:0.46, La-138:0.36, U-235:0.36, K-40:0.32 — идентично «ВЫКЛ». Тест `test_158_peak_search_and_poisson_mask_ignore_normalization`. **Полный pytest — 596 passed** (595 + 1). Ждёт визуального подтверждения оператора.

`#157` — **оси/сетка 3D просвечивают сквозь рельеф** (`awf/ui/view3d.py` — `_add_grid_line()` получил `glOptions="translucent"`; `tests/test_view3d_rays.py` +1 тест). Оператор (скриншот): линии координатной сетки видны сквозь рельеф. Корень (как #95): дефолт `GLLinePlotItem` — `'additive'` = depth-тест ВЫКЛ. Фикс: `'translucent'` = depth-тест ВКЛ + блендинг (не `'opaque'`: цвета сетки несут альфу 0.55/0.9, opaque выключил бы GL_BLEND). Тест `test_157_grid_lines_depth_tested_not_additive` (GL_DEPTH_TEST + GL_BLEND у всех `_grid_items`). Ждёт визуального подтверждения оператора.

`#156` — **нормализация по эффективности регистрации ε(E) + меню загрузки кривой эффективности** (`awf/analysis/efficiency.py` — новый Qt-free модуль (`EfficiencyCurve`, `default_gamma1s`, `load_efficiency_curve`, `apply_efficiency`); `awf/ui/main_window.py` — тумблер «Нормализация по эффективности» + «Загрузить кривую эффективности…» + инфо-пункт в меню «Анализ», врезка в `_active_spectrogram()`; `awf/ui/i18n.py` +7 EN-строк; `tests/test_efficiency.py` — 13 новых тестов). Оператор: «Сделай нормализацию по эффективности регистрации. Добавь меню загрузка кривой эффективности. Если кривая не загружена, используй по-умолчанию кривую Гамма-1С из проекта СперктаВайб». Физика: ε фотопика NaI падает на порядок с энергией — нормализация умножает отсчёты канала на ε_ref/ε(E), ε_ref = максимум кривой (Гамма-1С: 1.0 на 121.78 кэВ, ~3.7 на 662, ~10.76 на 1408; за краями — плоская экстраполяция np.interp). Кривая по умолчанию — измеренная Гамма-1С из SpectraVibe (LSRM .efr, Gamma-1S-NB1 NaI(Tl) 63×63 мм, Point-15cm, Eu-152/Na-22/Ba-133/Cs-137/Am-241, 16 точек 59.5–1408 кэВ), захардкожена в `_GAMMA1S_POINTS` (SpectraVibe read-only §12). Интерполяция лог-лог (как `relative_efficiency` #130, но по измеренным точкам). Загрузчики: LSRM `.efr`/`.efa` (cp1251, числовой ключ регэкспа отфильтровывает метаданные), JSON (`[[E,eps],…]`/`{"points":…}`), двухколоночный текст (`,`/`;`/пробел, комментарии); дубли усредняются, <2 точек → ValueError → QMessageBox, кривая не меняется. Проводка: `_active_spectrogram()` — нормализация ПОСЛЕ вычета фона (вычитать в сырых отсчётах, один детектор); кривая — свойство детектора (переживает reload файла, session-only). Известное взаимодействие (вне объёма): поиск пиков/идентификация работают по нормализованной спектрограмме, а `identify.py` сам ε-корректирует библиотечные интенсивности (#130) — возможна двойная коррекция весов кандидатов при включённом тумблере; семантика согласована с #96 (вычет фона тоже меняет вход поиска). **Полный pytest — 594 passed** (581 + 13). Ждёт визуального подтверждения оператора.

`#155` — **тёмные названия изотопов не читаются на тёмном фоне** (`awf/ui/nuclide_panel.py` — палитра `COLORS` пропущена через luma-пол; `tests/test_nuclide_panel.py` +2 теста). Оператор (скриншот): имена Bi-212/Pb-210/Hg-203 в списке неразличимы на тёмной теме. Корень: палитра 18 цветов содержала тёмные члены (`#000075`, `#800000`, `#911eb4`, `#008080`, `#9a6324`…) — `setForeground` красит имя нуклида ими на тёмном фоне. Фикс: `_readable_on_dark()` итеративно поднимает HSL-светлоту с сохранением оттенка до Rec.709-luma ≥ 0.55 (`_MIN_LUMA`; HSL-светлота непригодна как критерий — синий при L=0.5 перцептивно тёмный). 9 тёмных подняты (navy→`#8989ff`, maroon→`#ff7676`, purple→`#ce78e9`…), 9 светлых не тронуты, все 18 уникальны. Одна палитра — все потребители: дерево изотопов, кандидаты идентификации, линии-маркеры на плоскостях 3D. **Полный pytest — 581 passed** (579 + 2). Ждёт визуального подтверждения оператора.

`#154` — **семейства изотопов свернуть в родителей (Th-232, Ra-226)** (`awf/io/nuclide_families.py` — новый Qt-free модуль (`FAMILY_PARENTS`, `FAMILY_DAUGHTERS`, `family_parent_of`, `collapse_families`); `awf/ui/nuclide_panel.py` — свёртка в `set_library()`; `tests/test_nuclide_families.py` — 9 новых тестов; `tests/test_nuclide_panel.py` — 3 счётных теста переведены на свёрнутую библиотеку). Оператор: «Убрать из списка изотопы из семейств. Оставить только родительские Th-232 и Ra-226, а линии дочерних включить в состав родителей». **Ключевой факт (проверен по nuclides.json):** родительские записи УЖЕ несут равновесные линии дочек — Th-232: 238.6 (Pb-212), 583.2/2614.5 (Tl-208, ветвление Bi-212→Tl-208 0.36 применено: 35.90/99.75=0.360), 911.2/338.3 (Ac-228), 727.3 (Bi-212); Ra-226: 351.9/295.2 (Pb-214), 609.3/1120.3/1764.5 (Bi-214), 186.2 (своя) → свёртка = чистый фильтр-скрытие дочек, линии не синтезируются/не дублируются. Дочка скрывается ТОЛЬКО при присутствии родителя в списке (синтетическая библиотека нуклид не теряет; идемпотентно). Семейства: Th-232 ← Ra-228/Ac-228/Th-228/Pb-212/Bi-212/Tl-208; Ra-226 ← Pb-214/Bi-214/Pb-210. Применение в `set_library()` покрывает дерево, идентификацию (#127) и `library()` → сегментацию (#131). Границы: U-238/Th-234/U-234/Th-230/U-235/Pa-231 НЕ свёрнуты (оператор назвал родителями только Th-232/Ra-226; вышестоящий урановый/актиниевый ряд в Ra-226 не сливаем — обратная физика; расширяемо); `add_nuclide()` (IAEA) не сворачивает — явное действие сохраняет добытую дочку; `identify_with_chains` (#133) без UI-потребителя, не затронут. Интеграционный тест: ториевый спектр 238.6/583.2/911.2/2614.5 (площади ∝ I×отн.эфф.) → кандидат Th-232, ни одной дочки. **Полный pytest — 579 passed** (570 + 9). Ждёт визуального подтверждения оператора.

`#153` — **нижняя граница поиска пиков 50 кэВ** (`awf/ui/view3d.py` — константа `_MIN_ENERGY_KEV=50.0` + двусторонний фильтр в `_found_peaks()`; `tests/test_peaks_ui.py` +1 тест; `tests/test_transient_peaks.py` — 1 ассерт обновлён). Оператор: «Поставить нижние границы поиска пиков 50 кэВ». Ниже ~50 кэВ — рентгеновский/шумовой край сцинтиллятора («пики» 23.6/37.1 кэВ на реальном файле — не гамма-линии). Реализация по образцу #119: фильтр `_found_peaks()` стал `_MIN_ENERGY_KEV ≤ E ≤ _MAX_ENERGY_KEV`; рельеф/сетка не обрезаются — граница только для поиска пиков (единый источник истины для гребней 3D, таблицы #111, идентификации #127). Побочно закрывает край-эффект #152 (пики 23.6/37.1 кэВ с drawn=0 удалены целиком). Тест `test_153_found_peaks_floor_at_min_energy` — monkeypatch детекции (как #141): фейк-пики 34/104 кэВ → остаётся 104 (вариант через реальную детекцию на 0.5 кэВ/канал отброшен — дефолт-модель FWHM на <50 кэВ слепа, ядро ~30 каналов против пика 8). Легаси-ассерт `test_view3d_short_record_no_transient_scan` зеркалит новый фильтр (интегральный скан находил артефакт ~5 кэВ высотой ~1e-13). E2E: 23.6/37.1 кэВ исчезли из списка, остальные 17 пиков без изменений. **Полный pytest — 570 passed** (569 + 1). Ждёт визуального подтверждения оператора.

`#152` — **обнаруженные пики отображаются гребнями в пустой зоне (рецидив темы #112/#126/#129, новый корень)** (`awf/analysis/peaks.py` — пуассонов гейт значимости в `peak_time_mask` (параметры `poisson`, `peak_hw`; константа `_PTM_POISSON_K=3`), у `_shoulder_baseline` параметризован `gap`; `awf/ui/view3d.py` — матрица СЫРЫХ отсчётов `_z_counts_int` (sum-LOD) + проводка `poisson=True, peak_hw=hw` в оба вызова маски; `tests/test_peak_time_mask.py` +2 теста). Симптом оператора (скриншот 3D): длинные прямые зелёные гребни пиков по полу в пустой зоне ~1500–2300 кэВ на поздних временах (колонки 1440.6/1573.0/2095.5/2607.2 кэВ). **Корень (systematic-debugging, Phase 1):** `peak_time_mask` по построению масштабно-инвариантна (MAD-порог от нижней половины, относительный floor #129, колоночный гейт Карри `net_max ≥ k·bg`) — на cps-матрице (max-LOD) она слепа к АБСОЛЮТНОЙ статистике: почти пустая колонка (единичные отсчёты) самоподобна колонке сильной линии, проходила все гейты, а бинарное closing сшивало редкие выбросы → маски на всю ось времени (322–400 бинов из 400) → гребни по полу. **Фикс (две части):** (1) view3d сташит `_z_counts_int` — сырые суммарные отсчёты sum-LOD (`sg.downsample(max_time, max_chan, method="sum")`, с тем же keep-тримом) и передаёт их в маску; (2) в `peak_time_mask` добавлен пуассонов гейт: `gnet_s ≥ 3·√(max(gbase_s,1))`, где базлайн гейта — ДАЛЬНИЕ плечи за пиком (`gap = peak_hw + 1`; `peak_hw = max(2, round(nc/64))`: 256→±4, 512→±8), а сигнал гейта НАМЕРЕННО одноканальный. Две отвергнутые ветки (уроки в код-комментарии и TASKS.md): близкие плечи `gap=1` сидят на склоне гауссианы пика (FWHM 8 кан > 1 LOD-бина) и съедают ~3/4 нетто-амплитуды — реальный транзиент #126 давал ~1σ при фактических ≫5σ (валил легаси-тест `test_transient_ridge_not_extended_to_empty`); оконное суммирование сигнала ch±hw ловит СОСЕДНИЕ реальные фоновые линии (K-40 1461 / Tl-208 2614 в ±hw от призрачного столбца) и возвращает гребни (E2E: 1440.6 → 276 бинов, 2607.2 → 230). Финал (сигнал одноканальный, плечи дальние) закрывает оба режима падений. E2E на реальном файле (869 срезов → LOD 400×422, hw=7): призраки 755.6/963.8/1573.0/2095.5 кэВ → 0 бинов; 1440.6 → 8 бинов [351..358] (реальная локальная флуктуация); 2607.2 → 24 бина [0..23] (где Tl-208 реально светил); сильные линии сохранили физические диапазоны (67.2 [0..366], 237.2 [0..226], 580.6 [0..221], 660.2 [221..273] и др.). Тесты: `test_152_sparse_trace_column_empty_with_poisson_gate` (Poisson(0.08) — маска пуста), `test_152_poisson_gate_keeps_strong_stable_line` (сильная стабильная линия — маска ≥ NT−4); легаси-тест транзиента жив. **Полный pytest — 569 passed** (567 + 2). Ждёт визуального подтверждения оператора.

`#151` — **«при вычете фона зона в плоскости сечения не вычитается» — Phase 1: вычет корректен; Phase 2: «узкий гребень слева» = квантование плоскостей через центры LOD-бинов — исправлено** (`awf/ui/view3d.py` `time_plane_range`; `tests/test_bg_ui.py` +1 тест; численная проверка на реальном файле оператора — 869 срезов, 0–17360 с, argv/gitignored). Оператор (2 скриншота: тулбар; 3D после вычета — фиолетовое поле шума + бирюзовое «плато» со ступенчатой границей, жёлтый блок ~100–400 кэВ, зелёный ~650 кэВ): «При вычете фона осталось зона в поскости сечения. Должна тоже вычитаться». **Phase 1:** воспроизведён полный пайплайн (фон = срезы 797–868 ≙ 15940–17340 с, позиции плоскостей со скриншота панели сечений → `tile_background_block(phase=lo)` → `subtract_background(bg_raw)`): внутри фонового участка `net.max() == 0.0` — самовычет ТОЧНЫЙ (инвариант #147/#149 подтверждён на реальных данных). Вне участка нетто = реальное превышение: скорость по сегментам 610→490→320→190 cps (ступени = смены конфигурации источника); после вычета хвостового фона (~168 cps) ранние сегменты остаются на 440/323/140 cps нетто — это и есть «плато» (границы сегментов по времени × максимальные энергии линий по энергии дают ступенчатый контур). Зелёный блок ~650 кэВ = зона 662 кэВ (Cs-137; энергоокно оператора 647–677 кэВ). Отрицательные ячейки (сегменты тише фона) гасятся к 0 на отображении (знаковая модель #134). **Вердикт Phase 1: остающиеся зоны — статистически значимый нетто-сигнал, вычитаться в 0 они не должны.** **Phase 2 (уточнение оператора: «вот на скрине узкий гребень слева» — колючая гряда вдоль оси энергий у переднего края):** корень — `time_plane_range()` возвращал ЦЕНТРЫ LOD-бинов (`downsample(max_time=400)`: 869 срезов → 400 бинов ~2.2 среза/бин); центр последнего бина 17340 = t[867] → диалог (argmin по сырой сетке) давал hi=868 → сырой срез 868 (t=17360) выпадал из выборки фона → его нетто-шум (до +7 отсчётов) max-прорежка поднимала в последний LOD-бин → гребень ровно в плоскости сечения. Фикс: `time_plane_range` возвращает КРАЯ крайних бинов в сырых срезах (разбиение как в `sg.downsample`: `np.unique(np.linspace(0, ns, nt+1))`), невидимый слот → сырой край оси. Тест `test_view3d_time_plane_range_lod_bins_cover_raw_edges` (ns=8/max_time=4, плоскости на краях → сырые t[0]/t[-1]; красный до фикса); E2E на файле оператора: выборка 797–869, последний LOD-бин max 0.0 — гребень исчезает; старый тест #148 жив (LOD 1:1 — края = центры). **Полный pytest — 567 passed** (566 + 1). Ждёт визуального подтверждения оператора.

`#150` — **«Подложка» неактивна (выключена) по умолчанию** (`awf/ui/main_window.py` — `_floor_check.setChecked(False)` при создании и в сбросе #51; `awf/ui/view3d.py` — `_floor_visible = False`; `tests/test_group4_remarks.py` — 3 правки, `tests/test_bg_ui.py` — 1 правка). Оператор: «подложку сделай не активной по умолчанию». Дефолт тумблера «Подложка» (#76) и зеркального атрибута view3d — выключено; «Сброс» (#51) тоже приводит к выключенному. Тесты: `test_floor_visible_by_default` → `test_floor_hidden_by_default` (дефолт False, ячейки дна alpha 0, явное включение возвращает непрозрачность); в `test_floor_hidden_zeroes_base_cells` и `test_floor_toggle_hides_bg_sheet_floor` добавлено явное `set_floor_visible(True)`; `test_floor_toolbar_checkbox_drives_view` инвертирован (дефолт выкл → вкл → выкл). **Полный pytest — 566 passed** (число не изменилось: тесты переименованы/инвертированы). Ждёт визуального подтверждения оператора.

`#149` — **фон под-диапазона: транслировать сырые срезы на всю шкалу времени** (`awf/model/background.py` — новый `tile_background_block(bg_counts, bg_lt, n_slices, phase=0)`; `awf/ui/main_window.py` — `_compute_background`: range- и file-ветки сташат тайлированный блок, импорт; `tests/test_background.py` +4 теста; `tests/test_bg_ui.py` +1 тест). Оператор (скриншот: аномальная жёлтая простыня фона на 3D после выбора фона плоскостями #148; плоскости Времени 15950/17340 с — участок в конце файла 17360 с): «выбрал фон из сечения. что-то не так с оборажением фона» + «и надо его аппроксимировать не усреднением, а копированием сырых данных» + «имеется в виду транслировать на временную шкалу». **Корень (Phase 1):** для под-диапазона `_bg_raw` имел форму (hi−lo, nc) ≠ `sg.counts.shape` → ВСЕ потребители сырого блока (простыня 3D `_bg_sheet_field` #144, оверлей срезов #139, вычет #147) гейтятся на совпадение формы и падали в гладкий fallback усреднённого `bg_cps` — отсюда «не такая» простыня. **Фикс:** сырой блок участка циклически копируется на всю шкалу: срез t получает клон bg-среза `(t − phase) mod m`, phase = lo (позиция участка). Ключевой инвариант: срезы ВНУТРИ [lo, hi) получают клонами самих себя → самовычет фонового участка — точный ноль в каждой ячейке (#147); полный диапазон/phase=0 → тождество (старые сценарии #139–#147 не изменились). File-ветка (#141) тоже тайлится (phase=0) — файл-фон другой длины по времени растягивается на шкалу цели. Фон аппроксимируется КОПИРОВАНИЕМ сырых данных, не усреднением (директива #138). Тесты: `test_tile_full_range_phase0_is_identity`, `test_tile_subrange_clones_itself_inside_and_cycles_outside`, `test_tile_bad_shape_raises`, `test_tile_subtract_bg_region_is_exact_zero`, `test_compute_background_range_tiles_raw_to_full_scale`. **Полный pytest — 566 passed** (561 + 5). Ждёт визуального подтверждения оператора.

`#148` — **выбирать фоновый участок секущими плоскостями** (`awf/ui/view3d.py` — новый `time_plane_range()`; `awf/ui/background_dialog.py` — параметр `plane_range`, кнопка «Из сечений», хелпер `_apply_planes`, импорт numpy; `awf/ui/main_window.py` — `_on_bg_select` передаёт `plane_range=self._view3d.time_plane_range()`; `tests/test_bg_ui.py` +3 теста). Оператор (скриншот диалога «Выбор фона»): «Сделай, чтобы фоновый участок можно было выбирать секущими плоскостями». Реализация: диапазон для источника «из текущего измерения» берётся из секущих плоскостей Времени 3D. `time_plane_range()` → (t0, t1) в секундах между видимыми плоскостями Времени; невидимый слот — край оси (семантика #84: слот 0 — нижняя граница, слот 1 — верхняя), обе невидимы → None; результат отсортирован (min, max). Диалог: при переданном `plane_range` поля срезов сразу предзаполняются ближайшими к позициям плоскостей срезами (`argmin |time_offsets − t|`; hi включает верхний срез: +1, полуоткрытый [lo, hi)) и активируется радио «Из текущего измерения»; кнопка «Из сечений» возвращает диапазон плоскостей после ручной правки полей (неактивна без видимых плоскостей Времени). Сценарий: плоскости Времени на фоновом участке → «Анализ → Выбор фона…» → диапазон уже стоит → OK. Тесты: `test_bg_dialog_plane_range_prefill`, `test_bg_dialog_no_planes_disabled`, `test_view3d_time_plane_range`. **Полный pytest — 561 passed** (558 + 3). Ждёт визуального подтверждения оператора.

`#147` — **вычет абсолютно совпадающих простыней не нулевой — поячеечный вычет сырого блока** (`awf/model/background.py` — `subtract_background(sg, bg_cps, bg_raw=None)`, сырая ветка; `awf/ui/main_window.py` — `_active_spectrogram` передаёт `self._bg_raw`; `tests/test_background.py` +2, `tests/test_bg_ui.py` +1, 1 обновлён). Оператор: «Но вычет абсолютно совпадающих простыней не нулевой, ищи и исправляй» (скриншоты: простыни совпали, но после вычета остаток ~0.001–0.01 cps в срезах на лог-шкале). **Корень (Phase 1):** вычет шёл через УСРЕДНЁННЫЙ `bg_cps` (`net = counts − bg_cps·lt`, #134/#138) — срез образца несёт пуассонов шум √N, усреднённый фон гладкий → поячеечный остаток = знаковый шум, ноль только в интеграле по времени. А отображение простыней (#139/#141) использует СЫРОЙ блок `_bg_raw` поканально/посрезно — потому простыни совпадали точно, а вычет — нет. **Фикс:** `subtract_background` принял третий аргумент `bg_raw=(counts_блок, lt_блок)`; при форме блока == `sg.counts.shape` (фон = тот же файл с совпадающей энергосеткой #141 или весь диапазон #139) вычет поячеечный `net = counts − bg_counts·(lt/bg_lt)` — при самовычете `lt == bg_lt` → остаток ТОЧНЫЙ ноль в каждой ячейке (согласуется с доктриной #138 «вычитай сырые данные поканально, ничего не усредняй»). Иная форма (под-диапазон, чужая сетка) — прежний путь через средний `bg_cps` (инвариант #134 сохранён). Проводка: `_active_spectrogram` → `subtract_background(self._sg, self._bg_cps, self._bg_raw)`. Тест `test_mainwindow_subtract_reduces_waterfall` обновлён осознанно: ассерт «знаковый остаток с отрицательными» при фон=весь файл заменён на «точный ноль поячеечно» (#147 усиливает #134; знаковость по-прежнему покрыта `test_subtract_keeps_signed_net`). Новые тесты: `test_self_subtraction_raw_block_exact_zero_per_cell` (raw-самовычет → все ячейки 0, усреднённый путь ≠ 0), `test_subtract_raw_block_shape_mismatch_falls_back` (блок иной формы → прежняя формула), `test_active_spectrogram_passes_raw_block` (проводка UI: `_bg_raw` установлен → нули; сброшен → шум). **Полный pytest — 558 passed** (555 + 3). Ждёт визуального подтверждения оператора.

`#146` — **«Подложка» отключает подложку обеих простыней (образец + фон)** (`awf/ui/view3d.py` — блок гашения дна в `_add_bg_sheet`, NaN-вырезка для стиля «каркас» в `_rebuild_surface` и `_add_bg_sheet`, `set_floor_visible` дополнительно вызывает `_rebuild_bg_sheet()`; `tests/test_bg_ui.py` +1 тест `test_floor_toggle_hides_bg_sheet_floor`). Запрос оператора: «Сделай, чтобы кнопка Подложка отключала подложку и фона и образца». **Корень:** тумблер «Подложка» (#76) гасил дно только у рельефа-образца (`_rebuild_surface`: alpha 0 при `zsub <= _FLOOR_FRAC·height_scale`); простыня фона (`_add_bg_sheet`) блока гашения не имела вовсе, а `set_floor_visible` её не перестраивал. **Фикс:** в `_add_bg_sheet` ячейки дна `zn <= _FLOOR_FRAC` (эквивалент критерия рельефа: z = zn·height_scale) получают alpha 0; `set_floor_visible` перестраивает и простыню фона. Особый случай — стиль «каркас» (#145): рёбра GLMeshItem рисуются ЕДИНЫМ `edgeColor` без per-vertex alpha → гашение прозрачностью невозможно, ячейки дна вырезаются `z=NaN` (OpenGL отбрасывает примитивы с NaN-вершинами; NaN в `GLSurfacePlotItem` construct/`setData` безопасен — проверено зондом на pyqtgraph 0.14.0 offscreen). Та же NaN-вырезка добавлена рельефу-образцу в `_rebuild_surface` (обязателен `zsub.copy()` перед записью NaN — `ascontiguousarray` может вернуть view на `_z_surface`). Тест: фон ~0 (вся простыня — «дно») → подложка вкл: у простыни фона alpha 0.55; выкл: alpha 0 везде; стиль «каркас»: все z = NaN; возврат подложки — NaN исчезают. **Полный pytest — 555 passed** (554 + 1). Ждёт визуального подтверждения оператора.

`#145` — **раздельный стиль простыни для образца и фона: палитра / однотонный / каркас** (`awf/ui/view3d.py` — константы `_SHEET_RGB_SMP=(0.45,0.65,0.95)`/`_SHEET_RGB_BG=(1.0,0.55,0.15)`, флаги `_surface_style`/`_bg_style`, сеттеры `set_surface_style`/`set_bg_sheet_style`, хелперы `_sheet_style_kwargs`/`_apply_solid_style`, применение стиля в `_rebuild_surface` и `_add_bg_sheet`; `awf/ui/main_window.py` — два CycleButton `_smp_style_combo`/`_bg_style_combo` («Стиль обр.:» / «Стиль фона:») на тулбаре «Вид» после «Фон среза», слоты `_on_surface_style_changed`/`_on_bg_style_changed`, сброс обоих в «Палитра» в `_on_reset_display`; `awf/ui/i18n.py` +2 строки; `tests/test_bg_ui.py` +1 тест). Запрос оператора: «сделай чтобы для фона и образца можно было выбрать разный стиль простыни». Три стиля, выбираются НЕЗАВИСИМО для каждой простыни: **«Палитра»** (дефолт) — прежний цветной рельеф текущей палитрой; **«Однотонный»** — полупрозрачная одноцветная простыня (образец голубой alpha 0.45, фон оранжевый alpha 0.55 — конвенция цветов окна срезов), перекрас `_apply_solid_style` сохраняет полностью прозрачные ячейки (обрезка по счёту IV-R3, скрытое дно #76); **«Каркас»** — wireframe-сетка без граней через kwargs `GLSurfacePlotItem(..., drawEdges=True, drawFaces=False, edgeColor=(*rgb, 0.85))` (GLSurfacePlotItem наследует GLMeshItem — проверено экспериментально на pyqtgraph 0.14.0, offscreen). Для образца при нестандартном стиле принудительно `setGLOptions("translucent")` (условие расширено: `counts_active or not _floor_visible or _surface_style != "palette"`). Тест `test_sheet_styles_independent`: смена стиля фона (каркас, `opts["drawEdges"]`/`["drawFaces"]`) не трогает стиль образца и наоборот (однотонный); «Сброс» возвращает оба в «palette». **Полный pytest — 554 passed** (553 + 1). Ждёт визуального подтверждения оператора.

`#144` — **простыня фона отображается некорректно (обрезка каналов, «зубцы», гладкая) — сделать как рельеф-образец** (`awf/ui/view3d.py` — переписаны `_rebuild_bg_sheet` и `_add_bg_sheet` (1D→2D), новый `_bg_sheet_field`; `tests/test_bg_ui.py` — тест #140 переписан под 2D-инвариант). Скриншоты оператора: (1) в 3D-окне простыня — беловато-серый лохматый лист, оборванный по каналам заметно раньше рельефа образца (~2200 кэВ vs ~2800 кэВ); (2) для сравнения — сам рельеф образца в палитре iZotope до 2800+ кэВ. **Корень (Phase 1):** после #140 простыня всё ещё была 1D-«лентой»: `_rebuild_bg_sheet` брал `background_window_like(bg_counts, bg_lt, lt_ref)` → одну строку `col[:nc]` от LOD-max → передавал в `_add_bg_sheet(bg_h)`, который стелил её плоско по времени (`np.vstack([bg_h, bg_h])`) серо-белым цветом `(0.82,0.86,0.95,0.16)` alpha≈0.16. Итог: (а) 1D-профиль не может «выглядеть как рельеф» — в скринах именно и виден «жест» этой ленты; (б) серо-белый = не палитра образца; (в) alpha 0.16 = тусклый лист. Обрезка по каналам не в самой простыне (`smooth`+`:nc` симметрично рельефу), а в **визуальном впечатлении** от белого 1D-полога. **Фикс (2 файла, 3 узла):** (1) `_rebuild_bg_sheet` теперь строит 2D-матрицу фона тем же пайплайном, что рельеф — `_bg_sheet_field(bg)` → `sg.downsample(method="max", data=bg_field)` → clip до `_MAX_ENERGY_KEV=3000` → `smooth_counts(·,self._smooth,axis=1)[:, :nc]`. (2) Новый `_bg_sheet_field(bg_cps)`: при `_bg_sheet_raw.shape == sg.counts.shape` (файл-фон #141 с совпадающей энергосеткой, либо range-фон #139) возвращает сырую матрицу счётов в текущих единицах — `bg_counts/lt[:,None]` при `unit=="cps"`, `bg_counts` при `unit=="counts"`; иначе fallback — константное поле поканального `bg_cps` (или `bg_cps*lt_ref` для counts). (3) Новый `_add_bg_sheet(z_bg)`: принимает 2D, применяет `apply_z_scale` к фону и образцу с ТЕМИ ЖЕ `z_mode/gain/gamma/clip`, нормирует высоты фона на `zmax = z_smp.max()` (одна высотная система — при фон=образец простыня ложится ровно на рельеф), красит через `get_colormap(self._cmap_name).map(zn,mode="float")` — та же палитра, — `alpha=0.55` (полупрозрачно, но видно). Тест `test_view3d_bg_sheet_built_like_relief` переписан под новый инвариант: `_add_bg_sheet` мокается `lambda z: cap["z"]=…`, проверяется, что `cap["z"].shape == (n_lod_t, v._nc)` и `np.allclose(cap["z"], smooth_counts(sg.downsample(counts/lt[:,None])[0], v._smooth, axis=1)[:, :nc], atol=1e-6)`. **Полный pytest — 553 passed** (без изменения числа: 1 тест переписан). Ждёт визуального подтверждения оператора.

`#143` — **тумблер «Простыня образца» на тулбаре «Вид»** (`awf/ui/view3d.py` — флаг `_surface_on` + метод `set_surface_visible(bool)` + gate в `_rebuild_surface`; `awf/ui/main_window.py` — чекбокс `_surface_check` после «Подложка», обработчик `_on_surface_toggled`, сброс в `_on_reset_display`; `awf/ui/i18n.py` +1 строка; `tests/test_bg_ui.py` +1 тест). Запрос оператора после #142 (скриншот тулбара «Вид»): «добавь отключение простыни образца». Реализация независимая от режима наложения фона (в отличие от #142): чекбокс всегда enabled, дефолт checked. Гейт стоит в самом `_rebuild_surface` — `_surface_on=False` снимает текущий `GLSurfacePlotItem` и выходит; повторная сборка (смена Z/gain/gamma/clip/smooth/palette/light/unit/time-unit/reset — все идут через `_rebuild_surface`) корректно уважает флаг. Сечения (`_maybe_reclip`) вызывают `_rebuild_surface` — тоже погашены. i18n: "Простыня образца"→"Sample sheet". Тест `test_surface_visibility_toggle`: дефолт checked+enabled, после `set_spectrogram` рельеф построен, снятие → `_surface` == None и `_surface_on` == False, возврат → рельеф пересобран. **Полный pytest — 553 passed** (552 + 1). Ждёт визуального подтверждения оператора.

`#142` — **тумблеры видимости элементов наложения фона на тулбаре «Вид»** (`awf/ui/main_window.py` — `_build_toolbar` +2 чекбокса, `_on_bg_overlay_toggled` + новый `_apply_bg_overlay_visibility`, `_reset_background`, `_on_reset_display`; `awf/ui/i18n.py` +2 строки; `tests/test_bg_ui.py` +1 тест). Запрос оператора: «Сделай кнопку "показть/отключить" простыню образца и спетра среза в режиме наложения образца и фона»; уточнено через AskUserQuestion: **два независимых тумблера** (не один общий, не скрытие образца), размещение — **тулбар «Вид»**. Реализация: чекбоксы «Простыня фона» (3D-простыня) и «Фон среза» (кривая фона в окне среза), по умолчанию checked, но disabled; активируются только при включённом «Наложение фона» (меню Анализ). Видимость элемента = `_bg_overlay AND чекбокс` — считается в `_apply_bg_overlay_visibility()` (единая точка: `_slices.set_background_overlay(...)` + `_view3d.set_background_sheet_visible(...)`); выключение режима наложения гасит оба элемента и disable-ит тумблеры, состояние чекбоксов сохраняется. `_reset_background` (новый файл/сброс фона) возвращает тумблеры в дефолт (checked, disabled, blockSignals); кнопка «Сброс» (#51) ставит оба в checked. i18n: "Простыня фона"→"Background sheet", "Фон среза"→"Slice background". Тест `test_bg_overlay_visibility_toggles`: до режима — disabled; включение режима — enabled + оба флага панелей взведены; снятие каждого чекбокса режет свой флаг независимо; выключение режима — всё скрыто и disabled даже при checked. **Полный pytest — 552 passed** (551 + 1). Ждёт визуального подтверждения оператора.

`#214` — **утилита перекалибровки ASWF: `scripts/recalibrate_aswf.py`** (`scripts/recalibrate_aswf.py`). Запрос оператора: файл `spectrogram.aswf` с неверной калибровкой (UI показывал Sb-122, реальное содержимое — Th-232). Утилита `recalibrate(src, dst, new_cal)`: читает бинарный .aswf, парсит JSON-заголовок, заменяет `hdr["calibration"]`, переписывает dst с дополнением нулями до оригинального `header_len`. Вспомогательная `fit_calibration(current_cal, peak_pairs, deg=4)`: инвертирует текущий полином через `scipy.optimize.brentq`, находит каналы 11 известных гамма-линий цепочки Th-232 (Pb-212 238.6, Tl-208 277.4/510.8/583.2/860.6/2614.5 кэВ, Ac-228 338.3/911.2/969.0/1588.2 кэВ, Bi-212 727.3 кэВ, K-40 1460.8 кэВ), затем `numpy.polyfit(channels, true_energies, deg=4)`. Новая калибровка: `[-11.342, 0.38277, 3.325e-05, -2.287e-09, 4.379e-13]`, RMSE 2.35 кэВ, max|Δ| 5.05 кэВ (Ac-228 911.2). Выход: `spectrogram_th232recal.aswf` (8 664 324 байт, локальный путь оператора). Тесты: нет (standalone-скрипт). **Полный pytest — 667 passed.**

`#213` — **2D-карта: Y-ось отображает реальное время с/мин/ч** (`awf/ui/panels.py` — новый класс `_TimeAxisItem(pg.AxisItem)` + методы `HeatmapPanel.set_time_unit(unit)` и обновление в `set_spectrogram`; `awf/ui/main_window.py` — `_on_time_unit_changed` теперь вызывает и `_heatmap.set_time_unit(unit)`; `awf/ui/i18n.py` — добавлены переводы `"Время, мин"→"Time, min"` и `"Время, ч"→"Time, h"`). До задачи: Y-ось 2D-карты (HeatmapPanel) показывала индексы строк 0..N вместо реального времени. `_TimeAxisItem`: наследует `pg.AxisItem("left")`, хранит `_offsets` (массив `time_offsets_s`), `_div` (1.0/60.0/3600.0 по единицам), переопределяет `tickStrings` — маппинг float display-индекса → реальное время через `_offsets[clamp(round(v * t_scale))] / _div`. `set_time_unit` обновляет метку оси и вызывает `_time_axis.set_data(sg.time_offsets_s, t_scale, unit)`. Тесты: нет отдельных (smoke через полный pytest). **Полный pytest — 667 passed.**

`#217` — **`awf/io/becqmoni_loader.py`: BecqMoni Live XML одиночного спектра** (`awf/io/becqmoni_loader.py`, `tests/test_becqmoni_loader.py`, `scripts/ollama/_spec_217_becqmoni_loader.md`, `scripts/ollama/_spec_217_test_becqmoni_loader.md`). Гибкий XML-парсер BecqMoni Live. `load_becqmoni(path) -> Spectrogram` — 1×N_ch. Namespace-strip: `_localname(el) = el.tag.rsplit("}", 1)[-1]`; поиск регистронезависимый (`_find_local`/`_findall_local`). Корень: `<ResultDataFile>` (или любой), ищет `<EnergySpectrum>` вниз от root. Тайминги из `<EnergySpectrum>`: `<MeasurementTime>` → real=live (fallback); `<LiveTime>` + `<RealTime>` — оба при наличии; отсутствие → NaN. Старт: `_first_text(root, ["StartTime", "SampleDateTime", "AcquisitionStartTime"])` — ищет в целом дереве (не только scope), парсит `_parse_iso` через `datetime.fromisoformat` + стрип `Z`; нераспарсимо → None. Калибровка из `<EnergyCalibration>/<Coefficients>/<Coefficient>` — список float; нет → fallback `[0.0, 1.0]`. Источник отсчётов (приоритет): (1) `<DataPoint>` внутри `<Spectrum>` — последовательные int; (2) `<Channel>` с атрибутом `n`/`index` — сортируется по индексу, паддинг нулями до max+1; (3) text-content `<Spectrum>` whitespace-split. NumberOfChannels: если задан и > длины → паддинг нулями справа; если < → предупреждение + обрезание. Отрицательные → 0 + `warnings.warn`. dtype `uint16` при `max<=65535`, иначе `int32`. Ошибки: XML ParseError → `ValueError("not valid XML: …")`; нет `<EnergySpectrum>` → `ValueError("no EnergySpectrum found")`; нет отсчётов → `ValueError("no spectral data found")`. Генерация через Ollama qwen3-coder:30b (§16 IRON). Ollama-огрехи (исправлены вручную inline): (1) `from xml.etree.ElementTree import ElementTree, ParseError` → `import xml.etree.ElementTree as ET; from xml.etree.ElementTree import ParseError`; (2) type-hints `ElementTree.Element | None` → `ET.Element | None` (replace_all); (3) `_first_text(scope, […])` → `_first_text(root, […])` (StartTime в родителе ResultData, вне EnergySpectrum-scope). Тесты: 10 юнит-тестов через helper `_write_xml(tmp_path, body)` — full (DataPoints+StartTime+EnergyCalibration), channel-elements (n attr, reordered), spectrum-text (whitespace split), no-calibration-fallback, live+real-both, missing-timings-nan, start-time-invalid-none, number-of-channels-pads, not-xml-raises ValueError, int32-when-gt-uint16. Часть Фазы 6 импортов PC (#PC-1). **Полный pytest — 749 passed** (739 baseline + 10 becqmoni_loader).

`#216` — **`awf/io/csv_loader.py`: гибкий CSV одиночного спектра** (`awf/io/csv_loader.py`, `tests/test_csv_loader.py`, `scripts/ollama/_spec_216_csv_loader.md`, `scripts/ollama/_spec_216_test_csv_loader.md`). Универсальный 2-колоночный CSV. `load_csv(path) -> Spectrogram` — 1×N_ch. Автодетект разделителя `_detect_delimiter(lines)` из кандидатов `[",", ";", "\t"]`: считает число строк с `>=2` полями в первых 20; выбор max, при равенстве приоритет `,`>`;`>`\t`, fallback `,`. Комментарии `#…` и пустые строки — skip. Header-детект: если первая ячейка не парсится как float → вся строка skip. Тип первой колонки — `_is_channel_index(xs)`: строго `xs[i] == i` (int) для всех `i` → channel-index, калибровка identity `[0.0, 1.0]`; иначе энергии в кэВ — линейная калибровка `a0=xs[0]`, `a1=(xs[-1]-xs[0])/(N-1)` (fallback `1.0` при `N==1`). Отсчёты — `float(...)` → `int(round(...))`; отрицательные клипаются к 0 (одноразовый `warnings.warn`); dtype `uint16` при `max<=65535`, иначе `int32`. Оси: `time_offsets_s=[0.0]`, `real_time_s=live_time_s=[NaN]`, `t0_iso=None`, `source_path=str(path)`. Ошибки: пустой файл (после фильтра) → `ValueError`; parse-ошибки/недобор колонок — `warnings.warn` (с флагами `_warned_short`/`_warned_bad`/`_warned_neg`/`_warned_round`, чтобы не флудить). Хелперы: `_detect_delimiter`, `_try_float(s)`, `_is_channel_index(xs)`. Генерация через Ollama qwen3-coder:30b (§16 IRON) — без правок с первого прогона (v1 чистый и loader, и тесты). Тесты: 10 юнит-тестов через helper `_write_csv(tmp_path, lines)` — channel-counts (comma), energy-counts linear fit (`10.0,5→[10.0, 5.0]`), semicolon separator, tab separator, header skipped, comments+blanks skipped, empty raises `ValueError`, int32 при `70000`, negative clipped to 0, single-channel energy fallback (`100.5,42 → [100.5, 1.0]`). Часть Фазы 6 импортов PC (#PC-1). **Полный pytest — 730 passed** (719 baseline + 10 csv_loader + 1 pre-existing; #UI-2 `test_top_menus_skeleton_present` починился где-то параллельно вне цепочки #PC-1 — сейчас проходит).

`#215` — **`awf/io/spe_loader.py`: ORTEC/IAEA ASCII `.spe` loader** (`awf/io/spe_loader.py`, `tests/test_spe_loader.py`, `scripts/ollama/_spec_215_spe_loader.md`, `scripts/ollama/_spec_215_test_spe_loader.md`). Односпектральный ASCII-формат (Ortec/GammaVision, IAEA). `load_spe(path) -> Spectrogram` — 1×N_ch: `_parse_sections` разбивает файл на секции по regex `^\$[A-Z0-9_]+:$` (дубли — первое вхождение). Обязательно `$DATA:` — первая строка тела `start end`, дальше int-отсчёты по одному на строку; при `start>0` каналы `0..start-1` дополняются нулями (`n_channels = end + 1`); длина не совпала → `warnings.warn` + padding до `expected`. dtype: `uint16` если `max<=65535`, иначе `int32`. `$MEAS_TIM:` — `<live> <real>` (порядок Ortec), нет секции → NaN. Калибровка по приоритету: `$MCA_CAL:` (первая строка = N, вторая = N/N+1 токенов; N float + опц. единица `keV` игнорируется), затем `$ENER_FIT:` (два float `a0 a1` в одной строке), fallback `[0.0, 1.0]`. `$DATE_MEA:` — Ortec `%m/%d/%Y %H:%M:%S` → ISO-8601 без tz, иначе `datetime.fromisoformat` (с хвостовым `Z`); нераспарсимо → `t0_iso=None`. Ошибки: нет `$DATA:` → `ValueError`; всё прочее парсимо, но битое — `warnings.warn` и fallback. Генерация через Ollama qwen3-coder:30b (§16 IRON) — без правок с первого прогона (v1 чистый и loader, и тесты). Тесты: 12 юнит-тестов через helper `_write_spe(tmp_path, sections)` — minimal, ener_fit_only, mca_cal_priority, mca_cal_without_units, meas_tim_live_real, date_ortec (`09/24/2018 14:05:37` → `2018-09-24T14:05:37`), date_iso, date_invalid_none, start_nonzero_pads_left (`start=3` → 3 нуля впереди), missing_data_raises, no_calibration_fallback, int32_when_gt_uint16 (значение 70000 → dtype `int32`). Часть Фазы 6 импортов PC (#PC-1). **Полный pytest — 719 passed, 1 failed** (702 baseline + 12 spe_loader + 5 pre-existing вне цепочки; 1 fail = `tests/test_group4_remarks.py::test_top_menus_skeleton_present` — expected menu set не содержит нового пункта `'calibration'`, вне зоны #PC-1, зарегистрирован как #UI-2).

`#213` — **`awf/model/series.py`: `RecordingSeries` — план серии live-USB записей** (`awf/model/series.py`, `tests/test_series.py`, `scripts/ollama/_spec_213_series.md`, `scripts/ollama/_spec_213_test_series.md`). Чистая Python-модель (dataclass, без Qt/I/O/numpy). Конструктор: `durations_sec: list[float]`, `out_dir: Path|str`, `file_prefix="series"`, `gap_sec=0.0`, `stop_on_error=True`. Валидация в `__post_init__` (непустой список, `d>0`, `gap>=0`, prefix). Фазы `SeriesPhase(Enum)`: IDLE → RECORDING → (PAUSE →) RECORDING … → DONE. Ключевые методы: `start(now)`, `tick(now)`/`advance(now)` (авто-переход по достижению длительности; `_phase_started_at += phase_dur` — устраняет дрейф при поздних тиках; `while` крутит цикл при большом скачке `now`, корректно пропускает несколько фаз одним вызовом), `mark_error(now)` (stop_on_error→DONE+errored, иначе advance), `reset()`. Свойства: `n_tracks`, `phase`, `current_index`, `started_at`, `is_running`/`is_done`/`errored`, `current_file` (Path только в RECORDING: `out_dir/f"{prefix}_{idx:03d}"`), `current_duration_sec`, `remaining_sec(now)`, `progress() → (done, total)`. Особенности: после последней дорожки — сразу DONE, без хвостовой паузы даже при `gap_sec>0`; в PAUSE `current_file`=None (writer в это время не пишет). Генерация через Ollama qwen3-coder:30b (§16 IRON) — без правок с первого прогона (v1 чистый). Тесты: 14 юнит-тестов (init defaults + 4 валидационных, start, start-twice, tick без gap, tick с gap alternates RECORDING/PAUSE, gap-skipped-after-last, large-jump-skips-multiple, mark_error stop/no-stop, reset+restart). Часть замечания #PC-1 (Фаза 6). **Полный pytest — 702 passed** (679 baseline + 14 series + 9 pre-existing, добавленных вне цепочки #PC-1).

`#211` — **`awf/io/aswf_writer.py`: сегментный писатель ASWF v3** (`awf/io/aswf_writer.py`). `AswfSegmentedWriter` — запись live-USB водопада в каталог сегментов (`seg_NNNNN.aswf`). Конструктор принимает `out_dir`, `started_at`, `interval_sec`, `baseline` (uint32×8192), `calibration`, `serial`, `note`, `max_rows=64`, `max_age_sec=600`, `fsync_batch=4`. `write_row(row: WaterfallRow)`: пишет spectrum uint16×8192 + duration uint16 = 16386 байт; fsync каждые 4 строки; ротирует сегмент при ≥64 строк или ≥600 с. `tick(now)` — проверка age-ротации без записи. `close()` — финализирует последний сегмент (пустой удаляется). Формат ASWF v3: magic `ASWF` + `header_len=4096` + JSON-заголовок + baseline 32 КБ + строки данных. Порт структуры из `firmware/atomspectra-waterfall/main/spectrogram.h`. Тесты: 12 юнит-тестов (`tests/test_aswf_writer.py`). Часть Фазы 6 Live-USB (#PC-1). **Полный pytest — 679 passed** (667 + 12 writer).

`#210` — **`awf/usb/collector.py`: cumulative→delta преобразователь** (`awf/usb/collector.py`, `tests/test_collector.py`, `scripts/ollama/_spec_210_collector.md`, `scripts/ollama/_spec_210_test_collector.md`). Порт `wf_task()` из `firmware/atomspectra-waterfall/main/spectrogram.c` (строки 667–723). `DeltaCollector` хранит `_prev_bins` (uint32×8192), `_prev_total`, `_prev_time`, `_has_prev`. `feed(snap)` детектирует reset (`not has_prev` OR `snap.total_counts < prev_total`), считает векторную дельту через numpy int64 + `np.clip(delta, 0, 65535, out=…)` → uint16. `dt = reset ? cur_time : (cur_time >= prev_time ? cur_time - prev_time : 0)`; клип 65535. State update — `_prev_bins = snap.bins.copy()` (копия, не ссылка). Типы `SpectrumSnapshot`/`WaterfallRow` — `@dataclass(frozen=True)`. Read-only свойства `prev_total`/`prev_time`/`has_prev`. Генерация через Ollama qwen3-coder:30b (§16). Правка Ollama-огреха inline: (1) trailing ` ``` ` в конце файла (SyntaxError line 88) — удалена через `sed -i '88d'`. 12/12 тестов зелёные. Часть замечания #PC-1 (Live-USB collector, Фаза 6). **Полный pytest — 667 passed** (655 + 12 collector).

`#209` — **`awf/usb/device.py`: pyserial reader-поток + shproto-декодер** (`awf/usb/device.py`, `tests/test_device.py`, `scripts/ollama/_spec_209_device.md`, `scripts/ollama/_spec_209_test_device.md`). Qt-free `SerialSpectraDevice`: конструктор `(port, baudrate=115200, *, on_frame=None, on_error=None, read_chunk_size=512, read_timeout_s=0.1)`. `open()` идемпотентен, `serial.serial_for_url` (поддерживает `"COM16"` и `"loop://"` echo для тестов без железа). Reader daemon-поток `_reader_loop` читает чанки, скармливает `ShprotoDecoder`, кладёт кадры в `queue.Queue` + `on_frame(cmd,payload)` под try/except (callback-исключения не валят reader; `on_error(exc)` при SerialException/OSError один раз перед выходом). `send_frame(cmd,payload)` под `_write_lock` требует `is_open` иначе `RuntimeError("device not open")`; возвращает записанные байты. `read_frame(timeout)` / `drain_frames()` — синхронный API поверх очереди. `close()` идемпотентен: `_stop.set()`, `thread.join(2.0)`, `_ser.close()`. Генерация через Ollama qwen3-coder:30b (§16). Правки Ollama-огрехов: (1) в device.py `_reader_loop` — убран внешний `try/…finally: return` (SyntaxWarning: return в finally); (2) в первом gen тестов Ollama пропустил `dev.open()` в 8 тестах — spec обновлён (KRIT-блок про обязательный `open()`), regen — все 12 тестов зелёные. Часть замечания #PC-1 (Live-USB collector, Фаза 6). **Полный pytest — 655 passed** (643 + 12 device).

`#208` — **порт shproto: encoder/decoder + CRC-16 табличный** (`awf/usb/shproto.py`, `tests/test_shproto.py`, `scripts/ollama/_spec_208_shproto.md`, `scripts/ollama/_spec_208_test_shproto.md`). Чистый Python-порт из `firmware/atomspectra-waterfall/components/shproto/shproto.c` (144 строки C). 256-entry `_CRC16_TABLE` verbatim. `ShprotoDecoder` (state-машина `byte_received/feed/take_frame/frames/reset` + счётчик `dropped`), `ShprotoEncoder` (`encode(cmd,payload)` статический + потоковый `packet_start/packet_add_data/packet_complete`). Константы: `SHPROTO_START=0xFE`, `SHPROTO_ESC=0xFD`, `SHPROTO_FINISH=0xA5`, `CRC_INIT=0xFFFF`; escape маркер → `ESC + (~byte & 0xFF)`; валидность RX — `crc16_buf(cmd|payload|crc_le)==0`. Генерация через Ollama qwen3-coder:30b (§16). Правка Ollama-огреха inline: (1) `packet_start` пропустил `self._crc = _crc16_byte(self._crc, cmd)` — уронил `test_streaming_encoder_matches_static` (b'\x96' != b'\xb4'); (2) `test_second_start_resets_buffer` использовал `second_valid[2:]` (срез SHPROTO_START обесценил цель теста) → `[1:]`. 12/12 тестов зелёные. Часть замечания #PC-1 (Live-USB collector, Фаза 6). **Полный pytest — 643 passed** (631 + 12).

`#207` — **каркас `awf/usb/` + `pyserial` dep + `[tool.setuptools] packages`** (`awf/usb/__init__.py`, `pyproject.toml`, `requirements.txt`). Создан Qt-free пакет `awf.usb` (планируемые модули: `shproto`, `device`, `collector`). `pyserial>=3.5` добавлен в `pyproject.toml` (после `PyOpenGL>=3.1.7`) и в `requirements.txt`. `awf.usb` зарегистрирован в `[tool.setuptools] packages`. Часть замечания #PC-1 (Live-USB collector из firmware/atomspectra-waterfall). **Полный pytest — 631 passed** (baseline не менялся).

`#206` — **релиз v0.1.4: пересборка exe, доки, пуш, GitHub release** (команда оператора: «пересобери exe, оформляй доки, пуш, грузи релиз»; версия v0.1.4 выбрана через AskUserQuestion — patch-bump с 0.1.3, т.к. #196–#205 накопились после бампа 0.1.3). Хардкод версии обновлён в 3 местах: `awf/__init__.py` `__version__="0.1.4"` (SSOT, `pyproject.toml` тянет динамически через `attr=awf.__version__`), `CLAUDE.md:28` и `docs/dev/KNOWN_ISSUES.md:22` (счётчик 625→631 passed). `INSTALL.md`/`README` версию не хардкодят (плейсхолдер `vX.Y.Z` + ссылка `/releases/latest`). `.gitignore` +3 правила: `*.zip` (релизные архивы в GitHub Releases, не в git), `build_log.txt`, `test_out.txt` (артефакты прогонов). Пересборка: `py -3.14 -m PyInstaller waterfall-viewer.spec --noconfirm` (onedir) → `dist/waterfall-viewer/waterfall-viewer.exe` **18.0 МБ** (spec удалил `opengl32sw.dll` пост-билд), **461 файл** в дистрибутиве. Архив `waterfall-viewer-v0.1.4-win64.zip` **91.0 МБ** (Compress-Archive из `dist/waterfall-viewer/*`). Коммит — конкретные пути (§7 секрет-гигиена, никогда `-A`): `awf/__init__.py` + 4 UI-файла (#203/#204/#205) + `docs/dev/CLAUDE_NOTES.md` + `docs/dev/KNOWN_ISSUES.md` + `CLAUDE.md` + `.gitignore` + `tests/test_group4_remarks.py`; pre-commit PII/секрет-скан на diff — чисто. Тег `v0.1.4` + push, `gh release create v0.1.4` с zip и RU-нотами. **Полный pytest — 631 passed** (только строка версии + доки после прогона, логика кода не менялась).

`#204` — **нижний график «Скорость в полосе»: при загрузке Y-ось авто-подгон под данные, а не 0–372** (`awf/ui/panels.py::SlicePanel._expand_series_y_if_needed` — порядок `setLimits` ДО `setYRange`). Скриншот оператора: только открыл файл, ничего не трогал — Y-ось нижнего графика открывалась ~0–372 (данные сплюснуты внизу ~90 cps) вместо подгонки под высоту полосы ROI. **Корень (Phase 1, faithful MainWindow-репро на реальном .aswf через `_on_loaded`):** `_expand_series_y_if_needed` вызывается ДВАЖДЫ за загрузку. (1) синхронно из `_lock_views_to_data` при `_raw_series` = ПОЛНАЯ полоса каналов (`band_time_series(0, n_ch)`, max ~673 cps) → ставит `setYRange(0,707)` + `setLimits(minYRange=707, maxYRange=2827)`; (2) `heatmap.set_spectrogram` эмитит `roiChanged` дефолтной ЦЕНТРАЛЬНОЙ четверти → `show_roi` → `_set_series` меняет `_raw_series` на ROI-полосу (центр каналов, max ~89 cps) без ре-подгона Y; (3) отложенный `singleShot(0)` вызывает `_expand` уже с ROI-полосой (y_top≈93), но `setYRange(0,93)` шёл ПЕРВЫМ и ужать не мог — устаревший `minYRange=707` держал диапазон, а следующий `setLimits(maxYRange=93*4=372)` прижимал Y к потолку `y_top*4`. Отсюда стабильные «0–372». **Фикс:** `setLimits(...)` вызывается ДО `setYRange(0, y_top)` — новые (меньшие) лимиты вступают в силу первыми и позволяют ужать диапазон до фактических данных; сохранены инварианты #203 (`minYRange=y_top` — нельзя ужать ниже подгонки, `maxYRange=y_top*4` — потолок). Проверка на реальных файлах: было Y=`[0,372.1]` → стало `[0,93.0]` (данные 88.6 cps); второй файл `[0,94.2]` (данные 89.7). **Полный pytest — 631 passed.**

`#203` — **нижний график: Y-масштаб нельзя ужать ниже начальной подгонки** (`awf/ui/panels.py::SlicePanel._expand_series_y_if_needed` — `setLimits(minYRange=y_top)`). Добавлен нижний предел диапазона Y = стартовая высота подгонки, чтобы зумом нельзя было схлопнуть график ниже видимой полезной высоты (потолок `maxYRange=y_top*4` сохранён). **Полный pytest — 631 passed.**

`#205` — **3D-вьюер: начальная ориентация при загрузке — начало данных вверху (дальний угол)** (`awf/ui/view3d.py` — `set_spectrogram` блок `is_new` кадрирует камеру `azimuth=45`; инициализация вьюера остаётся `azimuth=-45`). Угол (0,0) = старт по времени в дальнем углу. Тест `test_axis_labels_follow_viewer_side` обновлён под новый дефолтный азимут (tm0/en0 `pos>0`). **Полный pytest — 631 passed.**

`#195` — **Энергоокно: «— откл —» и «— вручную —» в комбобоксе** (`awf/ui/panels.py` — 2 специальных пункта в начале `_ewin_preset`: idx 0 = «— откл —» (гасит жёлтую кривую, `_ewin_active=None`), idx 1 = «— вручную —» (применяет текущие спинбоксы `_ewin_lo`/`_ewin_hi`); пресеты нуклидов сдвинуты на idx 2+. `_on_ewin_preset` переписан: idx 0 → `_ewin_curve.setData([],[])`, idx 1 → `show_energy_window(spin)`, idx≥2 → пресет `DEFAULT_WINDOWS[idx-2]`. `_on_ewin_spin` → устанавливает idx=1 («вручную») вместо -1. `retranslate` обновлён (+2 оффсет). `awf/ui/i18n.py` +2 строки EN («— off —» / «— manual —»). Тесты `test_slice_analysis.py`: 3 теста обновлены под новую структуру (индексы, `test_ewin_preset_structure` заменил `test_ewin_preset_has_no_manual_item`). **Полный pytest — 625 passed.**

`#194` — **окно срезов: лимит 3000 кэВ + минимальный зум + LMB-drag pan** (`awf/ui/panels.py` — `_NoPanViewBox` → `_PanViewBox`: `setMouseMode(PanMode)` + `mouseDragEvent` фильтрует только LMB (RMB/MMB игнорируются → `ev.ignore()`); ось X ограничена `min(emax, 3000.0)` в `set_spectrogram` (спинбоксы `_ewin_lo`/`_ewin_hi`) и `_lock_views_to_data` (`setLimits(xMax=3000, maxXRange=3000-emin)`); зум колесиком по курсору работает (PanMode). Тест обновлён: `test_slice_spectrum_no_mouse_pan` → `test_slice_spectrum_pan_viewbox` (проверяет `isinstance(_PanViewBox)`, `mouseEnabled==[True,True]`, RMB-drag не меняет viewRange). **Полный pytest — 625 passed.**

`#193` — **одиночные энергетические иглы на правом краю спектра 3D** (`awf/ui/view3d.py` — трёхточечный медианный фильтр по оси E (axis=1), порог 10×, в `set_spectrogram` после блока #192). Оператор (скриншот крупного плана правого края 3D-водопада): «меньше, но иглы остались». Корень: одиночный аномальный канал на правом краю спектра захватывался как max своего energy-LOD-бина (`np.maximum.reduceat`, method="max") → игла на правом краю рельефа. Фикс: `_epr = np.concatenate([z_counts[:, :1], z_counts[:, :-1]], axis=1)`, `_enx = np.concatenate([z_counts[:, 1:], z_counts[:, -1:]], axis=1)`, `_emed3 = np.median(np.stack([_epr, z_counts, _enx], axis=0), axis=0)`, `_espike = z_counts > (10.0 * np.maximum(_emed3, 1e-10))` → замена медианой. Порог 10× консервативнее 6× по t: реальные фотопики NaI span ≥6 LOD-бинов по E → median высокий → не давится. Оператор принял результат: «оставляй так». **Полный pytest — 625 passed.**

`#192` — **одиночные временны́е иглы на границах зон 3D (рецидив #181)** (`awf/ui/view3d.py` — трёхточечный медианный фильтр по оси t (axis=0), порог 6×, в `set_spectrogram` после `_z_counts_int = z_int`). Оператор (2 скриншота 3D-водопада): «Остались единичные иглы на границах зон. Ищи дальше». Корень: `np.maximum.reduceat` (LOD method="max") захватывал одиночный аномальный срез на границе зоны как max LOD-бина → игла вверх по оси времени. Фикс (Phase 3 systematic-debugging): `_pr = np.concatenate([z_counts[:1], z_counts[:-1]], axis=0)`, `_nx = np.concatenate([z_counts[1:], z_counts[-1:]], axis=0)`, `_med3 = np.median(np.stack([_pr, z_counts, _nx], axis=0), axis=0)`, `_spike = z_counts > (6.0 * np.maximum(_med3, 1e-10))` → замена медианой. Два смежных ненулевых бина = настоящий источник (median высокий) → не подавляется. Граничные строки при самоповторе: median == self → `_spike` False. **Полный pytest — 625 passed.**

`#191` — **io P1: n42 и aswf не держат усечённый файл — тихая потеря данных / сырой traceback** (`awf/io/n42_loader.py` — P1-n42-1/P1-n42-2/P1-n42-3; `awf/io/aswf_loader.py` — P1-aswf-1/P1-aswf-2/P1-aswf-3). Шесть P1-находок code-audit-core. **n42:** P1-n42-1 — `etree.iterparse` без try/finally: file handle оставался открытым на Windows при исключении внутри цикла → `try/finally: del context`; P1-n42-2 — `np.fromstring` тихо останавливается на нечисловом токене без предупреждения → сравнение `len(toks) < n_raw` + `warnings.warn` с диагностикой; P1-n42-3 — `context.error_log` читался ДО `del context` → moved после `del`, выброс `warnings.warn` при непустом логе. **aswf:** P1-aswf-1 — файл короче 8 байт → `struct.error` → явный `if len(head) < 8: raise ValueError`; P1-aswf-2 — файл обрезан посреди заголовка → сырой `UnicodeDecodeError`/`JSONDecodeError` → `try/except (UnicodeDecodeError, json.JSONDecodeError) as exc: raise ValueError(...) from exc`; P1-aswf-3 — неполная последняя строка (краш прибора) тихо игнорировалась → `_tail = (file_size - data_off) % row_stride; if _tail: warnings.warn(...)`. **Полный pytest — 625 passed.**

`#190` — **перф F11/F12: `_found_peaks` без кэша вызывалась на каждый слайдер/перестройку** (`awf/ui/view3d.py` — кэш `_peaks_cache_key`/`_peaks_cache` в `_found_peaks()`, инвалидация по `(id(sg), _peak_sigma)`). Находки F11/F12 code-audit-core: `_found_peaks()` запускала полный поиск пиков (Mariscotti+Currie, автокалибровка FWHM) при каждом вызове — при движении слайдеров регулировок/сечений вызывалась 5–10 раз за кадр. Пики не зависят от положения секущих плоскостей и регулировок вида (только от `sg` и `_peak_sigma`). Фикс: `_ck = (id(sg), self._peak_sigma); if getattr(self, '_peaks_cache_key', None) == _ck: return self._peaks_cache`; после вычисления — `self._peaks_cache_key = _ck; self._peaks_cache = result`. Инвалидация автоматическая: при новом файле или смене порога σ `id(sg)` или `_peak_sigma` меняется. **Полный pytest — 625 passed.**

`#188` — **F13: `net_height ≤ 0` в SNIP-режиме тихо роняет значимый пик** (`awf/analysis/peaks.py` — строки 1068-1076: fallback `net_height = float(counts[ch])` при `snip_iterations != 0`). Находка F13 code-audit-core: при `snip_iterations > 0` (SNIP-континуум вычтен до детекции) `counts[ch]` уже есть нетто-высота; локальный фон из плеч (`b_left`/`b_right`) тогда является артефактом двойного вычета и может превысить `counts[ch]` → `net_height = counts[ch] - b ≤ 0` → `continue` (пик потерян). В gross-режиме (`snip_iterations == 0`) канал реально ниже фона → skip правильный. Фикс: `if net_height <= 0.0: net_height = float(counts[ch]) if snip_iterations != 0 else 0.0` + повторная проверка `if net_height <= 0.0: continue`. **Полный pytest — 625 passed.**

`#187` — **F14 XXE: `etree.iterparse` в n42_loader.py без защиты от XML External Entity** (`awf/io/n42_loader.py` — `resolve_entities=False, no_network=True` добавлены в параметры `iterparse`). Находка F14 code-audit-core: N42-файлы поступают от разных операторов (атомные объекты, радиационный контроль); без `resolve_entities=False` lxml мог обработать `<!ENTITY xxe SYSTEM "file:///etc/passwd">` или `<!ENTITY xxe SYSTEM "http://…">` — XXE-атака через вредоносный N42-файл. `no_network=True` дополнительно блокирует любые сетевые fetch внутри XML. Оба флага добавлены в вызов, поведение на корректных файлах не меняется. **Полный pytest — 625 passed.**

`#173` — **разделить панель «Нуклиды» на два отдельных дока: «Библиотека нуклидов» и «Идентификация по найденным пикам»** (`awf/ui/main_window.py` — `ndock` / `_nlib_dock` objectName `"dock_nuclide_lib"` + новый `nidock` / `_nident_dock` objectName `"dock_nuclide_ident"`, `tabifyDockWidget(ndock, nidock)`; меню «Изотопы» и «Инструменты» — `toggleViewAction()` для каждого дока). До задачи: оба виджета (библиотека + идентификация по пикам) находились в единой `NuclidePanel` за одним `QDockWidget`. После: `_nuclides` (библиотека, дерево нуклидов + подсветка маркеров) — отдельный док слева; `self._nuclides.ident_widget` (таблица совпадений, кнопки фильтрации) — второй док слева, по умолчанию вынесен в отдельную вкладку рядом с первым (`tabifyDockWidget`). Оба дока добавлены в меню «Изотопы» (два `toggleViewAction`) и в меню «Инструменты» (аналогично). QSettings сохраняет расположение обоих по objectName. **Полный pytest — 625 passed.**

`#172` — **сглаживание простыни 3D по оси времени: целиком и по сегментам** (`awf/ui/view3d.py` — поля `_t_smooth: int` (режим 0..6) и `_t_smooth_by_seg: bool`; module-level функция `_smooth_by_segs(arr, mode, radius, t_centers, seg_bounds_sec)` — применяет SMA/WMA независимо внутри каждого временно́го сегмента; `set_t_smoothing(mode, by_seg)` — публичный сеттер, перестраивает рельеф при живой спектрограмме; в `set_spectrogram` после энергетического сглаживания (#90) добавлен блок `if self._t_smooth: smooth_by_mode(axis=0)` или `_smooth_by_segs(...)` при `_t_smooth_by_seg`; `set_segments()` тоже перестраивает рельеф при активном «по сегм.»; `awf/ui/main_window.py` — проводка через knobs-словарь `v["tsmooth"]` / `v["tsmooth_by_seg"]` в `set_t_smoothing()`; при первом включении «по сегм.» → авто-запуск сегментации (#174)). Сглаживание по оси времени убирает LOD-зернистость при крупных прыжках зон и между сегментами без воздействия на энергетический профиль. **Полный pytest — 625 passed.**

`Аудит P-2` — **кеш `counts_in_unit('cps')` — устранение повторных float64-копий при перестройке рельефа** (`awf/model/spectrogram.py` — `counts_in_unit()` хранит результат в `self._counts_in_unit_cache[mode]`, lazy-initialized dict; на объекте `Spectrogram` `self.counts` не переприсваивается после `__init__`, поэтому один расчёт на экземпляр достаточен; `counts` и `live_time_s` не мутируются → кеш корректен навсегда). Находка P-2 code-audit-core: `counts_in_unit` вызывался многократно в одном rebuild (surface + bg_sheet + peak_mask + slice) → каждый раз выделялось ~57 МБ float64 и выполнялось деление по всем срезам. Кеш инвалидируется автоматически при создании нового объекта `Spectrogram` (новый файл/trim). **Полный pytest — 625 passed.**

`Аудит P-3` — **переиспользование `zmax` образца в `_add_bg_sheet` — устранение лишнего `apply_z_scale`** (`awf/ui/view3d.py` — поле `self._zmax_sample = 0.0` в `__init__`; в `set_spectrogram` после `z_disp = apply_z_scale(z_counts, ...)` и до floor-сдвига: `self._zmax_sample = float(z_disp.max()) if z_disp.size else 0.0`; в `_add_bg_sheet` удалён повторный вызов `apply_z_scale(self._z_counts, ...)` + `.max()`, заменён на `zmax = self._zmax_sample`). Находка P-3 code-audit-core: `_add_bg_sheet` повторял ту же дорогостоящую Z-трансформацию только ради получения `zmax`, уже посчитанного в `set_spectrogram`. `_zmax_sample` сохраняет значение до floor-сдвига, что семантически верно: высотная нормировка фона должна совпадать с нормировкой рельефа-образца. **Полный pytest — 625 passed.**

> ⚠ **Коллизия номеров (2026-07-05):** ниже три операторских замечания идут под номерами `#215`/`#216`/`#217`, полученными в TaskList для реплики оператора «1 меню калибровки. 2 сохранить файл. 3 создать спектр из спектрограммы». Те же номера `#215`/`#216` УЖЕ заняты выше в этом файле — записи Phase 6 PC-1 (`spe_loader` / `csv_loader`). Записи ниже — про **операторские правки UI/экспорта**, никак не связаны с loader'ами. Различать по существу задачи; в TASKS.md — тоже с явной пометкой.

`#215` (оператор) — **меню «Калибровка» + интерактивная перекалибровка** (`awf/ui/calibration_dialog.py` NEW 235 стр — `CalibrationDialog(QDialog)`: таблица «канал ↔ известная энергия (кэВ)», авто-подстановка каналов найденных пиков, полиномиальный фит degree 1..4 через `numpy.polyfit`, live-preview новых коэффициентов; `awf/ui/main_window.py:24-25` — импорт `CalibrationDialog` и `Calibration` из `model.spectrogram`, `main_window.py:331` — новое верхнее меню `("calibration", "Калибровка")` рядом с «Инструменты», `main_window.py:383/837-846` — экшн `_open_calibration_dialog` открывает диалог для активной спектрограммы, применяет `sg.calibration = Calibration(coeffs)`, вызывает `_on_calibration_changed` (перестройка пиков/маркеров/меток осей); `awf/ui/i18n.py` — «Калибровка»→«Calibration», строки диалога). Запрос оператора: «я просил меню калибровки добавить». Работает по цепочке: пользователь запускает поиск пиков (Mariscotti+Currie) → в диалоге сопоставляет каналы с эталонными γ-линиями (Cs-137 661.66 / K-40 1460.82 / Tl-208 2614.51 и т.д.) → фит → замена калибровки → авто-переоткрытие маркеров нуклидов и осей энергии. Тесты калибровки идут через Phase 6 PC-1 baseline; отдельного `test_calibration_dialog.py` нет (Qt-диалог + numpy.polyfit, покрывается интеграционно через `test_group4_remarks.py::test_top_menus_skeleton_present` — меню `calibration` присутствует в списке верхних меню). **Полный pytest — 730 passed.**

`#216` (оператор) — **пункт «Сохранить как…» в меню Файл → `.aswf` v3 с текущей калибровкой** (`awf/io/aswf_single_writer.py` NEW 125 стр — `write_aswf_single(path, sg, note="")`: пишет одиночный ASWF v3 (magic `ASWF` + uint32 `header_len=4096` + JSON `hdr` + опц. baseline uint32×8192 + строки uint16×8192 `counts` + uint16 `duration_s`), заголовок содержит текущий `sg.calibration.coeffs`, `sg.t0_iso`, `serial` (обезличено — «unknown»), `note`, `interval_sec` (медиана `sg.real_time_s`), `n_channels=8192`; проверка `sg.counts.shape[1] == 8192`, отсутствующий baseline при `sg._baseline is None` → флаг `has_baseline=false` в JSON; `awf/ui/main_window.py:303,861+` — экшн «Сохранить как…» в меню «Файл» после «Открыть», `_do_save_as_aswf` открывает `QFileDialog.getSaveFileName(filter="ASWF (*.aswf)")` — принудительное `.aswf`, вызывает `write_aswf_single(path, self._sg, note=…)`, показывает `QMessageBox.critical` при исключении, статус-строка «Сохранено: …» при успехе; `awf/ui/i18n.py` +2 строки — «Сохранить как…»/«Save as…», «Ошибка сохранения»/«Save error»; `tests/test_aswf_single_writer.py` NEW 69 стр, 4 теста — round-trip через `aswf_loader.load_aswf`, сохранение калибровки, отсутствие baseline при `_baseline=None`, обезличенный serial). Запрос оператора: «И пункт сохранить файл в меню файл». Полезно для сохранения перекалиброванной (после #215) или обрезанной спектрограммы. **Полный pytest — 734 passed.**

`#217` (оператор) — **«Создать спектр из спектрограммы» → экспорт 1D-спектра в BecqMoni `.tka` / LSRM-IAEA `.spe` / InterSpec-ANSI N42 `.n42`** (`awf/io/tka_writer.py` NEW 41 стр — `write_tka(path, counts_1d, *, live_time_s, real_time_s)` ASCII: 2-строчный header `int(live)\nint(real)\n`, дальше по одному int-отсчёту на строку; отвергает 2D `ValueError("expected 1D")`, float→int через `round`; `awf/io/spe_writer.py` NEW 73 стр — `write_spe(path, counts_1d, *, live_time_s, real_time_s, calibration, spec_id="waterfall-viewer")` LSRM/IAEA ASCII секции `$SPEC_ID:`/`$DATE_MEA:`(UTC ISO)/`$MEAS_TIM:`(int live/real)/`$DATA:`(0..N-1 + int counts построчно)/`$ENER_FIT:`(a0 a1, %f)/`$MCA_CAL:`(N-строчный полином с суффиксом ` keV`)/`$ROI:`(0 записей)/`$PRESETS:`; `awf/io/n42_writer.py` NEW 87 стр — `write_n42(path, counts_1d, *, live_time_s, real_time_s, calibration)` ANSI N42.42-2012 XML через `xml.etree.ElementTree` в namespace `http://physics.nist.gov/N42/2011/N42`: `<RadInstrumentData>/<RadMeasurement>/<Spectrum>` с `<EnergyCalibration>/<CoefficientValues>` и `<ChannelData>` (пробел-разделённые int-отсчёты); `awf/ui/main_window.py` — новый экшн «Создать спектр из спектрограммы» в меню «Файл» после «Сохранить как…», handler-цепочка `_export_spectrum` → `_detect_export_fmt(path)` → `_do_export_spectrum` → `_write_spectrum` (§16-split на 4 метода <25 net-стр каждая) — `QFileDialog.getSaveFileName(filter="BecqMoni (*.tka);;LSRM/IAEA (*.spe);;InterSpec/ANSI N42 (*.n42)")` с принудительным расширением по выбранному фильтру, агрегация `spec = sg.total_spectrum().astype(int64)` (Σ counts по времени), `lt = Σ sg.live_time_s`, `rt = Σ sg.real_time_s`, `cal = list(sg.calibration.coeffs)`, диспетчер по формату, `QMessageBox.critical` при ошибке, статус-строка «Экспорт: …» при успехе; `awf/ui/i18n.py` +4 строки — «Создать спектр из спектрограммы»/«Export Spectrum…», «Ошибка экспорта»/«Export error», «Сначала откройте спектрограмму»/«Open spectrogram first», «Экспорт»/«Exported»; `tests/test_spectrum_export.py` NEW 79 стр, 5 тестов — `test_tka_header_and_counts` (header + int-отсчёты), `test_spe_sections` (все обязательные `$`-секции + `1.500000 0.300000` в `$ENER_FIT:`), `test_n42_valid_xml_and_channeldata` (парсится ElementTree в правильном namespace, `<EnergyCalibration>` содержит коэф. `0.3`, `<ChannelData>` = int-отсчёты), `test_writers_accept_float_counts` (float 1.4/2.6/3.5/0.0 → 1/3/4/0 через `round`), `test_reject_2d` (все три отвергают 2D с `ValueError("1D")`)). Запрос оператора: «сделай еще пункт создать спектр из спектрограммы с сохранением в форматы бекмони, лсрм и интерспек». Полезно для экспорта агрегированного долгого замера в стандартные γ-спектрометрические тулчейны. **Полный pytest — 749 passed** (730 baseline после Phase 6 csv_loader + 4 aswf_single_writer #216 + 5 spectrum_export #217 + 10 becqmoni_loader Phase 6 закрытие; teardown Qt warnings pyqtgraph LabelItem/_SeriesPanViewBox после summary-строки — безобидны, exit 0. 41 UI-fail «cross-test poison _export_spectrum/_save_as_aswf» из старой Phase 6 записи #217 becqmoni_loader в TASKS.md — устарел, реальный прогон 2026-07-05 чистый).

`#218` — **`awf/usb/assembler.py` — Python-порт firmware staging-логики (HistogramAssembler)** (`awf/usb/assembler.py` NEW 116 стр; `tests/test_assembler.py` NEW 105 стр). Константи: `CHANNELS=8192`, `CMD_HISTOGRAM=0x01`, `CMD_STAT=0x04`; `_CHUNK_BINS=64` (128 чанків на свіп) — локально в тестах. `SweepResult(bins, total_counts, total_time_sec, cps)` — frozen dataclass. `HistogramAssembler.feed(cmd: int, data: bytes) -> SweepResult | None` — два окремі аргументи (НЕ tuple): `cmd==CMD_HISTOGRAM` → парсити chunk (offset uint16 LE + bins uint32 LE), зазор → drop; `cmd==CMD_STAT` → буферувати staged_time/staged_cps для наступного свіпу; решта → None. Свіп повний при `next_offset >= CHANNELS`. Порт `spectrum_process_histogram_chunk/stat` firmware. 10 unit-тестів зелені. Spec: `scripts/ollama/_spec_218_assembler.md` + `scripts/ollama/_spec_218_assembler_tests.md`. **Повний pytest — 759 passed, exit 0.**

`#212` — **`awf/ui/tab_live.py` — вкладка «Live USB» (пряме підключення AtomSpectra до PC)** (`awf/ui/tab_live.py` NEW 369 стр; `awf/ui/main_window.py` інтеграція). Пряме USB-CDC через pyserial (без ESP32). `BAUDRATE=600_000`, `_POLL_MS=200`, `_EMIT_EVERY=5`. UI: GroupBox «Підключення» (combo портів + Refresh + Connect/Disconnect), GroupBox «Статус» (CPS, час, свіпів/дропів), GroupBox «Запис .aswf» (папка + Start/Stop). Ланцюжок: `QTimer._poll()` → `device.drain_frames()` → `assembler.feed(cmd, data)` → `collector.feed(snap)` → `writer.write_row(row)` → `snapshotReady.emit(sg)` кожні `_EMIT_EVERY` свіпів. Сигнали: `snapshotReady = Signal(object)` (Spectrogram), `statusChanged = Signal(str)`. `main_window.py`: 4-та вкладка «Live USB», `_on_live_snapshot(sg)` → `_redistribute(reset=False)`. Import-огріх Ollama виправлено inline (<25 стр): `from awf.ui.i18n import tr, i18n` → `from awf.ui.i18n import tr` + `from awf.ui import i18n`. Spec: `scripts/ollama/_spec_212_tab_live.md`. Фізичне підключення AtomSpectra — за командою оператора. **Повний pytest — 759 passed, exit 0.**

`#225` (оператор) — **после «Применить» в диалоге калибровки не обновляются найденные пики и идентификация** (`awf/ui/view3d.py:1165` — ключ кэша `_peaks_cache_key` расширен: `(id(sg), self._peak_sigma)` → `(id(sg), id(sg.calibration), self._peak_sigma)`). Root cause: `_apply_new_calibration` (#215) в `main_window.py:851` присваивает `self._sg.calibration = Calibration(...)` — объект `sg` тот же, но `id(sg.calibration)` меняется. `_found_peaks()` в `view3d.py` кэшировал результат по `(id(sg), sigma)`; после калибровки ключ совпадал → `set_peaks(peaks)` в `_refresh_peaks_panel` получал старый список `FoundPeak` со старыми энергиями, соответственно `identify_peaks(...)` в панели идентификации нуклидов работал по устаревшим `pk.energy` (для оператора: 61.86 → 61.68 не пересчитывалось, Cs-137 не совпадал с 662, идентификация проваливалась). Инвалидация автоматическая — `Calibration(coeffs=...)` в `_apply_new_calibration` создаёт новый объект → новый `id`. Работает и через `_sg_analysis` (`_analysis_spectrogram` либо возвращает `self._sg` напрямую, либо новый `subtract_background(sg,...)` — в обоих случаях `id(calibration)` свежий). Запрос оператора: «после калибровки найденные пики не обновляются. идентификация соответственно не проходит». **Полный pytest — 759 passed, exit 0.**

`#226` (оператор, 2026-07-05) — **#217-fix: правильные форматы экспорта — BecqMoni XML + LSRM бинарный SPE**. Оператор дословно: «#217 — «Создать спектр из спектрограммы» → BecqMoni .tka / LSRM .spe / InterSpec .n42 - ошибка . Правильно Бекмони xml, ЛСРМ spe (бинарный). Переделывай все. Пуш и релизь». Спецификация ЛСРМ и BecqMoni взята из SpectraVibe (§12 read-only): `scripts/gamma/io/lsrm_spe.py::write_lsrm_spe` (LSRM SpectraLine 2.0 бинарный) и `scripts/gamma/io/becqmoni_xml.py::write_becqmoni_xml` (ResultDataFile FormatVersion 120920). Правки: (1) удалён `awf/io/tka_writer.py`; (2) NEW `awf/io/becqmoni_writer.py` (99 стр) — `write_becqmoni_xml(path, counts, *, live_time_s, real_time_s, calibration, mea_time, sample_name)` — строит XML `ResultDataFile/ResultDataList/ResultData/{SampleInfo,DeviceConfigReference,StartTime,EndTime,EnergySpectrum,Visible}`; `EnergySpectrum/{NumberOfChannels, ChannelPitch=1, EnergyCalibration/{PolynomialOrder=len(coefs)-1, Coefficients/Coefficient×N}, ValidPulseCount, TotalPulseCount, MeasurementTime %.4f (real_time), LiveTime %.6f, NumberOfSamples=0, Spectrum/DataPoint×N}`; BUG-BQ1 fix (пустая калибровка → identity `[0,1]`); ET.indent + `xml_declaration=True`; atomic write через `.tmp+replace`. (3) REPLACE `awf/io/spe_writer.py` (73 стр ASCII IAEA → 62 стр LSRM бинарный) — `write_spe(path, counts, *, live_time_s, real_time_s, calibration, mea_time, spec_id)` — CP-1251 header `KEY=VALUE\r\n` (SHIFR/NOMER/CONFIGNAME/MEASBEGIN формат `%d-%m-%y %H:%M:%S.ff`/TLIVE `%.2f`/TREAL `%.2f`/ENERGY `degree,c0,..,c6` — 7 фиксированных слотов/SPECTRSIZE), маркер `SPECTR=` без trailing `\r\n`, затем бинарный блок `uint32-LE` (`<u4`), по 4 байта на канал. (4) `awf/ui/main_window.py:891` — `flt="BecqMoni XML (*.xml);;LSRM (*.spe);;InterSpec/ANSI N42 (*.n42)"`; `_detect_export_fmt` (строки 902-916) — распознаёт `.xml`/`.spe`/`.n42` + fallback по имени фильтра (`becqmoni`/`lsrm`); `_write_spectrum` (933-943) — диспетчер `xml → write_becqmoni_xml(..., calibration=cal)` / `spe → write_spe(..., calibration=cal)` / n42. (5) `tests/test_spectrum_export.py` — 7 тестов (round-trip BecqMoni через `load_becqmoni`, структурный test `PolynomialOrder`/`Coefficient`/`DataPoint`, identity fallback для empty cal, `test_spe_binary_lsrm` — проверяет CP-1251 header `SHIFR=/TLIVE=60.00/TREAL=65.00/SPECTRSIZE=16/ENERGY=2,1.5,0.3,`, маркер `SPECTR=`, `np.frombuffer(binary, dtype="<u4")` == spec, n42 unchanged, float→int, reject 2D). Спеки → Ollama `qwen3-coder:30b` → `py_compile` + локальный pytest 7/7 зелёные. **Полный pytest — TODO** (обновится после прогона).

`#221/#222/#223/#224` (оператор) — **правки 2D-карты + диалога калибровки** (`awf/ui/panels.py` — `HeatmapPanel._ch_pen` width=1→2 (толще перекрестье); поле `self._floor_visible=True` + метод `_apply_cmap()`: при `_floor_visible=False` берёт `cmap.getLookupTable(0.0,1.0,256,alpha=True).copy()`, зануляет `lut[:13,3]=0` (нижние ~5% LUT прозрачны — аналог 3D `_FLOOR_FRAC`), `_img.setLookupTable(lut)`; иначе `_img.setColorMap(cmap)`; публичный `set_floor_visible(on)`; `set_colormap` теперь вызывает `_apply_cmap`; `awf/ui/main_window.py:_on_floor_toggled` — доп. вызов `self._heatmap.set_floor_visible(on)` (Подложка теперь работает и на 2D). `awf/ui/calibration_dialog.py` — таблица расширена до 4 колонок: новая `_COL_ENABLED=0` (чекбокс), остальные сдвинуты (+1); `_add_row` вставляет `QTableWidgetItem` с `ItemIsUserCheckable | ItemIsEnabled` и `Checked` в col 0; `_collect_pairs` пропускает строки где `checkState() != Checked`; `_on_table_changed` реагирует на смену чекбокса → `_update_row_dimming(row)` красит текст `#c0c0c0`/`#555555`; поле `self._active_nuclides: set = set(PRESETS.keys())` + UI-строка «Нуклиды: N/total ▼» (`QMenu` с чекбоксами `PRESETS`) и кнопка «Подобрать E ▼»; `_on_pick_energy` строит `sorted((abs(e-e_ref), n, e) for n in active for e in PRESETS[n])` — топ-25 в `QMenu`, выбор записывает в `E_TRUE`. Splits <25 net-стр каждый (§16). Запросы оператора: «сделай линию перекрестья чуть толще», «И отключение подложки не работает на 2Д», «сделай чтобы можно было отключать отдельные пики», «сделай чтобы можно было в окне Еист выбрать из списка нужную энергию нужного ближайшего по энергии нуклида из набора». **Полный pytest — 759 passed, exit 0** (teardown-warnings pyqtgraph LabelItem/_SeriesPanViewBox после summary — безобидный shiboken cleanup, не в моих правках).

`#REL-6` (2026-07-05) — **релиз v0.1.6** (`awf/__version__="0.1.6"`; коммит `5ce9960`; PyInstaller onedir → `dist/waterfall-viewer-0.1.6-windows-x64.zip` ~241 МБ; `gh release create v0.1.6`). **Полный pytest — 759 passed.**

`#BQ2` (2026-07-06) — **BecqMoni StartTime/EndTime: attach local TZ** (`awf/io/becqmoni_writer.py` — при `mea_time.tzinfo is None` → `mea_time.astimezone()` (attach системного TZ); оба поля `StartTime`/`EndTime` форматируются `isoformat(timespec="seconds")` с суффиксом `+HH:MM`). Запрос оператора: «старт-стоп время в бекмони без TZ».

`#MENU-2..5` (2026-07-06) — **реструктурировать меню + хоткеи + статусбар** (`awf/ui/main_window.py`, `awf/ui/i18n.py`):
- `#MENU-2`: убраны верхнеуровневые «Изотопы» и «О программе»; «Инструменты»→«Вид»; новый порядок Файл→Вид→Анализ→Калибровка→Сервис→Справка; «О программе» смёржено в «Справка» (Windows-конвенция); F1 → «Справка…».
- `#MENU-3`: меню «Анализ» реструктурировано через подменю «Фон» / «Пики» / «Эффективность» + «Мощность дозы» отдельным пунктом; Python-ref `self._bg_menu`/`_peaks_menu`/`_eff_menu` против GC.
- `#MENU-4`: хоткеи Ctrl+E экспорт, Ctrl+F поиск пиков, Ctrl+B выбор фона, Ctrl+Shift+B вычет фона, Ctrl+K калибровка, Ctrl+T сегментация.
- `#MENU-5`: `_act_eff_info` (disabled-пункт меню с именем кривой ε(E)) удалён; вместо него постоянный `QLabel self._eff_info_label` в `statusBar().addPermanentWidget(…)`, обновляется через `_update_eff_info()`.
- i18n: +3 строки «Фон»→«Background», «Пики»→«Peaks», «Эффективность»→«Efficiency».
- Тесты обновлены: `tests/test_group4_remarks.py` (новая структура меню, подменю Фон через `w._bg_menu`), `tests/test_segments_ui.py` (`_menus["view"]`), `tests/test_efficiency.py` (`_eff_info_label`).
**Полный pytest — 761 passed.**

`#REL-7` (2026-07-07) — **релиз v0.1.7** (`awf/__version__="0.1.7"`; коммиты `527f9bb`+`458bca9`; PyInstaller onedir → `dist/waterfall-viewer-0.1.7-windows-x64.zip` ~99 МБ zip / 241 МБ распакованный; `gh release create v0.1.7`). **Полный pytest — 761 passed.**

`#228..#230` (2026-07-07, откачены) — **попытка управления окнами EdgeBar + PyQtAds — ОТКАТ**. Хронология: `#228` EdgeBar (боковые флажки для скрытых доков), `#229` SidePageBar (BecqMoni-style), `#230` миграция PySide6→PyQt5 + PyQtAds CDockManager — всё откачено по требованию оператора (exe не запускался, боковые панели не нужны). HEAD после откатов: `549b428` (Revert "#228"). Pip: только PySide6 6.11.1 (PyQt5/PyQtAds удалены). Код: PySide6, без EdgeBar, без ADS. **Полный pytest — 761 passed (exit 0).** dist/ содержит v0.1.7 exe — требует пересборки без EdgeBar (в релизном exe #228 ещё присутствовал).

`#231` (2026-07-08) — **дефолтный layout окон = скриншот оператора, зафиксирован как первый запуск** (`main_window.py`): убран `tabifyDockWidget` правых доков — «Срезы/Сечения/Выборки» и «Сечения (3D)» теперь раздельными блоками друг под другом (не в одной вкладке). `_restore_layout()`: при отсутствии сохранённого `QSettings` state → `_apply_default_layout()` прячет все левые доки (Нуклиды×2/Регулировки/Найденные пики/Сегментация). Новый пункт «Сервис → Сбросить layout окон» (`_reset_layout()`) — удаляет `geometry`/`windowState` из `QSettings` и переприменяет дефолт, без правки реестра вручную. i18n: +«Сбросить layout окон»/«Language»/«Русский». Оператор подтвердил «работает». **Полный pytest — 761 passed.**

`#232` (2026-07-08) — **пересборка exe v0.1.7 после #231; попутно найден и исправлен мёртвый spec-файл**. Root cause (систематическая отладка): `waterfall-viewer.spec` со времён отката #230 остался с `import PyQtAds`/PyQt5-хуками — сборка падала `ModuleNotFoundError: No module named 'PyQtAds'` (пакет удалён при откате). Спек переписан под текущий стек PySide6 (hiddenimports/datas/plugins PySide6 вместо PyQt5+PyQtAds, exclude PyQt5/PyQtAds/PySide2). PyInstaller onedir собран, smoke-launch exe (6 c, не упал) → упакован `waterfall-viewer-0.1.7-windows-x64.zip` (~108 МБ) в корне проекта. Пуш/GitHub-релиз не запрошен оператором — не выполнялся.

`#DATA-1` (2026-07-09, оператор) — **проверка целостности .aswf v4 + отчёт при загрузке**. Формат-референс `ASWF_V4_FORMAT.md` (зона ESP32, read-only): v4 несёт per-row CRC32. `awf/io/aswf_loader.py` — при разборе v4 считает CRC32 каждой строки данных, сверяет с сохранённым; собирает `integrity_report` (`version`, `checked_rows`, `ok_rows`, `bad_rows: [индексы]`, `crc_present: bool`). `awf/model/spectrogram.py` — новый параметр/атрибут `integrity_report: dict | None = None` (проброс в `trimmed_channels`). `awf/ui/main_window.py` — суффикс статуса «✓ целостность N/N» либо «⚠ повреждено M строк»; при повреждении — `QMessageBox.warning` со списком битых строк. Тесты: `test_aswf_v4_integrity_ok`, `test_aswf_v4_integrity_corrupt` (2 нетто-новых). Запрос оператора: «Добавь проверку целостности данных и отчёт по проверке». **Полный pytest — 763 passed.**

`#UI-233` (2026-07-09, оператор) — **растянуть ось времени под размер окна (спектрограмма перестала ужиматься)**. Root cause: 3D-геометрия в INDEX-пространстве, 1 world-unit на индекс. При nt≪nc (напр. 10 временны́х бинов × 256 каналов) лента ~25× длиннее по энергии → время сплющено, подписи нечитаемы. Fix (`awf/ui/view3d.py`): поле `self._x_scale` (world-units на индекс времени); в `set_spectrogram` авто-fit `self._x_scale = float(nc)/float(nt) if nt>0 else 1.0` («квадратит» лист под окно); все 8 world-X сайтов домножены на `x_scale` — поверхность/подложка через item `.scale(x_scale,1,1, local=False)` (композиция S·T → centered scaling), inline `* self._x_scale` в `_time_ticks`/`_rebuild_plane_nuclides`/`_add_peak_ridge`/`_axis_extent`/`_apply_plane`; камера кадрируется по `max(nt*x_scale, nc, 10)`. Y (энергия) не тронут. Тесты `tests/test_peaks_ui.py` (3 ассерта позиций хребтов пересчитаны под x_scale=25.6). Запрос оператора: «Спектрограмма странно ужимается. Время не читается. Исправь начальный формат (растянуть время под размер окна)». **Полный pytest — 763 passed.** Ждёт визуального подтверждения оператора.

`#UI-234` (2026-07-09, оператор) — **движок «Окно t»: 0 в центре шкалы, сжатие/растяжение оси времени, мельче деления**. Предыстория: после #UI-233 (авто-fit `x_scale=nc/nt` под квадрат) старая рукоятка «Окно t» (управляла ЧИСЛОМ временны́х бинов) стала визуально инертна — лист всегда nc-широкий независимо от числа бинов. Решение: перепрофилировать «Окно t» из счётчика бинов в **визуальный множитель масштаба оси t** поверх авто-fit. Эффективный `x_scale = base_fit(nc/nt) × _x_user`. `awf/ui/view3d.py`: новое поле `self._x_user=1.0`; в `set_spectrogram` — `self._x_scale = (nc/nt) * self._x_user`; новый метод `set_x_stretch(mult)` пишет только `_x_user` и ре-рендерит из той же sg (LOD/число бинов не трогаем, зум не сбрасываем — инвариант #92). `awf/ui/knobs.py`: `_tbin_mult(v)=2^(v/50)` (v=0→1.0 нейтраль, v=+100→4.0 растянуть, v=−100→0.25 сжать), формат `_tbin_fmt` («0» в центре, иначе «N.NN×»); спека `tbin` → диапазон −100..100 дефолт 0; `Knob`/`KnobRow`/`AdjustPanel` получили флаг `bipolar` — биполярный фейдер заливает зелёным ОТ ЦЕНТРА к каретке и рисует 9 мелких рисок с высокой центральной (0). `awf/ui/main_window.py`: импорт `_tbin_mult`, обработчик `tbin` зовёт `set_x_stretch(_tbin_mult(v))`; удалён мёртвый `_tbin_to_bins`. Тесты `tests/test_knobs_panel.py`: `_DEFAULTS['tbin']=0`; два теста переписаны на `_x_user` (дефолт 1.0, +100→4.0, −100→0.25, bypass→1.0, персист множителя через загрузку нового файла). `set_time_bins` в view3d оставлен dead-but-callable (churn-минимум). Запрос оператора: «Движок t сделай 0 в центре шкалы. нужно уметь сжимать шкалу и расширять. шаги (деления) сделай меньше для большей управляемости». **Полный pytest — 763 passed.** Ждёт визуального подтверждения оператора.

`#UI-235` (2026-07-09, оператор) — **нижний график: суммарный cps на оси Y (отдельная кривая)**. Запрос: «на оси Y сделай суммарый cps». Через AskUserQuestion оператор выбрал «Отдельная кривая (реком.)»: добавить ТРЕТЬЮ кривую «суммарный cps» = валовая скорость по ВСЕМ каналам (Σcounts/live_time по срезу), независимую от выбора ROI/энергоокна; магента «полоса ROI» и жёлтая «энергоокно» остаются. `awf/ui/panels.py`: новая зелёная (80,220,100) кривая `_total_curve` + кэш `_raw_total`; сидинг из полной полосы каналов `sg.band_time_series(0, sg.n_channels)` в `set_spectrogram` и `update_spectrogram` (всегда все каналы, независим от вида roi/slice/integral); helper `_set_total`; перерисовка из кэша в `set_unit_mode`; `_expand_series_y_if_needed` расширен — суммарный ≥ полосы ROI задаёт Y-верх (не клиппится при узком ROI); третья запись легенды + `_retranslate_legend`. `awf/ui/i18n.py`: ключ «суммарный cps»→"total cps". **Root-cause фикс перезагрузки:** на reload `set_spectrogram` ставил новый `self._live`, затем `_set_series`→`_expand_series_y_if_needed` читал СТАРЫЙ `self._raw_total` (массив прошлого файла) и делил на новый `_live` → `ValueError: operands could not be broadcast together with shapes (30,)(20,)`. Фикс: `self._raw_total = None` в начале `set_spectrogram` (сразу после `_live`), свежий `_set_total` заполняет после. Тест `test_total_cps_curve_independent_of_roi` (суммарный = Σ/live_time, не меняется с ROI). **Полный pytest — 764 passed.**

`#BUILD-1` (2026-07-09) — **exe раздулся до ~1 ГБ — исключить ML/OCR-пакеты из spec**. Root cause (систематическая отладка): окружение `py -3.14` между сборкой #232 (7 июля, чистый ~99 МБ zip) и релизом #REL-8 набрало torch (366 МБ), cv2 (112), pyarrow (80), transformers (42), onnxruntime (34), tokenizers, hf_xet, pdfminer, pypdfium2. Код `awf` их НЕ импортирует (grep: только ленивые `try: import sklearn…` в `awf/analysis/cluster.py`), но статический modulegraph PyInstaller следует за условными импортами внутри sklearn/scipy и тащит всё в дистрибутив → `dist/` раздулся 287→**1034 МБ**, zip **399 МБ**. Fix: добавлены в `Analysis(excludes)` в `waterfall-viewer.spec` (torch/torchvision/torchaudio/torchgen, sympy, networkx, cv2, onnx, onnxruntime, transformers, tokenizers, huggingface_hub, hf_xet, safetensors, accelerate, pyarrow, pdfminer, pypdfium2, cryptography, pydantic, numba, llvmlite, IPython, notebook). Безопасность проверена import-hook-тестом (заблокированы pyarrow/torch/cv2/onnxruntime/transformers/tokenizers → sklearn DBSCAN + PCA отработали). После фикса: dist **1034→287 МБ**, zip **399→120 МБ**, ML/OCR-каталоги в `_internal` отсутствуют. Коммит `207de70`.

`#REL-8` (2026-07-09) — **релиз v0.1.8** (`awf/__version__="0.1.8"`; коммиты `e2cdd20` (фичи #DATA-1/#UI-233/#UI-234/#UI-235) + `207de70` (#BUILD-1 — вычистка раздутого spec); PyInstaller onedir с чистым spec → `dist/waterfall-viewer-0.1.8-windows-x64.zip` ~120 МБ zip / 287 МБ распакованный; smoke-launch exe (8 с, не упал); `gh release create v0.1.8`). Собрал воедино четыре задачи с v0.1.7: #DATA-1 (CRC32-целостность .aswf v4), #UI-233 (растяжка оси времени x_scale=nc/nt), #UI-234 (биполярный движок «Окно t»), #UI-235 (зелёная кривая «суммарный cps»). **Полный pytest — 764 passed.**

`#ASWF4-1` (2026-07-10, оператор — «проверяй что viewer читает версию 4») — **сверка `aswf_loader.py` со спекой `ASWF_V4_FORMAT.md` (зона ESP32, read-only) + приведение baseline-ключа**. Полная статическая + динамическая проверка чтения v4: magic, `header_reserve` u32 @off4, JSON-шапка, baseline-секция (8192×u32=32768 Б → payload @36872), `row_stride`=16406 из шапки, n_rows из размера файла (`saved_rows`/`saved_at` в v4 всегда 0 — спека стр.60-64), 7 полей строки (spectrum/duration/timestamp/lat/lon/dose_rate/crc32), `dose_rate` dual-name v3(`dose_rate_usv_h`)/v4(`dose_rate`), timestamp→`time_offsets`, CRC32 (zlib, covers=16402), `seg_seq`/`total_at_open` в `integrity_report`. **Вердикт: функционального дефекта чтения v4 нет.** Единственная находка — `aswf_loader.py:90` читал baseline-счётчик через `bi.get("count")`, а спека (стр.92,116) даёт ключ `channels`; работало «случайно» через fallback `or n_channels` (baseline.channels ≡ каналам спектра). Fix: `bi.get("channels", bi.get("count")) or n_channels` — литеральное соответствие спеке + `count` как legacy для старых v3-тестов (`test_aswf_loader.py:261` пишет `{"count":4}`). 1 строка. **Полный pytest — 770 passed.**

`#ASWF4-2` (2026-07-10) — **постоянный v4-регресс-тест `tests/test_aswf_v4.py`** (в репо не было ни v4-теста, ни реального v4-файла — оба локальных `.aswf` version=1). Helper `_build_aswf_v4(tmp_path, n_rows, corrupt_row, channels)` строит spec-conformant файл (magic+reserve+JSON header version=4/row_stride=16406/row_fields incl crc32/baseline channels/seg_seq/total_at_open + 32768 baseline + строки с валидным zlib CRC32); 6 тестов — форма из размера файла (saved_rows=0 игнор), значения spectrum, baseline-секция, dose+time_offsets, integrity ok, integrity corrupt (порча строки → `status=corrupt`, `bad_rows=[1]`, load не падает). Генерация §16: спека `scripts/ollama/_spec_test_aswf_v4.md` → Ollama `qwen3-coder:30b` (`scripts/ollama/gen_test_aswf_v4.py`) → Claude-верификация (2 правки вывода: маркерный канал 100 в builder + `.tolist()` в asserts — `ndarray==list` кидал ambiguous-truth). **Полный pytest — 770 passed.** (+6 нетто-новых).

`#REL-9` (2026-07-10, оператор — «пересобери … по готовности релизь») — **релиз v0.1.9: пересборка exe строго с текущего HEAD**. Причина: коммит `663667d` (#ASWF4-1/#ASWF4-2) вышел ПОСЛЕ релиза v0.1.8 (тег `02cf505`), т.е. exe в релизе на 1 коммит отставал. Правка #ASWF4-1 функционально нейтральна для чтения v4 (fallback уже давал верный `baseline_count`), #ASWF4-2 — только тест; наблюдаемой разницы в exe нет, но для чистой провенанс-цепочки (exe собран с HEAD) сделан patch-bump. `awf/__version__` 0.1.8→0.1.9 (SSOT, `pyproject.toml` тянет динамически). PyInstaller onedir по неизменному `waterfall-viewer.spec` (excludes #BUILD-1) → `dist/waterfall-viewer/waterfall-viewer.exe` **22.3 МБ**, dist **290 МБ**, ML/OCR-каталоги в `_internal` отсутствуют. Smoke-launch exe (8 с, WS 240 МБ, не упал). Архив `dist/waterfall-viewer-0.1.9-windows-x64.zip` **120.1 МБ** (Compress-Archive из `dist/waterfall-viewer/*`). Коммит — конкретные пути: `awf/__init__.py` + `docs/dev/CLAUDE_NOTES.md`; тег `v0.1.9`; `gh release create v0.1.9`. **Полный pytest — 770 passed** (только строка версии + доки после прогона, логика не менялась). **Верификация 2026-07-10** (оператор — «фиксируй состояние»): `gh release list` → v0.1.9 **Latest**, ассет `waterfall-viewer-0.1.9-windows-x64.zip` на месте; `git describe`=`v0.1.9`; HEAD `28d2cb1`; дерево чистое. Гигиена: `dist_v017/` (старый билд v0.1.7, 342 МБ) и `scripts/ollama/gen_test_aswf_v4.py` (одноразовый Ollama-генератор) внесены в `.gitignore` — untracked более не болтаются в `git status`.

`#DATA-2` (2026-07-11, оператор — «научи ватерфалвьер читать новые форматы») — **чтение ASWF v5** (спека `ASWF_FORMAT.md` прошивки, firmware-v1.0.11 #FW-41): в v5 перед `crc32` добавлено поле `temperature` (float32 °C, offset 16402), `crc32` сдвинут на 16406 (`covers=16406`), `row_stride=16410`. Лоадер был generic (stride/offsets из `row_fields`) — v5 читался, но температура терялась. Добавлено: `Spectrogram.temperature_c` (float64, NaN=нет; + прокидка в `trimmed_channels`), извлечение в обоих ветках лоадера (raw + RLE), комментарий CRC «только v4» исправлен на v4/v5. RLE-сжатие v3+ и dual-name `dose_rate`/`dose_rate_usv_h` уже поддерживались. Регресс-тест `tests/test_aswf_v5.py` (§16: спека `scripts/ollama/_spec_test_aswf_v5.md` → qwen3-coder → 1 правка: галлюцинация API `sg, report = load_aswf()` — лоадер возвращает только Spectrogram): 7 тестов — shape/values/temperature/temp-NaN/dose+time/integrity ok/corrupt. **Полный pytest — 777 passed.**

`#DATA-3` (2026-07-11, оператор — «создай инструменты анализа других данных из файла») — **вкладка «Прибор»** (`awf/ui/device_data_panel.py`, §16-генерация + 2 фикса: индексация `normed` полным индексом при частичном GPS → IndexError, `setPoints`→`setData` в pyqtgraph ScatterPlotItem): три графика — мощность дозы (мкЗв/ч), температура детектора °C (v5), GPS-трек (lat/lon, точки окрашены по cps синий→красный) + **экспорт CSV** (`build_device_csv()` — чистая функция: index,t_offset_s,duration_s,cps,dose_rate_usv_h,temperature_c,latitude,longitude; NaN/None → пустая ячейка). Статус-строка «Доза: ✓ · Температура: ✓ · GPS: —». Вшито: main_window (таб «Прибор» + set_spectrogram + retranslate-список), i18n RU→EN. Тесты `tests/test_device_data_panel.py` — 7 шт (CSV header/values/empty-cells + smoke full/none/gps-partial/no-gps).

`#UI-236` (2026-07-11, оператор — «при открывании файла выводи отчет о целостности с возможностью сохранения») — **диалог отчёта целостности** (`awf/ui/integrity_dialog.py`, §16-генерация + 2 фикса: кнопка `self._save` затирала одноимённый метод → connect на не-callable, enum шрифта приведён к scoped `QFontDatabase.SystemFont.FixedFont`): `build_integrity_text()` — файл/срезы/каналы/сумма отсчётов/t0/длительность, блок CRC32 (ok / ⚠ ПОВРЕЖДЕНО+номера строк / пропущен-сжатый / «недоступен (формат без CRC32)» для v1–v3/rcspg/n42), версия ASWF + seg_seq/total_at_open, наличие дозы/температуры/GPS/baseline, метка формирования UTC. Диалог **немодальный** (`show()` + WA_DeleteOnClose — `exec()` завесил бы offscreen-тесты и блокировал UI), показывается при каждом открытии файла из `_on_loaded`; кнопка «Сохранить отчёт…» → .txt UTF-8; пункт меню «Файл → Отчёт целостности…» для повторного показа. Прежний warning-мессаджбокс #DATA-1 удалён (corrupt виден в отчёте), суффикс статус-бара сохранён. Тесты `tests/test_integrity_dialog.py` — 5 шт. **Полный pytest — 789 passed.** E2E-смоук offscreen: MainWindow + синтетический v5 → диалог виден, вкладки `['3D Waterfall', '2D Карта (Время×Энергия)', 'Аналитика', 'Прибор']`, статус «Доза: ✓ · Температура: ✓ · GPS: —», не упал.

`#UI-237/#UI-238/#UI-239` (2026-07-11, оператор по скриншоту вкладки «Прибор» на реальном файле) — **доводка вкладки «Прибор» + оверлей температуры на 2D**. #UI-237: ось времени графиков дозы/температуры фиксируется [0..t_max] (`setLimits`+`setXRange padding=0`) — раньше пустой график дозы рисовал autorange-мусор 0.1..0.9. #UI-238: `DeviceDataPanel.set_time_unit('с'|'мин'|'ч')` — подключена к существующей тулбар-кнопке «Время:» (`_on_time_unit_changed`, как 3D/heatmap #64/#207); перерисовка через повторный `set_spectrogram` (ряды короткие). #UI-239: отключаемый оверлей температуры на 2D-карте — чекбокс «Темп. (2D)» в тулбаре; дизайн: вертикальная кривая вдоль оси времени (Y=срез/t_scale), T нормируется в полосу [5%..95%] ширины карты, белая линия width=2 с чёрной обводкой shadowPen width=4 (видима на любой палитре), NaN рвёт линию (`connect='finite'`), шкала °C — верхняя ось `_TempAxisItem` (обратное отображение display-X→градусы, паттерн `_TimeAxisItem` #207), появляется только при включённом оверлее с данными. §16-дисциплина: guard дважды резал правки ≥25 строк — методы разбиты (`_apply_temp_overlay` гейт + `_draw_temp_overlay` отрисовка), тесты дописаны по одному. Тесты +4 (ось от нуля, юнит-скейл, toggle оверлея, no-data гейт). **Полный pytest — 793 passed.** E2E-смоук offscreen: оверлей 6/6 точек, часы 0.08333, ось дозы [0, 0.08333].

`#DATA-4` (2026-07-11, оператор — «перечитай спецификацию формата v5 и примени») — **аудит лоадера против спеки `ASWF_FORMAT.md`** (спека в репо не менялась — diff со скачанной копией пустой; полная построчная сверка). Соответствуют: hlen фактический, stride/offsets только из `row_fields`, dual-key baseline `channels`/`count`, dual-name `dose_rate`/`dose_rate_usv_h`, temperature v5, `crc32.covers` из дескриптора, RLE (наш декодер строже спеки: raise на 0xFFFF; спековский пример на нём зациклился бы), `duration=0→interval_sec` (cps по `live_time_s`), `saved_rows=0`, усечённый хвост, `seg_seq`/`total_at_open`. Найдено и исправлено 3 расхождения: **F1** `started_at==0` («NTP не синхронизирован») давал `t0_iso="1970-01-01…"` вместо `None` — фиктивная дата в UI; **F2** baseline без ключей `channels`/`count` подставлял `n_channels` (32 КБ мусора + сдвиг payload) — по спеке `B=(channels??count??0)×4` → секции нет; **F3** `dtype`/`byte_order` шапки не проверялись — чужое значение молча дало бы кашу, теперь честный `ValueError`. Тесты +3 в `tests/test_aswf_loader.py` (t0-zero, bad-dtype, baseline-no-keys). **Полный pytest — 796 passed.**

## Ориентация по проекту

Десктоп-вьюер waterfall-спектрограмм гамма-спектрометра. Оси спектрограммы: **время × энергия**
(пространственной/GPS-оси в данных нет). Форматы: `.aswf`, `.rcspg`, `.n42`.

- **Стек:** Python 3.14, PySide6 (Qt), pyqtgraph 0.14.0, PyOpenGL (3D-waterfall), numpy, lxml.
- **Модель данных:** `awf/model/spectrogram.py` — `Spectrogram` (counts `[n_slices×n_channels]`,
  `time_offsets_s`/`real_time_s`/`live_time_s`, методы спектров/рядов/`downsample`/`counts_in_unit`)
  и `Calibration` (полином канал→энергия).
- **UI:** `awf/ui/` — `main_window.py` (окно, тулбар, доки, оркестрация сигналов), `view3d.py`
  (`Waterfall3DView`, GL-поверхность + секущие плоскости), `panels.py` (`HeatmapPanel` 2D-карта,
  `SlicePanel` спектр + временные ряды, `SectionControls`, `NuclidePanel`), `analytics_panel.py`,
  `style.py` (тёмная QSS-тема).
- **Цвета осей/плоскостей 3D:** время = бирюза `(51,217,242)`, энергия = пурпур `(242,89,217)`,
  счёт = жёлтый `(242,217,64)`. UI-элементы, привязанные к оси/плоскости, используют тот же цвет.

## Команды

```bash
# тесты (headless Qt обязателен)
QT_QPA_PLATFORM=offscreen PYTHONIOENCODING=utf-8 python -m pytest -q
# запуск приложения
python -m awf            # или run.bat
```

- **Тесты Qt — только offscreen:** `QT_QPA_PLATFORM=offscreen`, вывод — `PYTHONIOENCODING=utf-8`.
- Новый функционал сопровождать тестом в `tests/`; держать прогон зелёным перед коммитом.
- Коммиты на русском; в репо — конкретные пути в `git add` (не `-A`/`.`); пуш в публичный
  репозиторий — только по явной команде оператора.