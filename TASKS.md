# Реестр задач внедрения — детальная спецификация

Подробный нумерованный backlog по промпту оператора (ТЗ 2026-06-25). **Последовательность не
фиксирована** — порядок решаем отдельно. Стратегия/фазы — в [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md).

Каждая задача расписана по единому шаблону:
- **Цель** — что получает пользователь.
- **Подзадачи** — конкретные шаги реализации (адресуемые: `N.1`, `N.2`…).
- **API / сигнатуры** — предлагаемые функции/классы (помечено «предлагается»; источник-сигнатуры из
  SpectraVibe помечен «источник»).
- **Параметры / формулы** — точные значения, с провенансом `file:line` где портируем.
- **UI** — конкретные элементы интерфейса.
- **Тесты** — проверяемые критерии готовности.
- **Зависимости** — пакеты и задачи-предпосылки.
- **Объём** — S (≤полдня) / M (1–2 дня) / L (3+ дня), грубо.

Сложность данных: 🟢 чистый numpy/stdlib · 🟡 нужен scipy/sklearn · 🔵 только UI · 🟣 требует рефактора
модели. SpectraVibe (`gamma-spectrum-analysis`) — **референс на чтение**, пути `scripts/gamma/...`.

Сквозной инвариант реализации: код ≥25 строк → спецификация `scripts/ollama/_spec_<name>.md` →
кодоген Ollama `qwen3-coder:30b` → валидация (`py_compile`+pytest). Численное ядро — без Qt, в
`awf/analysis/` или `awf/model/`. Калибровка — ascending-полином (совместима с SpectraVibe).

---

# Группа I. Фундамент

## Задача 1 — Каркас пакета анализа `awf/analysis/` 🟢 · Объём S
**Цель:** единая Qt-free площадка для всех численных методов, тестируемая без графики.

**Подзадачи:**
- 1.1. Создать `awf/analysis/__init__.py` с реэкспортом публичных функций по мере роста.
- 1.2. Определить общие типы результатов (dataclass): `FoundPeak`, `PeakArea`, `MdaResult`,
  `LineMatch`, `IdentResult` — чтобы методы возвращали структуры, а не кортежи.
- 1.3. Договориться о контракте входов: спектр 1D = `np.ndarray[float]`, ось энергий из
  `Calibration.energies(n)`, `fwhm_channels: float | Callable[[float], float]`.
- 1.4. Добавить хелпер `spectrum_from_selection(sg, t_lo, t_hi)` поверх `Spectrogram.sum_spectrum`.

**API (предлагается):**
```python
# awf/analysis/types.py
@dataclass(frozen=True)
class FoundPeak:
    channel: float; energy: float; height: float
    fwhm_channels: float; significance: float; area_estimate: float
```
**Тесты:** `tests/test_analysis_pkg.py` — импорт пакета, конструирование типов, хелпер выдаёт
спектр, совпадающий с `sum_spectrum`.
**Зависимости:** numpy. **Предпосылки:** —

---

# Группа II. Фон и пики (ТЗ-A.7, A.8, A.9)

## Задача 2 — Поиск пиков (Mariscotti + Currie) 🟢 · Объём M · *ТЗ-A.9*
**Цель:** автоматически находить фотопики в спектре среза/окна и в суммарном спектре.

**Подзадачи:**
- 2.1. Порт фильтра второй производной Гауссиана (Mariscotti) — свёртка спектра с ядром −G''.
- 2.2. Критерий значимости Currie L_C: пик принимается при `significance ≥ sigma_threshold`.
- 2.3. Banded segmentation: разбить спектр на полосы равного FWHM (max_ratio≈1.2), фильтр per-band.
- 2.4. Спайк-фильтр: отсев пиков с FWHM < 0.3×модели (узкие выбросы).
- 2.5. Предварительная площадь `area ≈ 2.507·σ·height`.
- 2.6. Интеграция: вызвать на `sum_spectrum`/`total_spectrum`, вернуть `list[FoundPeak]`.

**API (предлагается):**
```python
def find_peaks(counts_1d, fwhm_channels, *, sigma_threshold=3.0,
               spike_min_fwhm_frac=0.3) -> list[FoundPeak]
```
**Источник:** `peaks/search.py` `mariscotti_search()` (стр. 261–450); спайк-фильтр (413–431);
площадь (432); альт-метод `peaks/convolution_search.py` (matched-filter, для перекрёстной проверки).
**Параметры:** `sigma_threshold=3.0` (Currie 3σ), `max_ratio=1.2`, `spike_min_fwhm_frac=0.3`.
**UI:** маркеры пиков (`InfiniteLine`) на графике спектра в `SlicePanel`; чекбокс «Показать пики».
**Тесты:** синтетика 3 гауссианы (разной высоты) на наклонном фоне → находит ровно 3, центры ±0.5
канала, `significance` монотонна по высоте; узкий δ-выброс отсекается спайк-фильтром.
**Зависимости:** numpy. **Предпосылки:** 1; 8 (FWHM-модель; на старте — константный FWHM из заголовка).

## Задача 3 — Континуум/фон под пиком 🟢 · Объём M
**Цель:** оценить baseline под ROI пика для расчёта нетто.

**Подзадачи:**
- 3.1. Cowell: полином 1-го порядка по крыльям ROI (`numpy.polyfit`), интеграл baseline по full ROI.
- 3.2. Сглаженная ступень (Будыка 7.29): `B(E)=bg_r+(bg_l−bg_r)·0.5·erfc((E−E₀)/(σ√2))+slope·(E−E₀)`,
  `erfc` через `math.erfc` (без scipy).
- 3.3. Асимметричные окна фона: автосужение крыла при интерферирующем соседнем пике.
- 3.4. Выбор модели — параметр `method ∈ {'cowell','step'}`.

**API (предлагается):**
```python
def estimate_continuum(counts_1d, roi_lo, roi_hi, *, method='cowell',
                       wing=None) -> np.ndarray   # baseline по каналам ROI
```
**Источник:** `peaks/area.py:77–238` `cowell_area()`; `peaks/background_options.py:40–89`
`smoothed_step_bg()`; асимметрия `background_options.py:107–169`.
**Тесты:** на чисто-линейном фоне Cowell даёт нетто≈0; площадь известной гауссианы поверх фона в
пределах 3%; `step` непрерывна и монотонна между уровнями.
**Зависимости:** numpy, `math.erfc`. **Предпосылки:** 1.

## Задача 4 — Фоновая зона по времени + аппроксимация на всю шкалу 🟢 · Объём M · *ТЗ-A.7*
**Цель:** пользователь выделяет временной интервал как «фон», строится опорный спектр на всю запись.

**Подзадачи:**
- 4.1. `Spectrogram.background_spectrum(t_lo, t_hi)` — усреднённый спектр окна, нормировка на
  суммарный live-time окна (отсчёты/с по каналам).
- 4.2. Возврат опорного спектра + метаданных (использованный интервал, суммарный live-time).
- 4.3. UI-режим выбора фон-ROI **по оси времени** (вертикальная полоса на 2D-карте/временном ряду).

**API (предлагается):**
```python
def background_spectrum(self, t_lo: int, t_hi: int) -> np.ndarray  # rate[channel], отсчёты/с
```
**Параметры:** нормировка `sum(counts[t_lo:t_hi]) / sum(live_time_s[t_lo:t_hi])`.
**UI:** кнопка-режим «Выбрать фон по времени» → выделение интервала; индикатор выбранного окна.
**Тесты:** опорный спектр совпадает с ручным усреднением окна; корректная нормировка на live-time;
граничные индексы.
**Зависимости:** numpy. **Предпосылки:** 1.

## Задача 5 — Вычитание фонового массива 🟢 · Объём S · *ТЗ-A.8*
**Цель:** получить нетто-водопад (вычесть опорный фон из каждого среза).

**Подзадачи:**
- 5.1. `Spectrogram.subtract_background(bg_rate, scale='live_time')` → новый `Spectrogram`:
  `counts_net[t] = clip(counts[t] − bg_rate · live_time_s[t], 0, None)`.
- 5.2. Переключатель «Вычесть фон» в тулбаре; хранить и оригинал, и нетто (быстрое переключение).
- 5.3. Отражение нетто в 2D-карте и `SlicePanel`.

**API (предлагается):**
```python
def subtract_background(self, bg_rate: np.ndarray, *, scale='live_time') -> 'Spectrogram'
```
**Тесты:** вычитание идентичного фона → нули; нет ухода в минус (clip≥0); масштаб по live-time каждого
среза верен; форма/калибровка/оси сохранены.
**Зависимости:** numpy. **Предпосылки:** 4.

## Задача 6 — Площадь пика и нетто-счёт 🟢 · Объём M
**Цель:** интегральная площадь/нетто пика с погрешностью (основа активности и MDA).

**Подзадачи:**
- 6.1. `cowell_area`: `S = N_gross − B`, `dS = √(N_gross + B)`.
- 6.2. (опц.) гаусс-фит площади — позже через scipy (Задача 23); сейчас numpy-Cowell.
- 6.3. Связать с найденными пиками: ROI = `peak.channel ± k·FWHM`.

**API (предлагается):**
```python
def peak_area(counts_1d, peak_channel, fwhm_channels, *, k_roi=2.5,
              continuum='cowell') -> PeakArea   # net, d_net, gross, baseline
```
**Источник:** `peaks/area.py` `cowell_area()`.
**Тесты:** площадь синтетической линии в пределах допуска; погрешность ~√(gross+B); ROI-границы.
**Зависимости:** numpy. **Предпосылки:** 2, 3.

## Задача 7 — MDA по ISO 11929 🟢 · Объём S
**Цель:** порог принятия решения, предел обнаружения и минимально детектируемая активность.

**Подзадачи:**
- 7.1. `L_C = k_α·√(2·n_bg)` (порог решения, counts над фоном).
- 7.2. `L_D ≈ k_α² + 2·k_α·√(n_bg + L_C²/4)` (предел обнаружения).
- 7.3. `A_MDA = L_D / (ε·I·t)` (в Бк), `k_α = 1.645` (α=5%, односторонний).

**API (предлагается):**
```python
def mda_for_peak(n_bg, live_time_s, efficiency, intensity_frac,
                 *, k_alpha=1.645) -> MdaResult   # L_C, L_D, A_MDA
```
**Источник:** `identification/mda.py:101–150` `mda_for_peak()`; константы (66–69).
**Тесты:** L_C/L_D/A_MDA совпадают с ручным расчётом ISO 11929 на заданных n_bg/ε/I/t; деление на 0
обработано.
**Зависимости:** math, numpy. **Предпосылки:** 6.  *(ε эффективности — пользовательский ввод/калибровка)*

## Задача 8 — Модель разрешения FWHM(E) 🟢 · Объём M
**Цель:** FWHM(E) детектора для поиска пиков, окна идентификации, деконволюции.

**Подзадачи:**
- 8.1. Сцинтилляционная модель `FWHM(E) = k·√(E + α·E²)` (AtomSpectra — сцинтиллятор), floor 0.01 keV².
- 8.2. Фит по реперным пикам (k, α) методом наименьших квадратов.
- 8.3. Bootstrap-провайдер: если репера нет — оценить FWHM по значимым пикам самого спектра.
- 8.4. Перевод FWHM(E)→FWHM(каналы) через калибровку для входа в поиск пиков.

**API (предлагается):**
```python
def fit_fwhm_scintillator(energies, fwhms_keV) -> FwhmModel   # .fwhm_at(E)->keV
def fwhm_provider(calibration, *, peaks=None) -> Callable[[int], float]  # каналы
```
**Источник:** `calibration/fwhm_fit.py` (`fit_fwhm_scintillator`, floor BUG-37);
`calibration/fwhm_provider.py` (иерархия + F-449 bootstrap).
**Тесты:** фит даёт монотонную FWHM(E); floor не даёт отрицательного; bootstrap без репера работает.
**Зависимости:** numpy. **Предпосылки:** 1 (и 2 для bootstrap).

---

# Группа III. Библиотека МАГАТЭ и идентификация (ТЗ-A.10)

## Задача 9 — IAEA-fetcher (ENSDF CSV + кэш) 🟢 · Объём M · *ТЗ-A.10*
**Цель:** загрузка гамма-линий нуклида из IAEA LiveChart с офлайн-кэшем и воспроизводимостью.

**Подзадачи:**
- 9.1. Порт `fetch_iaea_gamma_lines()` 1-в-1: URL-билдер, HTTP-запрос с UA, парсинг CSV (по именам
  колонок — они меняются между ревизиями), таймаут 30 с.
- 9.2. Атомарный кэш (`os.replace` через tmp-файл) в `awf/data/iaea_cache/`; офлайн-чтение
  `load_iaea_gamma_lines_from_cache()`.
- 9.3. `_normalize_nuclide_name()` с fail-loud (SEC-02) — защита от URL-инъекции/path-traversal.
- 9.4. `merge_iaea_into_internal()` → `{"lines":[[E,I,dI],…]}` (формат `nuclides.json`).
- 9.5. Сетевой вызов — **в QThread, по кнопке**, не в основном потоке UI.

**API (источник, переносим как есть):**
```python
def fetch_iaea_gamma_lines(nuclide, *, cache_dir, rad_type='g',
                           force_refresh=False, timeout_seconds=30.0) -> list[IaeaGammaLine]
```
**Источник:** `data/iaea_fetcher.py` целиком; URL
`https://www-nds.iaea.org/relnsd/v1/data?fields=decay_rads&nuclides=<n>&rad_types=g`;
`merge_iaea_into_internal` (304); `_normalize_nuclide_name` (73).
**Тесты:** парсинг синтетического ENSDF-CSV; нормализация `Th-234`/`234th`/`234TH`→`234th`; fail-loud
на `?evil=`/`../`; атомарная запись; чтение из кэша. **Без реальной сети** (тест парсера+кэша).
**Зависимости:** stdlib `urllib`, `csv`. **Предпосылки:** —
**⚠ Опсек/§7:** жёсткий timeout, фон; IAEA отдаёт 403 на дефолтный UA (UA уже в порте). Кэш — в
`.gitignore`.

## Задача 10 — Категоризация нуклидов 🟢 · Объём M · *ТЗ-A.10*
**Цель:** разметка нуклидов: **природные / техногенные / медицинские / осколки деления** ×
**коротко-/долгоживущие**.

**Подзадачи:**
- 10.1. Добавить поля `category ∈ {natural, technogenic, medical, fission}` и
  `lifetime ∈ {short, long}` в `nuclides.json`.
- 10.2. Порог времени жизни: `long` если T½ ≥ 60 сут, иначе `short` (порог вынести в константу).
- 10.3. Перебукетизировать пресеты SpectraVibe в 4 категории (таблица соответствия).
- 10.4. Расширить dataclass `Nuclide` полями `category`, `lifetime` (<25 строк — напрямую).
- 10.5. Хелперы выборки `nuclides_by_category(cat)`, `nuclides_by_lifetime(lt)`.

**Таксономия-источник:** `data/targeted_libraries.py` — `ENVIRONMENTAL`→природные(NORM/космогенные)+
техногенный fallout; `NPP` fission products (I-131/132/133, Cs-134/137, Ru-103/106, Ba-140/La-140,
Zr-95/Nb-95…)→осколки деления; актиниды/активация (Co-60, Mn-54, актиниды)→техногенные;
I-125/I-131/Tc-99m/Ga-67/Tl-201→медицинские.
**Тесты:** каждая запись имеет валидную пару category/lifetime; фильтры дают ожидаемые подмножества;
граница порога T½.
**Зависимости:** —. **Предпосылки:** —

## Задача 11 — Матчинг линий + идентификация 🟢 · Объём L · *ТЗ-A.10*
**Цель:** сопоставить найденные пики с библиотекой и ранжировать нуклиды-кандидаты.

**Подзадачи:**
- 11.1. `lookup_by_energy(E, tol)` — линии в окне ±tol (tol ∝ FWHM(E)), сортировка по |Δ|.
- 11.2. Detection (Lsrm §6, шаг 1): для кандидата найти characteristic line (мин. MDA), искать пик в её окне.
- 11.3. Line-matching (шаг 2): для каждой линии кандидата искать пик; собрать совпадения.
- 11.4. Пропорциональность: соотношение площадей пиков vs библиотечных интенсивностей,
  `ratio_tolerance≈3.0`; приоритеты редких изотопов.
- 11.5. Confidence Index + ранжирование кандидатов.

**API (предлагается):**
```python
def identify_peaks(found_peaks, library, fwhm_model, *,
                   tol_factor=1.0, ratio_tolerance=3.0) -> list[IdentResult]
```
**Источник:** `data/nuclide_library.py:199` `lookup_by_energy`; `identification/identify.py` (3-шаг);
`identification/proportionality.py` (ratio_tolerance, rare-isotope priors).
**UI:** под выбранным пиком — список кандидатов с Δ(keV), интенсивностью, индексом уверенности.
**Тесты:** на синтетике (Cs-137/K-40/Co-60) правильный кандидат — первый; ложное одиночное совпадение
отбраковано пропорциональностью.
**Зависимости:** numpy + библиотека. **Предпосылки:** 2, 6, 8, 10.

## Задача 12 — UI: расширенная панель нуклидов 🔵 · Объём M · *ТЗ-A.10*
**Цель:** фильтры по категориям/времени жизни, подсказки под пики, добавление из IAEA.

**Подзадачи:**
- 12.1. Группировка дерева нуклидов по `category` с подгруппами `short/long`.
- 12.2. Чекбоксы-фильтры: природные/техногенные/медицинские/осколки × коротко-/долгоживущие.
- 12.3. Панель «Кандидаты» под выбранным найденным пиком (из Задачи 11).
- 12.4. Кнопка «Добавить нуклид из IAEA» → диалог имени → fetch (QThread) → запись в библиотеку.

**Где:** `awf/ui/nuclide_panel.py`, `panels.py` (маркеры), `main_window.py`.
**Тесты:** smoke — фильтры переключают видимые нуклиды; добавление из IAEA дописывает запись;
маркеры обновляются.
**Зависимости:** PySide6. **Предпосылки:** 9, 10, 11.

---

# Группа IV. Визуализация в стиле iZotope Insight (ТЗ-A.1…A.6)

## Задача 13 — Секущие плоскости: 2 на каждую ось 🔵 · Объём L · *ТЗ-A.1*
**Цель:** по 2 подвижные секущие плоскости на оси Время / Энергия / Отсчёты с профилем сечения.

**Подзадачи:**
- 13.1. Полупрозрачный квад-`GLMeshItem` для каждой плоскости; цвет/прозрачность из палитры.
- 13.2. Слайдеры позиции (2×3 = 6 контролов) в доке «Сечения».
- 13.3. Профиль пересечения: строка/столбец `counts` на позиции плоскости → `GLLinePlotItem`
  поверх поверхности (или вывод в `SlicePanel`).
- 13.4. Учесть LOD-прорежку: позиция плоскости в реальных единицах → индекс с учётом downsample.

**Где:** `awf/ui/view3d.py`.
**Тесты:** smoke — плоскости создаются, двигаются слайдерами; профиль соответствует данным среза на позиции.
**Зависимости:** pyqtgraph/OpenGL. **Предпосылки:** —

## Задача 14 — Градуировка каждой оси 🔵 · Объём M · *ТЗ-A.2*
**Цель:** подписи и тики осей в реальных единицах.

**Подзадачи:**
- 14.1. Энергия (keV) из калибровки; время (с/мин) из `time_offsets_s`; отсчёты по высоте.
- 14.2. `GLTextItem` для подписей (у `GLAxisItem` подписей нет; нужен pyqtgraph ≥0.13 — проверить версию).
- 14.3. Адаптивный шаг тиков; корректность при LOD (подписи в исходных единицах, не в индексах прорежки).

**Где:** `awf/ui/view3d.py`.
**Тесты:** smoke — тики/подписи в keV/с/отсчётах, не в индексах; не падает в headless GL-инициализации.
**Зависимости:** pyqtgraph ≥0.13/OpenGL. **Предпосылки:** —

## Задача 15 — Вертикальные энергетические лучи до линии спектра 🔵 · Объём M · *ТЗ-A.3*
**Цель:** вертикальные лучи от базовой плоскости до поверхности на выбранных энергиях/линиях нуклидов.

**Подзадачи:**
- 15.1. `GLLinePlotItem` — луч на энергии E: от (E, t, 0) до (E, t, surface(E,t)).
- 15.2. Источники энергий: ручной ввод, маркеры нуклидов (Задача 12), найденные пики (Задача 2).
- 15.3. Стиль как в референсе Insight (тонкие яркие линии).

**Где:** `awf/ui/view3d.py`.
**Тесты:** smoke — лучи на заданных энергиях, высота = значение поверхности.
**Зависимости:** pyqtgraph/OpenGL. **Предпосылки:** — (синергия с 2, 11, 12).

## Задача 16 — Регулировка интенсивности / цвета / контраста 🟢🔵 · Объём M · *ТЗ-A.4*
**Цель:** живая настройка контраста: усиление, гамма, отсечка выбросов.

**Подзадачи:**
- 16.1. Расширить `apply_z_scale`: `gain` (множитель), `gamma` (степень), `clip` (нижний/верхний перцентиль).
- 16.2. Применять до нормировки colormap; общий код для 2D и 3D.
- 16.3. Слайдеры в тулбаре «Вид», debounce-апдейт.

**API (предлагается):**
```python
def apply_z_scale(arr, mode, *, gain=1.0, gamma=1.0, clip=(0.0, 100.0)) -> np.ndarray
```
**Где:** `awf/ui/zscale.py` (+ тулбар в `main_window.py`).
**Тесты:** unit — gain/gamma/clip дают ожидаемые преобразования; clip по перцентилям убирает выбросы.
**Зависимости:** numpy/PySide6. **Предпосылки:** —

## Задача 17 — Цветовая гамма iZotope Insight 🔵 · Объём S · *ТЗ-A.5*
**Цель:** палитра оранжевый-на-чёрном + синяя база.

**Подзадачи:**
- 17.1. Кастомный `pg.ColorMap` (стопы: тёмно-синий → чёрный → оранжевый → жёлто-белый).
- 17.2. Применить в `view3d.py` (вместо `inferno`) и `HeatmapPanel`; фон 3D уже `(15,15,20)`.
- 17.3. Переключатель палитры (Insight / inferno / viridis).

**Где:** `awf/ui/view3d.py`, `panels.py`.
**Тесты:** smoke — палитра применяется к 2D и 3D; переключатель работает.
**Зависимости:** pyqtgraph. **Предпосылки:** —

## Задача 18 — Обесцвечивание массива + подсветка выбранных пиков 🔵 · Объём M · *ТЗ-A.6*
**Цель:** база приглушена (десатурация), выбранные/найденные пики — насыщенным цветом.

**Подзадачи:**
- 18.1. Десатурация базовой поверхности/карты (понижение saturation/alpha).
- 18.2. Оверлей выделенных каналов: 3D — `GLScatterPlotItem`/вторая поверхность; 2D — подсветка столбцов.
- 18.3. Список подсвеченных пиков управляется из UI (из Задачи 2/12).

**Где:** `zscale.py` (десатурация) + `view3d.py`/`panels.py` (оверлей).
**Тесты:** smoke — массив тускнеет, подсвеченные пики выделяются; список управляем.
**Зависимости:** numpy/pyqtgraph. **Предпосылки:** 2, 17.

---

## Замечания оператора по ревью Группы IV (против референса iZotope Insight + скрины) ✅ ВЫПОЛНЕНО

> Ревью 2026-06-25: оператор сверил готовую Группу IV с референсом iZotope Insight и скриншотами,
> выдал 5 замечаний. Присвоены номера IV-R1…IV-R5, учтены в плане, исправлены, покрыты тестами
> (`tests/test_group4_remarks.py`, 21 тест). Полный прогон — 159 passed.

### IV-R1 — Серая градиентная схема оформления окна ✅
**Замечание:** оформление окна не соответствует ТЗ — должна быть серая градиентная (металлик-хром,
как в iZotope Insight), а не дефолтная тема Qt.
**Решено:** новый модуль `awf/ui/style.py` (`APP_QSS`) — QSS с `qlineargradient`, тёмно-серая гамма
(#2b2d31 / #34373c / #45484d) + зелёный акцент (#4a7d4a). Scope: QMainWindow/QToolBar/QMenuBar/
QMenu/QDockWidget/QStatusBar/QLabel/QCheckBox/QComboBox/QSlider/QPushButton/QSpinBox/QTabBar/
QTreeView/QHeaderView/QScrollBar. Применён в `main_window.py` через `setStyleSheet(APP_QSS)`.
Намеренно НЕ трогает plot-виджеты pyqtgraph и per-item цвета дерева нуклидов.
**Тесты:** `APP_QSS` непустой; MainWindow применяет стиль.

### IV-R2 — Шкала энергий в конец по времени + вертикальные отрезки-зубцы ✅
**Замечание:** перенести отображение шкалы энергий в дальний по времени край; добавить вертикальные
отрезки шкалы энергий (зубцы), как на скрине Insight.
**Решено:** в `view3d._rebuild_axis_labels` подписи энергий вынесены на дальнюю по времени грань
(`xmax+pad`), к каждому делению добавлен вертикальный отрезок-зубец `GLLinePlotItem` высотой
`0.10*zmax` (цвет `_ENERGY_TICK_RGBA`). Зубцы хранятся в `_axis_items` и снимаются вместе с
подписями при toggle.
**Тесты:** зубцы присутствуют после загрузки; снимаются при скрытии подписей.

### IV-R3 — При включении сечений показывать только то, что между плоскостями ✅
**Замечание:** при включении секущих плоскостей отображать ТОЛЬКО объём между ними; всё, что снаружи
— не показывать.
**Решено:** клиппинг по паре плоскостей на оси (ось клиппирует, только когда обе её плоскости видимы).
Время/энергия → оконный срез индексов (i0..i1, j0..j1) с пересборкой поверхности; счёт → полоса высот
(z_lo/z_hi) с alpha-маской вне диапазона + `translucent`. `_clip_windows()` / `_rebuild_surface()` /
`_maybe_reclip()` (пересборка только при смене сигнатуры клиппинга).
**Тесты:** одна плоскость не клиппирует; две — сужают; пересборка поверхности; клип по высоте счёта;
выключение восстанавливает полный объём.

### IV-R4 — Регулируемая функция усреднения спектра ✅
**Замечание:** добавить регулируемую функцию усреднения (сглаживания) данных спектра.
**Решено:** `awf/ui/zscale.py::smooth_counts(arr, radius, axis)` — скользящее среднее (box-фильтр
ширины 2r+1, cumsum, edge-padding; radius<=0 — без изменений). Применяется в `view3d` (axis=1 по
каналам), `HeatmapPanel` (axis=1), `SlicePanel` (axis=-1 по кэшированному сырому спектру). Слайдер
0–15 в тулбаре; общий обработчик `_on_smooth_changed` рассылает радиус во все три вида.
**Тесты:** `smooth_counts` (тождество при r=0, сглаживание при r>0, сохранение формы); сеттеры
во всех трёх видах.

### IV-R5 — Не отображать последний канал (мусор) ✅
**Замечание:** последний канал АЦП содержит мусор (переполнение) — не отображать.
**Решено:** `Spectrogram.trimmed_channels(drop_last=1)` — новая спектрограмма без последних N каналов
(калибровка/время не меняются). Применяется единой точкой в `main_window._on_loaded` (`sg =
sg.trimmed_channels(1)`) — все виды согласованы.
**Тесты:** обрезка даёт n-1 каналов; калибровка сохранена; drop_last<=0 — возврат self; защита от
обрезки до нуля каналов.

**Артефакты:** `awf/ui/style.py` (новый), правки `awf/ui/view3d.py`, `awf/ui/panels.py`,
`awf/ui/zscale.py`, `awf/ui/main_window.py`, `awf/model/spectrogram.py`; тесты
`tests/test_group4_remarks.py` (новый) + правка `tests/test_view3d_axes.py` (учёт зубцов IV-R2).

---

# Группа V. Продвинутая аналитика по матрице время×энергия (ТЗ-B.3,4,6,8,9)

> Оси координат нет — её роль играет **ось времени** (запись = маршрут во времени). Пространственные
> варианты — Группа VI.

## Задача 19 — Slice Analysis (сечения) 🟢🔵 · Объём M · *ТЗ-B.3*
**Цель:** спектр в точке (энергия→счёт) и профиль вдоль времени в энергоокне (время→счёт).

**Подзадачи:**
- 19.1. Спектр выбранного среза/точки — уже есть в `SlicePanel`; формализовать «точку».
- 19.2. Профиль по времени в окне энергии: `Spectrogram.energy_band_time_series(e_lo, e_hi)` (есть) —
  пример: 662 кэВ → Cs-137, 1461 кэВ → K-40.
- 19.3. UI — задание энергоокна (поля/перетаскивание), вывод временного профиля.

**Где:** `panels.py`, `model/spectrogram.py` (метод есть).
**Тесты:** клик по точке → спектр; энергоокно 662±Δ → временной профиль, совпадающий с ручной суммой.
**Зависимости:** numpy/pyqtgraph. **Предпосылки:** 1.

## Задача 20 — Изолинии (Contour Plot) 🔵 · Объём M · *ТЗ-B.4*
**Цель:** линии одинаковой интенсивности на 2D-карте (топографический вид).

**Подзадачи:**
- 20.1. `pg.IsocurveItem` поверх `HeatmapPanel`; уровни — по квантилям интенсивности.
- 20.2. Применять к Z-преобразованным данным (log/sqrt) для слабых пиков.
- 20.3. UI — число/шаг уровней, вкл/выкл.

**Где:** `panels.py`.
**Тесты:** smoke — изолинии по уровням; слабые пики видны на длинной записи.
**Зависимости:** pyqtgraph. **Предпосылки:** —

## Задача 21 — Карта отдельных пиков (по времени) 🟢🔵 · Объём M · *ТЗ-B.7 (временной)*
**Цель:** интенсивность энергетических окон интереса вдоль оси времени.

**Подзадачи:**
- 21.1. Реестр окон: 662 (Cs-137), 1461 (K-40), 609/1764 (U-серия), 2614 (Th-серия) — редактируемый.
- 21.2. Для каждого окна — временной ряд нетто (желательно после вычитания фона, Задача 5).
- 21.3. Мультиоконный график (наложение рядов) + экспорт.

**Где:** `awf/analysis/peakmap.py`; UI-график.
**Тесты:** для каждого окна корректный временной ряд; нетто согласуется с площадью пика.
**Зависимости:** numpy/pyqtgraph. **Предпосылки:** 4, 5, 6.

## Задача 22 — Градиентный анализ 🟢 · Объём S · *ТЗ-B.8 (временной)*
**Цель:** производная d(счёт)/d(время) — границы зон, малые/скрытые источники.

**Подзадачи:**
- 22.1. `numpy.gradient` по времени для полного счёта и/или окон энергий.
- 22.2. Сглаживание перед дифференцированием (опц., скользящее среднее).
- 22.3. Визуализация: профиль градиента / подсветка фронтов на карте.

**Где:** `awf/analysis/gradient.py`.
**Тесты:** на ступенчатой записи градиент локализует фронт (максимум на ступени).
**Зависимости:** numpy. **Предпосылки:** 1.

## Задача 23 — Деконволюция мультиплетов 🟡 · Объём L
**Цель:** разделить перекрывающиеся пики, получить площади компонент.

**Подзадачи:**
- 23.1. Модель: фикс. позиции/ширины (из калибровки+FWHM), свободные площади ≥0 + континуум (linear/step).
- 23.2. Решатель `scipy.optimize.lsq_linear` (trf, bounds A_k≥0); fallback `numpy.linalg.lstsq`+клиппинг.
- 23.3. χ²/dof, ковариация, погрешности площадей.
- 23.4. (опц.) гаусс+erfc-step для сильного континуума.

**API (предлагается):**
```python
def deconvolve_multiplet(counts_1d, centers, sigmas, *,
                         continuum='linear') -> DeconvolutionResult
```
**Источник:** `peaks/deconvolve.py` `multi_gaussian_fit()`; `peaks/area_step_continuum.py` (Gilmore §9.7).
**Тесты:** две близкие гауссианы разделяются, площади в допуске; без scipy fallback не падает.
**Зависимости:** scipy.optimize (+numpy fallback). **Предпосылки:** 2, 3, 8.

## Задача 24 — PCA / t-SNE / UMAP по срезам 🟡 · Объём M · *ТЗ-B.6*
**Цель:** проекция временных срезов в 2D для группировки/аномалий.

**Подзадачи:**
- 24.1. Матрица признаков: строка = спектр среза (опц. нормировка/лог).
- 24.2. PCA (`sklearn.decomposition.PCA`), t-SNE (`sklearn.manifold.TSNE`); UMAP (`umap-learn`, опц.).
- 24.3. Graceful import: метод выключается с понятным сообщением при отсутствии пакета.
- 24.4. Возврат 2D-координат + объяснённая дисперсия (для PCA).

**Где:** `awf/analysis/decomposition.py`.
**Тесты:** синтетика 2–3 кластеров спектров → проекции разделяют их (фикс. `random_state`).
**Зависимости:** scikit-learn (opt. umap-learn). **Предпосылки:** 1.

## Задача 25 — Спектральный кластерный анализ 🟡 · Объём L · *ТЗ-B.9*
**Цель:** автоматически выделить участки разного изотопного состава.

**Подзадачи:**
- 25.1. Признаки среза: отношения пиков, интегралы окон (Задача 21), PCA-компоненты (Задача 24).
- 25.2. Кластеризация: K-Means, DBSCAN (`sklearn.cluster`), HDBSCAN (`sklearn.cluster.HDBSCAN` ≥1.3 или
  `hdbscan`).
- 25.3. Перенос меток кластеров на шкалу времени (раскраска карты/временной оси).

**Где:** `awf/analysis/cluster.py`.
**Тесты:** синтетика → кластеры разделяются; метки согласованы со временем.
**Зависимости:** scikit-learn (opt. hdbscan). **Предпосылки:** 6, 21, 24.

## Задача 26 — Вкладка «Аналитика» 🔵 · Объём M
**Цель:** интерактивный обзор проекций/кластеров с навигацией к срезу.

**Подзадачи:**
- 26.1. Новая вкладка: 2D-скаттер (pyqtgraph) проекций (24) с раскраской по кластерам (25).
- 26.2. Клик по точке → переход к соответствующему временному срезу (спектр в `SlicePanel`).
- 26.3. Легенда кластеров, выбор метода проекции/кластеризации.

**Где:** `main_window.py` (+ новый виджет).
**Тесты:** smoke — скаттер отображает проекции/кластеры, клик открывает срез.
**Зависимости:** PySide6/pyqtgraph. **Предпосылки:** 24, 25.

---

## Группа V — РЕЗУЛЬТАТЫ ✅ ВЫПОЛНЕНО (Задачи 19–26)

> Слой аналитики `awf/analysis/` — Qt-free, только numpy (+ опц. scipy/sklearn), тестируется
> headless. Тяжёлые опциональные пакеты (sklearn/umap/hdbscan) **намеренно не ставились** — пути
> t-SNE/UMAP/DBSCAN/HDBSCAN сделаны graceful-optional (ImportError с понятным RU-сообщением),
> а первичные пути (PCA через SVD, KMeans через Lloyd+k-means++) работают всегда и протестированы.
> scipy 1.17.1 **есть** → Задача 23 использует `scipy.optimize.lsq_linear` (+ numpy-fallback).

**Задача 22 — Градиентный анализ** (`awf/analysis/gradient.py`): `moving_average` (box-сглаживание
cumsum), `GradientResult`, `total_counts_series(sg)`, `time_gradient(...)` (np.gradient по времени,
фронт = argmax|grad|), `band_gradient(sg, e_lo, e_hi, ...)`. Тесты: `tests/test_gradient.py` (8) —
ступенчатая запись, фронт локализован.

**Задача 21 — Карта пиков по времени** (`awf/analysis/peakmap.py`): `EnergyWindow` (центр±полуширина),
`DEFAULT_WINDOWS` (Cs-137 662, K-40 1461, U 609/1764, Th 2614), `WindowSeries` (gross/baseline/net),
`window_series(...)` (континуум — трапеция Ковелла по боковым полосам), `peak_map(...)`. Тесты:
`tests/test_peakmap.py` (8).

**Задача 24 — PCA/t-SNE/UMAP** (`awf/analysis/decomposition.py`): `feature_matrix(sg, *, normalize,
log)` (строка = спектр среза, log1p + L1-норма), `pca` (SVD центрированной матрицы + объяснённая
дисперсия), `project(X, method=...)` диспетчер, `is_available(method)`; t-SNE/UMAP — graceful-optional.
Тесты: `tests/test_decomposition.py` (8).

**Задача 23 — Деконволюция мультиплетов** (`awf/analysis/deconvolve.py`): `deconvolve_multiplet(
counts_1d, centers, sigmas, *, continuum='linear', use_scipy=True)` → `DeconvolutionResult` (площади,
погрешности, χ²/dof). Решатель `scipy.optimize.lsq_linear` (trf, площади≥0) + fallback
`numpy.linalg.lstsq`+клиппинг. Тесты: `tests/test_deconvolve.py` (7) — две близкие гауссианы
разделяются; без scipy не падает.

**Задача 25 — Кластерный анализ** (`awf/analysis/cluster.py`): `kmeans(X, k, ...)` (Lloyd + k-means++,
фикс. seed, best-inertia), `cluster(X, method=...)` диспетчер, `is_available`, `segments(labels)` →
непрерывные участки по времени; DBSCAN/HDBSCAN — graceful-optional. Тесты: `tests/test_cluster.py` (9).

**Задача 19 — Slice Analysis (UI)** (`awf/ui/panels.py`, `SlicePanel`): «спектр в точке» формализован
(`show_time_slice` рисует `energy_spectrum(i)`); добавлен временной профиль в энергоокне —
`show_energy_window(e_lo, e_hi)` (жёлтая кривая, `energy_band_time_series`, независима от ROI) +
UI-контролы (пресет нуклида `DEFAULT_WINDOWS` + два спинбокса границ, `panels.py:290+`). Тесты:
`tests/test_slice_analysis.py` (8) — точка→спектр совпадает; энергоокно 662±Δ→профиль == ручной сумме.

**Задача 20 — Изолинии (UI)** (`awf/ui/panels.py`, `HeatmapPanel`): `set_contours_enabled`,
`set_contour_levels`, `_contour_level_values` (уровни по квантилям Z-карты, строго внутри диапазона),
`_apply_contours` (`pg.IsocurveItem`, данные транспонируются под row-major карту). Тесты:
`tests/test_contour.py` (8) — уровни монотонны/в диапазоне, выкл. очищает, смена Z-шкалы пересоздаёт
без утечки, константа → 0 изолиний.

> **ВРЕМЕННО ОТКЛЮЧЕНО в UI (решение оператора 2026-06-26):** тулбар-контролы изолиний (чекбокс
> «Изолинии» + спинбокс уровней) убраны из `main_window._build_toolbar`. Причина: на сыром шумном
> waterfall изолинии дают «кашу», смысл появится только после поиска/идентификации пиков и подгонки
> их чистыми гауссианами. **Механизм в `HeatmapPanel` (`set_contours_enabled`/`set_contour_levels`/
> `_apply_contours`) и 8 тестов сохранены**; обработчики `_on_contours_toggled`/
> `_on_contour_levels_changed` оставлены для возврата контролов. Эмпирически механизм рабочий: рендер
> `HeatmapPanel` offscreen с включёнными изолиниями даёт ~78 тыс. белых пикселей контуров (исходное
> «не работают» = смотрели на вкладку 3D, изолинии только в 2D, + субпиксельная толщина линий).

### Дефекты UI (выявлены по скриншоту оператора 2026-06-25/26)
- **#34 — спинбокс «Изолинии» наезжал на соседний контрол тулбара** → снят вместе с отключением
  UI изолиний (см. выше).
- **#35 — откреплённый (floating) док терял тёмный стиль** (`awf/ui/style.py` + `awf/ui/main_window.py`):
  добавлено правило `QDockWidget > QWidget { background-color: #2b2d31; }` + `WA_StyledBackground` на
  трёх панелях-содержимого (`SlicePanel`/`SectionControls`/`NuclidePanel`) — кастомный QWidget-подкласс
  иначе игнорирует QSS-`background`. Проверено рендером всех трёх доков offscreen → тело тёмное.
  Сцены pyqtgraph не затронуты (потомки панели, не дока). Диагностика: `scripts/diag_dock_style.py`.
- **#36 — мелкий шрифт меню** (`awf/ui/style.py`): `font-size: 14px` для `QMenuBar`/`QMenu` +
  увеличенные `padding` пунктов.

### Доработки секущих плоскостей + раскладка (скриншоты оператора 2026-06-26)
- **#37 — плоскость сечения 3D перекрывала данные** (`awf/ui/view3d.py`): убрана полупрозрачная
  голубая заливка квадрата. `mesh` (GLMeshItem) переведён в `drawFaces=False, drawEdges=False` —
  ничего не рисует (и диагонального ребра quad'а больше нет), оставлен как держатель состояния
  видимости (тесты `_planes[...]["mesh"].visible()` сохранены). Видимый **контур прямоугольника**
  рисует новый `border` (GLLinePlotItem, замкнутый цикл по 4 углам, цвет оси). Профиль-кривая
  (`line`) сохранена. Проверено: `drawFaces=False`, контур из 5 точек замкнут.
- **#38 — синк дока «Срезы/Сечения/Выборки» с сечениями 3D** (`awf/ui/panels.py` `SlicePanel` +
  `awf/ui/view3d.py` + `main_window.py`): `Waterfall3DView.active_plane_values()` отдаёт реальные
  значения видимых плоскостей `{axis:[v0_or_None,v1_or_None]}`; `SlicePanel.sync_sections(t_vals,
  e_vals)` — обе плоскости Энергии → жёлтый временной профиль энергоокна; обе Времени → ROI окна
  времени (каналы из энергоплоскостей либо вся ось); одна Времени → спектр этого среза. real→индекс
  через `argmin|центры−v|` (LOD-aware).
- **#39 — синк 2D-карты с сечениями 3D** (`awf/ui/panels.py` `HeatmapPanel`):
  `set_section_markers(t_vals, e_vals)` рисует `pg.InfiniteLine`-маркеры — Время → горизонталь
  (ось Y), Энергия → вертикаль (ось X); real(с/кэВ) → дисплейные координаты через `_t_scale`/
  `_ch_scale`; цвета совпадают с осями 3D (время=бирюза `(51,217,242)`, энергия=пурпур
  `(242,89,217)`). Маркеры сбрасываются при загрузке нового файла. Оркестрация — в
  `MainWindow._on_plane_changed` (вызывается и из `emit_all()` после загрузки).
- **#40 — запоминание расположения окон** (`awf/ui/main_window.py`): `QSettings` (org
  `VibeEngineering-LLC`, app `AtomSpectraWaterfallViewer`). `closeEvent` сохраняет
  `saveGeometry()`/`saveState()`, `_restore_layout()` в конце `__init__` восстанавливает. Докам и
  тулбару заданы `objectName` (требование `saveState/restoreState`); `main()` ставит org/app на
  `QApplication`.
- Тесты: `tests/test_section_sync.py` (10) — `active_plane_values` (видимость/реальные единицы),
  `sync_sections` (3 режима + noop без данных), `set_section_markers` (число/углы/сброс),
  персистентность раскладки (round-trip через изолированный INI). **Полный pytest — 232 passed.**

### Графики среза: подсветка, синк, лог/лин, counts/cps (скриншоты оператора 2026-06-26/27)
- **#41 — на срезе 3D показать кривую спектра** (`awf/ui/view3d.py`; 2D — `panels.py` `SlicePanel`):
  - *Первая итерация (недостаточно):* перо 2D-спектра `_spectrum_curve` переведено на бирюзу рамки
    плоскости Времени `(51,217,242)` width 2 (визуальная привязка спектра к плоскости). Оставлено.
  - *Переоткрыто оператором («не сработало; на срезе картинка должна быть как на спектре»):* профиль
    сечения (`line` в `_planes`) совпадал по высоте с непрозрачным рельефом (`z = _z_surface[i,:]`),
    тонул в его «коже» (z-fighting) — на плоскости была видна только пустая рамка. **Фикс:** профиль
    рисуется ПОВЕРХ рельефа с выключенным depth-тестом — модульный `_PROFILE_GL`
    `{GL_DEPTH_TEST:False, GL_BLEND:False, GL_ALPHA_TEST:False, GL_CULL_FACE:False}`, `line.setGLOptions`
    при создании, цвет оси, width 2.5. Теперь на плоскости Времени видна кривая = срез поверхности по
    энергии (форма спектра), на плоскости Энергии — срез по времени; плоскость Отсчётов профиля не
    имеет. Тесты: профиль = `_z_surface[i,:]` и виден; depth-тест off; плоскость Отсчётов без профиля.
- **#42 — нижний график отсчётов синхронизируется с сечениями** (`awf/ui/panels.py` `SlicePanel`):
  `sync_sections` рисует вертикальные бирюзовые пунктирные `InfiniteLine`-маркеры срезов Времени
  (`_draw_series_sections`) на оси времени графика «Отсчёты в полосе»; цвет совпадает с плоскостью
  Времени 3D и маркерами 2D-карты. Сбрасываются при загрузке нового файла (`_clear_series_sections`
  в `set_spectrogram`). Тесты: 2 метки на 2 плоскости (углы 90°), 0 без плоскостей Времени, сброс.
- **#43 — спектр среза: переключатель лог/лин по Y** (`awf/ui/panels.py` `SlicePanel`): чекбокс
  «лог Y» в строке энергоокна → `set_spectrum_log(on)` → `_spectrum_plot.setLogMode(False, on)`
  (только ось Y). Тест: `getAxis("left").logMode` True/False, ось X не трогается.
- **#44 — все счётные графики: counts/cps** (`awf/model/spectrogram.py` + `view3d.py` + `panels.py`
  + `main_window.py`): cps = counts / live_time. Модель: `counts_in_unit(mode)` (полноразмерная
  по-срезовая скорость, «мёртвый» срез → 0), `live_time_total(t_lo,t_hi)` (делитель спектра окна),
  `downsample(..., data=src)` (прорежать cps-матрицу, а не counts). Комбо «Единицы: отсчёты /
  отсч-в-сек» в тулбаре → `MainWindow._on_unit_changed` веером на `view3d.set_unit_mode` /
  `heatmap.set_unit_mode` / `slices.set_unit_mode` + `emit_all()` (3D/2D пересчитываются от
  исходника → переразместить плоскости). Семантика: **спектр** ÷ суммарное live_time окна;
  **временной ряд** ÷ по-срезовое live_time; **2D-карта/3D-поверхность** — по-срезовая cps на ПОЛНОМ
  разрешении ДО `downsample(method="max")` (autoLevels/нормировка по zmax сокращают постоянный
  делитель — раскрываем только по-срезовую вариацию). Подписи осей Y переключаются (отсч. ↔ отсч/с).
  **Вкладка «Аналитика» исключена** — её величины (градиент, PCA, кластеры, деконволюция) не в
  счётных единицах. Тесты: модель (identity/делёж/мёртвый срез/`live_time_total`/`data`-param/
  shape-mismatch), SlicePanel (спектр и ряд масштабируются, подписи, возврат в counts), HeatmapPanel
  (малая карта делится на live_time), view3d (smoke + `plane_value` unit), глобальный веер из тулбара.
- **#45 — голубая линия профиля сечения теряется на рельефе** (`awf/ui/view3d.py`): кривая сечения
  красилась цветом оси (бирюза/пурпур) и тонула на поверхности viridis + сливалась с рамкой. Цвет
  профиля переведён на **белый** (`_PROFILE_RGB=(1,1,1)`, width 3.0) — контрастен и на чёрном фоне,
  и на цветном рельефе. Рамка плоскости остаётся цветом оси (идентичность плоскости).
  **Пересмотрено в #47/#48:** профиль больше не лежит на «коже» рельефа (стал проекцией суммы,
  вынесенной в высоту плоскости), поэтому белый снят и цвет возвращён к цвету рамки/оси (см. #48).
- **#46 — регулировка интенсивности теней/освещения 3D** (`awf/ui/view3d.py` + `main_window.py`):
  поверхность строилась `shader=None` (плоско-цветная по палитре, без объёма). Добавлено рельефное
  затенение по Ламберту: `_surface_shading(z_surface, intensity)` строит множитель освещённости из
  нормали (градиент высоты) и источника `_LIGHT_DIR`, `shade=ambient+(1-ambient)·lam` (ambient 0.35),
  итог `(1-I)+I·shade` — RGB×множитель в `set_spectrogram` (alpha не трогаем, только затемнение).
  `set_light_intensity(0..1)` + ползунок «Освещение» в тулбаре (0 = как было, без теней; 1 = полные
  тени). Только 3D (у плоской 2D-карты теней нет). Тесты: 0→None, диапазон [ambient,1], затемнение
  колонок + alpha не тронут, 0 восстанавливает исходные цвета, ползунок MainWindow → `_light`.
- **#47 — на плоскости сечения проекция суммы между плоскостями (не срез)** (`awf/ui/view3d.py`):
  профиль на секущей плоскости красился срезом рельефа по одному индексу (`_z_surface[i,:]`) —
  шумный спектр одного момента, лежащий на «коже» поверхности. Теперь профиль = **проекция-сумма**
  рельефа в объёме МЕЖДУ парой плоскостей оси: `_section_projection(axis)` суммирует `_z_surface`
  по оконному диапазону (time → по времени `[i0:i1]`, кривая по энергии `nc`; energy → по энергии
  `[j0:j1]`, кривая по времени `nt`) и вписывает в высоту плоскости `[0, zmax]`. Окно суммирования —
  как у обрезки IV-R3: обе плоскости оси видимы → между ними, иначе вся ось. `set_plane` обновляет
  профили **обоих** слотов оси (сумма зависит от пары). Обе плоскости показывают одну проекцию.
  Тесты: одиночная плоскость → проекция всей оси; пара → сумма между ними (оба профиля равны).
- **#48 — цвет профиля сечения = цвет рамки/оси** (`awf/ui/view3d.py`): белый из #45 снят. Профиль
  вынесен в высоту плоскости (#47) и рисуется поверх рельефа — больше не тонет, поэтому цвет
  возвращён к **цвету рамки/оси** («как рама плоскости»): time = бирюза `(0.20,0.85,0.95)`,
  energy = пурпур `(0.95,0.35,0.85)`. Константа `_PROFILE_RGB` удалена; цвет берётся из `_AXIS_RGB`.
  Тест: профиль time = цвет рамки time, профиль energy = цвет рамки energy.
- **#49 — суммарные линии сечения не видны снаружи плоскостей** (`awf/ui/view3d.py`): профиль-проекция
  тонул под непрозрачным рельефом на дальней плоскости. Корень — порядок отрисовки: поверхность
  пересоздаётся каждый рендер (`_rebuild_surface`: `removeItem`+`addItem`) и оказывается ПОСЛЕДНЕЙ в
  списке элементов → рисуется последней и перекрывает линию (выключенный depth-тест линии от этого не
  спасает — он влияет только на сравнение глубины при отрисовке самой линии). Фикс: `line.setDepthValue(10)`
  при создании плоскости — `GLViewWidget` сортирует элементы по `depthValue` (больше → позже), линия
  гарантированно рисуется после поверхности и видна снаружи плоскостей. Тест: `line.depthValue()==10 >
  surface.depthValue()`.
- **#50 — центральный пик не попадал в проекцию суммы** (`awf/ui/view3d.py`): `_section_projection`
  суммировал `_z_surface` — уже **log-сжатые** дисплейные высоты. `Σ log` переоценивает широкие полки
  и теряет узкие/транзиентные пики; рельеф же строится по `method="max"` и показывает такой пик башней —
  кривая суммы его «не считала» (максимум кривой садился на полку). Фикс: суммировать **сырые `_z_counts`**
  по окну, затем `apply_z_scale(сумма)` и нормировка в высоту плоскости — `log(Σ)` вместо `Σ log`.
  Диагностика `scripts/diag_projection_peak.py`: для вспышки в 3 срезах из 60 `argmax` кривой переходит
  с полки (канал 14) на реальный пик (канал 40). Тест: транзиентный пик → `argmax(prof)` = канал пика.
- **#51 — кнопка сброса настроек отображения** (`awf/ui/main_window.py`): в тулбар добавлена кнопка
  «Сброс» (`_reset_btn` после ползунка «Освещение»). Слот `_on_reset_display` ставит каждый контрол
  отображения в дефолт: Z-шкала=log (индекс 2), Палитра=Insight (0), Единицы=отсчёты (0), Оси=вкл,
  Подсветка=выкл, Усиление=100 (1.0), Гамма=100 (1.0), Отсечка%=100 (без отсечки), Сглаживание=0,
  Освещение=0. Применение «бесплатное»: Qt шлёт `valueChanged`/`currentIndexChanged`/`toggled` только
  при реальном изменении, и штатный обработчик каждого контрола веером доносит дефолт до 2D/3D/срезов.
  Тест: увести все контролы с умолчаний → `_on_reset_display()` → все контролы и параметры панелей
  (`_view3d._unit/_light/_gain`) вернулись к дефолту.
- **#52 — на плоскость сечения вывести спектр из окна спектра** (`awf/ui/view3d.py`): профиль на
  плоскости расходился с верхним-правым окном «Срезы/Сечения/Выборки». Корень: окно строит
  `sg.sum_spectrum(t_lo,t_hi)` — **интеграл сырых counts** по окну времени, а профиль суммировал
  `_z_counts` — LOD, прорежённую `method="max"` (в ячейке берётся максимум, не сумма) → фон раздут,
  форма не та. Фикс: завести отдельную **sum-LOD** `_z_counts_sum` = `downsample(method="sum")` на
  тех же бинах; `_section_projection` суммирует её → сумма sum-бинов по окну == `sum_spectrum`,
  прорежённому к `nc` каналам = ровно спектр окна. Меняет семантику #50: при истинной сумме широкая
  устойчивая полка (бо́льший интеграл) может быть выше короткого транзиента-башни — **так и показывает
  окно** (физично: окно интегрирует counts). Тест #50 (транзиент=argmax) заменён на #52
  (`argmax(profile) == argmax(sg.sum_spectrum) == канал устойчивого пика`, `prof[пик] > prof[башня]`).
- **#53 — все графики в cps; рельеф = cps/канал/момент; голубая = сумма cps на срезе**
  (`awf/ui/view3d.py`, `awf/ui/panels.py`, `awf/ui/main_window.py`): дефолтные единицы отображения
  переключены counts→cps. Машинерия cps уже была из #44 — сменён только дефолт: `_unit_combo`
  стартует в index 1 (cps) и кнопка «Сброс» возвращает туда же (`main_window.py`); панели
  `Waterfall3DView`/`HeatmapPanel`/`SlicePanel` дефолтят `_unit="cps"`; `SlicePanel.__init__`
  зовёт `_apply_unit_labels()`, чтобы подписи осей Y сразу были «отсч/с». Рельеф (оранжевый):
  `_z_counts` (max-LOD) уже строится из `src = sg.counts_in_unit(self._unit)` → при cps это
  **мгновенная скорость счёта в канале в каждый момент** (max сохраняет узкие пики). Голубая линия:
  `_z_counts_sum` (sum-LOD) из того же cps-`src` → `_section_projection` суммирует cps по окну =
  **сумма cps на срезе** (уточняет #52: была сумма counts → стала сумма cps). Связь со стартовым
  порядком: `setCurrentIndex(1)` стоит ДО `currentIndexChanged.connect`, поэтому преждевременный
  веер-сигнал не шлётся; `_on_loaded` единицы не переприменяет — панели берут собственный дефолт cps.
  Тест #53: `test_view3d_profile_is_cps_not_counts` — два пика с разным live_time меняют местами
  «кто выше» по counts vs cps, профиль идёт за cps (`argmax(prof)==argmax(Σcps)≠argmax(Σcounts)`).
  5 тестов `test_slice_analysis.py` + heatmap/sections/fan-out, сверявших сырые counts, получили явный
  `set_unit_mode("counts")` (семантику counts-ветки проверяют прежним образом).
- Тесты: `tests/test_units_log.py` (31) — модель единиц (6), #41 2D-цвет (1), #47/#52 проекция 3D (3:
  одиночная/между плоскостями/совпадение с окном), #49 depthValue + #47 depth-off (2) + counts без
  профиля (1), #43 лог (1), #42 маркеры (3), #44 (6), #48 цвет=рамка (1), #46 затенение/ползунок (5),
  #51 сброс настроек (1), #53 cps-профиль (1).
  **Полный pytest — 263 passed.**
- **#54 — лог Z-шкалы вырождался в линейную для cps** (`awf/ui/zscale.py`): проверка «правильности
  пересчёта 3D-массива в логарифм» вскрыла регрессию, открытую #53. Прежняя формула `log10(1+max(x,0))`
  калибрована под целые counts (x≥1, «+1» = естественный квант). После #53 дефолт = cps, где значения
  дробные <1; на реальном `sample_data/waterfall_sample.n42` (256×8192) **97.5% ненулевых cps-каналов
  имеют x<1**, а там `log10(1+x)≈0.4343·x` — лог вырождался в **линейный**, теряя сжатие динамического
  диапазона; нормированная форма рельефа становилась зависимой от единиц (counts↔cps). Фикс: истинный
  `log10(max(x,floor)/floor)` с авто-порогом `floor = 1-й перцентиль ненулевых значений` (`_base_transform`,
  ветка `log`). **Масштаб-инвариантно**: floor масштабируется вместе с данными, поэтому отношение
  `x/floor` (а значит и форма) не зависит от единиц. Применяется единообразно к 3D-рельефу, 2D-карте и
  профилю сечения (все через `apply_z_scale`). Верификация на реальном sample: старый лог отличался от
  линейного на 0.069 (нормир., вырождение), новый — 0.137 (вдвое логарифмичнее); counts vs cps дают
  идентичную нормированную форму (расхождение 0.0). Тесты: `test_log_scale_invariant_for_units` (counts↔cps
  одна форма), `test_log_not_degenerate_for_subunit_cps` (для cps<1 лог ≠ линейный); `test_backward_compat_defaults`
  обновлён на новую эталонную формулу (`_expected_log`).
  **Полный pytest — 265 passed.**
- **#55 — регулировки на отдельную панель в стиле «рукоятки» + per-knob/общий bypass**
  (`awf/ui/knobs.py` новый, `awf/ui/main_window.py`, `awf/ui/style.py`): 5 регулировок отображения
  (Усиление/Гамма/Отсечка%/Сглаживание/Освещение) вынесены из тулбара в отдельный док
  «Регулировки отображения» (`dock_adjust`, лево). Новый модуль `knobs.py`: `Knob` — круглый
  поворотный регулятор (зелёная дуга 240°, светлый указатель, вертикальный drag/колесо) с
  **QSlider-совместимым API** (`value/setValue/setRange/valueChanged`), поэтому обработчики
  main_window работают без правок (старые имена `_gain_slider…_light_slider` — алиасы на ручки).
  `KnobRow` = подпись + ручка + значение + индивидуальные «вкл/выкл» и «⟲ сброс».
  `AdjustPanel` = сетка 5 рядов + «Регулировки: ВКЛ/ВЫКЛ» (общий) + «Сброс всех». Bypass-семантика
  («при выключении — стандартный вид как без регулировки»): дефолты ручек нейтральны (gain 1.0,
  gamma 1.0, отсечка 100%, сглаживание 0, свет 0); `AdjustPanel.values()` отдаёт дефолт по ряду,
  если ряд выключен, и ВСЕ дефолты, если выключен общий — позиции ручек при этом сохраняются
  (повторное включение восстанавливает значение). Единый слот `_on_adjust_changed` (заменил
  `_build_contrast_controls`/`_on_contrast_changed`/`_on_smooth_changed`/`_on_light_changed`)
  читает `values()` и применяет только изменившиеся группы к 2D/3D/срезам + `emit_all()`. Кнопка
  «Сброс» (#51) теперь зовёт `AdjustPanel.reset_all()`. QSS для objectName ручек в `style.py`.
  Тесты: `tests/test_knobs_panel.py` (7) — QSlider-API Knob (кламп/сигнал/диапазон), per-row bypass
  с сохранением позиции, глобальный bypass + reset_all, док `dock_adjust` существует, проводка
  MainWindow (gain 250→2.5×, выкл ряда→1.0 с сохранением 250, общий выкл→все дефолты).
  **Полный pytest — 272 passed.**
- **#56 — рукоятка «Окно t»: ширина выборки по времени 3D-водопада**
  (`awf/ui/knobs.py`, `awf/ui/view3d.py`, `awf/ui/main_window.py`): добавлена 6-я ручка на ту же
  панель регулировок (#55), в том же стиле, с индивидуальными вкл/выкл+сброс и общим bypass.
  Оператор уточнил дословно: «фактически это изменение ширины выборки по времени» — ручка меняет
  **число временны́х бинов LOD-прорежки** 3D-поверхности (`max_time` в `Spectrogram.downsample`),
  т.е. перебивку данных по времени, а НЕ косметическое растяжение по X. Значение ручки v/100 =
  относительная ширина выборки w (диапазон ×0.25…×4.00, дефолт ×1.00); главное окно переводит её
  в `max_time = round(400/w)` с клипом [16..1600] (`_tbin_to_bins`). Шире выборка (ручка вверх) →
  меньше бинов → грубее/«сжато»; у́же (вниз) → больше бинов → детальнее/«растянуто». Дефолт 100
  (×1.00 → max_time=400) нейтрален = стандартный вид (bypass ряда/общий возвращает его). Новый
  `Waterfall3DView.set_time_bins()` (зеркало `set_smoothing`) ставит `_max_time` и ре-рендерит из
  той же sg. Сигнатура `set_spectrogram(max_time=None, max_chan=None)` теперь сохраняет текущие при
  None — поэтому ширина выборки переживает загрузку нового файла (аргументный вызов в `_on_loaded`),
  как `_gain/_smooth/_light`. Тесты: `tests/test_knobs_panel.py` (+2) — ручка→`_max_time`
  (×2→200, ×0.5→800, bypass→400, позиция сохранена) и персистентность через повторный
  `set_spectrogram`. **Полный pytest — 274 passed.**
- **#57 — регуляторы = вертикальные движки-фейдеры (не круглые крутилки)**
  (`awf/ui/knobs.py`): оператор (скриншот iZotope «Time Structure») — регуляторы панели
  должны быть вертикальными движками, а не круглыми поворотными крутилками. Класс `Knob`
  перерисован: вместо круга/дуги/радиальной риски — вертикальный фейдер (тёмный паз
  `drawRoundedRect`, зелёный «уровень» снизу до каретки, риски-шкала по 5 делений с обеих
  сторон, светлая металлическая каретка-указатель `_draw_cap` с центральной прорезью).
  Размер виджета 50×50 → 44×92 (узкий вертикальный). Управление прежнее — вертикальный
  drag/колесо (как раз естественно для фейдера). **Весь QSlider-совместимый API сохранён**
  (`value/setValue/setRange/minimum/maximum/valueChanged`), `KnobRow`/`AdjustPanel`,
  bypass-семантика и все 6 регуляторов — без изменений; имя класса `Knob` и алиасы
  `_*_slider` оставлены ради внешнего кода/тестов. `import math` и константы дуги
  `_A0/_SPAN` удалены (не нужны). Тест `test_knob_is_vertical_fader` (высота>ширины) +
  smoke-рендер панели (вкл/выкл/значения). **Полный pytest — 275 passed.**
- **#58 — движки горизонтальные, панель в одну колонку**
  (`awf/ui/knobs.py`): оператор (скриншот панели 3×2 вертикальных фейдеров) — сделать движки
  горизонтальными и расположить одной колонкой. `Knob` повёрнут на 90°: паз горизонтальный
  (`x0..x1`), зелёный «уровень» слева до каретки, риски-шкала по 5 делений сверху/снизу,
  каретка-указатель `_draw_cap` — вертикальная металлическая планка на позиции `hx`. Размер
  44×92 → низкий тянущийся: `setFixedHeight(24)`+`setMinimumWidth(80)`+Expanding-по-ширине,
  `sizeHint()=120×24`. Управление: вертикальный drag (`_drag_y`/`SizeVerCursor`) →
  горизонтальный (`_drag_x`/`SizeHorCursor`, вправо=+). `KnobRow` — ряд `QHBoxLayout`
  (подпись 96px слева | движок (stretch) | значение 56px справа | вкл | ⟲). `AdjustPanel`:
  сетка `grid.addWidget(row, i, 0)` — одна колонка (ряды друг под другом). **QSlider-API,
  bypass-семантика, все 6 регуляторов — без изменений.** Тест `test_knob_is_vertical_fader` →
  `test_knob_is_horizontal_fader` (`sizeHint`: ширина>высоты) + smoke-рендер горизонтальной
  панели (вкл/выкл/крайние значения). **Полный pytest — 275 passed.**
- **#59 — окно управления сечениями в том же knob-стиле, по умолчанию все выкл**
  (`awf/ui/view3d.py`, `SectionControls`): оператор (скриншот панели сечений под новой панелью
  регулировок) — оформить «Секущие плоскости (2 на ось)» в том же стиле горизонтальных движков.
  Панель получила `objectName("adjustPanel")` (общий фон/QSS), заголовки осей и слотов — стиль
  `knobTitle`, значение — `knobValue`. Каждый слот теперь ряд `#N | Knob | значение | вкл/выкл`:
  `QSlider` → `Knob` (горизонтальный фейдер #58), `QCheckBox` → `QToolButton` `knobToggle` с
  текстом «вкл»/«выкл». **По умолчанию все слоты выкл** — кнопки не нажаты, движки погашены
  (`setEnabled(False)`); включение слота оживляет движок и переиздаёт `planeChanged(visible=True)`.
  Публичный API без изменений (сигнал `planeChanged`, ключи `_rows`, `_frac`/`_emit`/
  `set_value_label`/`emit_all`) — проводка `main_window` и прежние тесты валидны. Тест
  `test_section_controls_default_all_off` + smoke-рендер панели. **Полный pytest — 276 passed.**
- **#60 — дефолтные положения рукояток/переключателей по эталон-скриншоту (обе панели)**
  (`awf/ui/knobs.py`, `awf/ui/view3d.py`): оператор (скриншот обеих панелей) — «положение
  рукояток и переключателей по умолчанию должно быть такое». Стартовое (дефолтное) состояние
  приведено к скриншоту: (1) **Регулировки** — общий выключатель по умолчанию **ВЫКЛ**
  (`_global.setChecked(False)`, текст «Регулировки: ВЫКЛ»), каждый ряд по умолчанию **выкл**
  (`KnobRow._chk` не нажат, ручка и значение погашены `setEnabled(False)`); ручки на нейтральных
  дефолтах (gain/gamma/clip=100, smooth/light=0, tbin=100) — bypass, стандартный вид без правок;
  (2) **Сечения** — все слоты выкл (с #59), движки слотов разведены по краям: слот **#1 — в
  минимуме** (`Knob`=0, frac 0, левый край), слот **#2 — в максимуме** (`Knob`=`_SLIDER_MAX`,
  frac 1, правый край). `reset_all()` приведён к новому дефолту (сброс → всё выкл, ручки на
  дефолтах) для согласованности «сброс == дефолт». Тесты `test_panel_defaults_all_off`,
  `test_section_default_slider_positions` + правки bypass-тестов (включают ряды перед проверкой
  passthrough) + smoke-рендер обеих панелей. **Полный pytest — 278 passed.**

- **#61 — убрать отображения на секущих плоскостях + грани плоскостей сделать прозрачнее**
  (`awf/ui/view3d.py`): оператор — «все отображения на плоскостях убирай. Грани плоскостей сделай
  прозрачными (не такими яркими)». (1) **Профиль/проекция на плоскости убраны** — элемент `line`
  каждой плоскости теперь всегда скрыт (`line.setVisible(False)` в начале `_apply_plane`), построение
  кривой удалено (сняты `prof = np.column_stack([...])` в ветках time/energy, убрана отрисовка
  `line.setData`). Это удаляет визуал фич #41/#47/#48/#49/#50/#52/#53 на самой плоскости; метод
  `_section_projection` больше не вызывается — оставлен мёртвым кодом с docstring-пометкой на случай
  возврата. Значения плоскостей (`plane_value`/`active_plane_values`, метки в `main_window.py`) —
  отдельная фича, не затронуты. (2) **Грань плоскости приглушена** — контур-рамка `border` рисуется
  цветом оси, но полупрозрачным дим-альфой `_BORDER_ALPHA = 0.30` (было 1.0) и тоньше (width 1.5
  вместо 2.0); заливка `mesh` остаётся выключенной (#37 — не возвращаем, иначе снова перекрыла бы
  рельеф). Тесты: 7 тестов профиля в `test_units_log.py` удалены вместе с фичей, один перепрофилирован
  в `test_view3d_section_border_is_dim` (грань = цвет оси + alpha `_BORDER_ALPHA`, `line` скрыт);
  `test_view3d_sections.py::test_set_plane_visibility_no_profile` (бывш. `_and_profile`) проверяет
  видимость mesh+border и скрытость line на всех осях. **Полный pytest — 271 passed** (счётчик ниже
  278 — удалены 7 тестов снятой фичи профиля).

- **#62 — тулбар «Вид» и строка статуса скучены: крупнее шрифт, выше панели**
  (`awf/ui/style.py`, `awf/ui/main_window.py`): оператор (скриншот) — «тут очень скученно. увеличить
  высоту и размер шрифта». (1) **Тулбар «Вид»** — в QSS подняты `spacing` 3→8px и `padding` 3→7/8px,
  добавлен `font-size:15px`; целевые правила потомков `QToolBar QComboBox/QPushButton` дают
  `min-height:28px` + увеличенный `padding` (комбобоксы Z-шкала/Палитра/Единицы и кнопка «Сброс»
  выросли с 22 до 40–42px, сам тулбар ~58px), `QToolBar QLabel/QCheckBox` — 15px. Селекторы по
  потомкам `QToolBar`, поэтому доки-панели (рукоятки/сечения) не раздуты. (2) **Строка статуса** —
  QSS `font-size:14px` + `QStatusBar QLabel`; высота `QStatusBar` Qt из QSS `min-height` не берёт
  (считает от layout), поэтому в коде `statusBar().setMinimumHeight(28)` и `pointSize+1`. Тесты
  `test_toolbar_and_statusbar_sized_up` (QSS содержит целевые правила) и
  `test_toolbar_combo_taller_than_default` (комбобокс выше дефолтного, строка статуса ≥28px).
  **Полный pytest — 273 passed.**

- **#63–#68 — координатная сетка 3D-водопада: рамка в клетку, единицы времени, единицы клеток,
  убрана вертикальная шкала** (`awf/ui/view3d.py`, `awf/ui/main_window.py`,
  `tests/test_group4_remarks.py`): пять замечаний со скриншота 3D-водопада «сверху», решённых одной
  связной правкой координатной сетки (по уточнению через AskUserQuestion).
  - **#63 — поля в 1 клетку вокруг данных.** Прежняя равномерная `GLGridItem` заменена координатной
    сеткой (`_rebuild_grid`/`_draw_grid_lines`): линии на круглых делениях шкал (`_GRID_RGBA`) плюс
    яркая рамка поля `_GRID_BORDER_RGBA` (width 1.8), отнесённая на одну клетку (`cellx`/`celly` =
    медиана шага делений) за край данных со всех 4 сторон. Сетка живёт в отдельном списке
    `_grid_items` (не в `_axis_items`), снимается вместе с осями (`set_axis_labels_visible`).
  - **#68 — размер клеток привязан к шкалам.** Линии сетки стоят ровно на «круглых» делениях
    `_time_ticks()`/`_energy_ticks()` (через `_nice_ticks`), т.е. клетка = один интервал шкалы t/E,
    а не косметический равномерный шаг.
  - **#64 — единицы оси времени (с/мин/ч) + значения клеток.** `_TIME_UNIT_SCALE` + `set_time_unit`;
    круглые деления считаются уже в выбранной единице (`_time_ticks`), мировые X — обратной
    интерполяцией. В тулбаре «Вид» добавлен комбобокс «Время» (с/мин/ч) → `_on_time_unit_changed` →
    `view3d.set_time_unit`; дефолт «с», сбрасывается кнопкой «Сброс».
  - **#66 — размерность каждой клетки.** Подпись каждого деления несёт единицу: время — `f"{tv:g} {unit}"`
    (например «30 с», «5 мин»), энергия — `f"{en:g} кэВ"`; зубцы шкалы энергий (IV-R2) сохранены.
  - **#65 — убрана вертикальная шкала.** Из `_rebuild_axis_labels` полностью удалён блок оси счёта
    (Z): нет ни заголовка «N, отсч./отсч/с», ни числовых делений высоты.
  Тесты: `test_grid_built_with_border`, `test_grid_cleared_when_axes_hidden`,
  `test_grid_border_extends_one_cell_beyond_data`, `test_vertical_count_scale_removed`,
  `test_axis_cell_labels_carry_units`, `test_time_unit_switch_changes_labels`,
  `test_mainwindow_time_unit_fans_out`. **Полный pytest — 280 passed.**

- **#67/#69 — маркеры выбранных линий нуклидов на гранях секущих плоскостей Времени**
  (`awf/ui/view3d.py`, `awf/ui/nuclide_panel.py`, `awf/ui/panels.py`, `tests/test_group4_remarks.py`):
  два замечания решены одной правкой (подсветка нуклидов перенесена с рельефа на грани плоскостей).
  - **#67 — подсветка изотопов на секущих плоскостях.** На каждой ВИДИМОЙ плоскости Времени
    (`("time", 0/1)`) для каждой выбранной гамма-линии в диапазоне энергий рисуется цветной
    вертикальный отрезок на позиции энергии (`px = i - nt/2`, `py = энергия→мировой Y`), цвет =
    цвет нуклида. Реализация: `_rebuild_plane_nuclides` + `_draw_plane_nuclide_lines` +
    `_add_plane_nuclide_line`, элементы в `_plane_nuclide_items`. Перестроение — из `set_spectrogram`,
    `set_energy_lines` и `set_plane(axis="time")` (видимость/позиция плоскости меняет маркеры).
  - **#69 — высота ∝ интенсивности, для нескольких линий семейства.** 4-й элемент кортежа линии —
    интенсивность (`GammaLine.intensity`, вероятность испускания) добавлен в `NuclidePanel._collect_lines`;
    `_collect_lines` уже эмитит все major-линии нуклида, поэтому «несколько линий семейства» —
    каждая линия = свой маркер своей высоты. Высота `h = (I / I_max) · zmax` (`_max_line_intensity`
    нормирует по ярчайшей среди выбранных; пол 0.04·zmax, чтобы слабая линия была видна). Кортежи без
    интенсивности (3-элементные) → полная высота `zmax` (обратная совместимость; все распаковки
    `_energy_lines` в `view3d.py`/`panels.py` сделаны толерантными к 3-/4-кортежу).
  Тесты: `test_plane_nuclides_drawn_on_visible_time_plane`, `test_plane_nuclides_absent_without_visible_plane`,
  `test_plane_nuclide_height_scales_with_intensity`, `test_plane_nuclides_backward_compat_3tuple`,
  `test_plane_nuclides_cleared_when_lines_removed`. **Полный pytest — 285 passed.**

- **#70 — убавить окружающее поле вокруг данных 3D** (`awf/ui/view3d.py`): отступ рамки координатной
  сетки от края данных уменьшен с целой клетки (#63) до **полклетки** — в `_rebuild_grid` введены
  `mx, my = 0.5*cellx, 0.5*celly`, поле `gx0/gx1/gy0/gy1` строится с этим половинным отступом.
  Рамка по-прежнему ярче линий сетки и обводит поле, но прижата к данным вдвое плотнее. Тест
  `test_grid_border_extends_one_cell_beyond_data` переименован в `…_half_cell_…` и усилен проверкой
  «отступ < 0.75 клетки» (т.е. ≈половина, не целая). **Полный pytest — 285 passed.**

- **#71 — шкала времени по 15 минут для больших водопадов** (`awf/ui/view3d.py`): добавлены
  helper'ы `_step_ticks(lo,hi,step)` (деления фиксированным шагом, кратные ему) и `_pick_ticks`
  (предпочтительный шаг, иначе откат на `_nice_ticks`), константы `_TIME_STEP_15MIN_S=900`,
  `_PREF_STEP_MIN_TICKS=3`/`_PREF_STEP_MAX_TICKS=40`. `_time_ticks` теперь для длинных записей
  (на диапазоне выходит 3..40 делений по 900 с) строит деления ровно по 15 мин (в секундах),
  значения подписей пересчитываются в выбранную единицу (с/мин/ч); короткая запись (<45 мин) —
  прежние авто-деления `_nice_ticks` в выбранной единице (#64 не сломан). Тесты
  `test_time_ticks_15min_step_large_waterfall`, `test_time_ticks_fallback_short_waterfall`.
  **Полный pytest — 287 passed.**

- **#72 — шкала энергии по 200 кэВ** (`awf/ui/view3d.py`): `_energy_ticks` переведён на
  `_pick_ticks(..., _ENERGY_STEP_KEV=200)` — для широкого спектра (3..40 делений по 200 кэВ)
  деления идут ровно по 200 кэВ; узкий диапазон (десятки кэВ) — откат на `_nice_ticks` (как было).
  Тесты `test_energy_ticks_200kev_step_wide_spectrum` (калибровка 3 кэВ/канал, 0..~3069 кэВ),
  `test_energy_ticks_fallback_narrow_spectrum`. **Полный pytest — 289 passed.**

- **#73 — уменьшить шрифт подписей делений 3D** (`awf/ui/view3d.py`): в `_rebuild_axis_labels`
  шрифт подписей делений `QFont("Helvetica", 10)` → `7` (нижние подписи энергии «1000 кэВ» и т.п.
  были слишком крупными). Тест `test_axis_label_font_reduced` (все GLTextItem осей ≤8 pt).
  **Полный pytest — 290 passed.**
- **#74 — перебор комбобоксов тулбара кнопкой/колесом вместо выпадающего списка**
  (`awf/ui/cyclebutton.py` NEW + `awf/ui/main_window.py`): новый виджет `CycleButton(QPushButton)` —
  кнопка показывает текущее значение, клик листает по кругу вперёд, колесо мыши — вперёд/назад.
  4 контрола тулбара («Z-шкала», «Палитра», «Единицы», «Время») переведены с `QComboBox` на
  `CycleButton`. API совместим с используемой частью QComboBox (`addItem(label,data)`, `count`,
  `currentIndex`/`setCurrentIndex`, `currentData`/`currentText`, `itemData(i)`/`itemText(i)`, сигнал
  `currentIndexChanged(int)` эмитится только при реальной смене индекса) — проводка и прежние тесты
  тулбара не тронуты. Наследник `QPushButton` → QSS-стиль `QToolBar QPushButton` из #62 применяется.
  Тесты `test_cyclebutton_click_cycles`, `test_cyclebutton_wheel_scrolls`. **Полный pytest — 292 passed.**
- **#75 — каркас верхних выпадающих меню** (`awf/ui/main_window.py`): к меню «Файл» добавлены пять
  выпадающих меню с правильными названиями — **Изотопы / Анализ / Сервис / Помощь / О программе**
  (`_build_stub_menus`, вызывается из `_build_menu`). Пока это каркас: в каждом меню один
  disabled-пункт-заглушка «— наполняется позже —»; реальные действия будут подключены в следующих
  задачах. Меню сохранены в `self._menus` (ключи `isotopes/analysis/service/help/about`) — будущая
  сессия легко довесит действия. Тест `test_top_menus_skeleton_present`. **Полный pytest — 293 passed.**
- **#76 — отключение подложки (фиолетовый прямоугольник = плоское дно рельефа)**
  (`awf/ui/view3d.py` + `awf/ui/main_window.py`): «подложка» — не отдельный объект, а ячейки дна
  единственной `GLSurfacePlotItem` с нормированной высотой `zn≈0` (в палитре — фиолетовый минимум),
  лежащие плашмя при `z≈0`. При выключении подложки такие ячейки (высота `≤ _FLOOR_FRAC·height_scale`,
  т.е. `zn ≤ 0.02`) делаются прозрачными (`alpha=0`), поверхность переводится в `translucent` —
  остаётся только рельеф-«всплески», сплошной фиолетовый прямоугольник базы исчезает (виден фон).
  Чекбокс «Подложка» в тулбаре (дефолт вкл, сбрасывается в вкл кнопкой «Сброс») → `_on_floor_toggled`
  → `view3d.set_floor_visible()` (alpha-only ре-рендер, без ре-LOD). Тесты `test_floor_visible_by_default`,
  `test_floor_hidden_zeroes_base_cells`, `test_floor_toolbar_checkbox_drives_view`.
  **Полный pytest — 296 passed.**
- **#77 — подписи делений осей: гасить за массивом, зажигать со стороны взгляда**
  (`awf/ui/view3d.py`): подписи единиц («15 мин» по времени, «200 кэВ» по энергии) и зубцы оси энергии
  раньше жёстко стояли на `y=ymin` / `x=xmax` — при повороте уезжали за рельеф. Теперь край подписи
  выбирается по знаку XY-направления камеры на зрителя (`_viewer_sides()`: камера→центр сцены, центр в
  (0,0)): время — на `ymax`/`ymin`, энергия (+зубцы) — на `xmax`/`xmin`, всегда на ближней к зрителю
  стороне. Перевешивание при вращении мышью — через override `mouseMoveEvent` → `_maybe_reorient_labels()`,
  перестройка только при смене квадранта (по сигнатуре `_label_side_sig`, не каждый кадр). Дефолт камеры
  (azimuth=−60) даёт прежнее размещение (время `ymin`, энергия `xmax`) — поведение по умолчанию не
  изменилось. Тест `test_axis_labels_follow_viewer_side`. **Полный pytest — 297 passed.**

- **#78 — сетку и вывод 3D ограничить 3000 кэВ** (`awf/ui/view3d.py`): каналы с энергией выше порога
  `_MAX_ENERGY_KEV = 3000.0` отсекаются в `set_spectrogram` сразу после LOD-прорежки — срез prefix'а
  массивов `z_counts`/`_z_counts_sum`/`ch_centers` (калибровка канал→энергия монотонно возрастает, верхние
  каналы = хвост). Поскольку `ch_centers` задаёт и рельеф, и деления оси энергии (`_energy_ticks`), и
  координатную сетку (`_rebuild_grid` через `_axis_extent`), обрезка данных автоматически обрезает и сетку,
  и подписи — отдельной логики для сетки не нужно. Если энергия файла и так ≤ порога — срез не выполняется.
  Тест `test_energy_axis_capped_at_3000`. **Полный pytest — 298 passed.**

- **#79 — в меню «Изотопы» ссылку на окно с изотопами** (`awf/ui/main_window.py`): пункт-заглушка
  меню «Изотопы» (каркас #75) заменён действующей ссылкой на док «Нуклиды». Док сохранён в
  `self._ndock`, в `_build_stub_menus` для ключа `isotopes` добавляется `self._ndock.toggleViewAction()`
  (текст «Окно изотопов (нуклиды)») — стандартный Qt-идиом «пункт меню ↔ панель»: открывает/прячет
  окно и галочкой отражает его видимость. Прочие меню остаются disabled-заглушками. Тест #75
  обновлён (исключает «Изотопы» из проверки «всё disabled»), добавлен `test_isotopes_menu_opens_nuclides_dock`.
  **Полный pytest — 299 passed.**

- **#80 — убрать вертикальные жёлтые отрезки (зубцы осей)** (`awf/ui/view3d.py`): оливковые
  вертикальные отрезки-«зубцы» шкалы энергий (добавлены в IV-R2/#77) убраны — на оси энергии
  остались только текстовые подписи делений (`{en:g} кэВ`). В `_rebuild_axis_labels` удалён вызов
  `_add_line(...)` (вместе с расчётом высоты зубца от `zmax`), как мёртвый код удалены сам метод
  `_add_line` и константа `_ENERGY_TICK_RGBA` (использовались только для зубцов; проверено grep).
  Тесты наличия зубцов перепрофилированы под отсутствие: `test_view3d_axes.py` (`teeth == 0`),
  `test_energy_scale_has_no_vertical_teeth` в `test_group4_remarks.py`. Новый тест не добавлялся.
  **Полный pytest — 299 passed.**

- **#81 — объяснить физический смысл пунктов «Регулировки отображения»** (без правок кода):
  даны пояснения по 6 регуляторам панели `AdjustPanel` (`awf/ui/knobs.py:242-249` `_SPECS`,
  применение `awf/ui/main_window.py:346-362`, математика контраста `awf/ui/zscale.py:37-77`):
  Усиление (gain 0.20×–5.00×, дефолт 1.00×), Гамма (0.20–3.00, дефолт 1.00), Отсечка
  (clip 80–100 %, дефолт 100 %), Сглаживание (0–15 каналов, дефолт 0), Освещение (0–100 %,
  дефолт 0 %), Окно t (0.25×–4.00×, дефолт 1.00×). Все регуляторы — только отображение, не
  меняют исходные отсчёты; дефолты = нейтральный bypass. Изменений кода/тестов нет — pytest
  остаётся 299 passed.

- **#82 — отключить жёлтое окно (ROI-выделение) на 2D-карте** (`awf/ui/panels.py`): жёлтый
  прямоугольник `pg.RectROI` (`HeatmapPanel._roi`) больше не показывается — в `set_spectrogram`
  установка `self._roi.setVisible(True)` заменена на `setVisible(False)`. Позицию/размер окна
  по умолчанию (центральная четверть) и вызов `_emit_roi()` оставили, поэтому панель срезов
  по-прежнему получает дефолтную «полосу ROI» (магента-кривая работает). Тест
  `test_heatmap_roi_hidden`. **Полный pytest — 300 passed.**

- **#83 — объяснить смысл пунктов вкладки «Аналитика» и подменю** (без правок кода): вкладка
  проецирует спектры временны́х срезов в 2D и красит точки кластерами (`awf/ui/analytics_panel.py`,
  бэкенд `awf/analysis/decomposition.py:15` `METHODS=("pca","tsne","umap")` + `feature_matrix`,
  `awf/analysis/cluster.py:15` `METHODS=("kmeans","dbscan","hdbscan")`). Контролы: Проекция
  (метод понижения размерности n_каналов→2D), Кластеры (метод группировки), k (число кластеров
  для kmeans, 1–20; dbscan/hdbscan определяют сами), Нормировка (L1 по строке — сравнение формы
  спектра без зависимости от полной активности; log1p применяется всегда), Пересчитать
  (авто-пересчёт только при ≤2000 срезов). Точка = один временно́й срез; клик → переход к срезу.
  «дисперсия PCA X%/Y%» — доля объяснённой дисперсии 1-й/2-й компонент. Изменений кода/тестов
  нет — pytest 300 passed.

- **#87 — 2D-спектрограмма «уезжает» при отдалении/панораме** (`awf/ui/panels.py`): добавлен
  `HeatmapPanel._lock_view_to_data()` — после `set_spectrogram` на ViewBox карты ставится
  `setLimits(xMin=0, xMax=cols, yMin=0, yMax=rows, maxXRange=cols, maxYRange=rows)`. Панорама
  больше не выходит за прямоугольник данных, а максимально отдалённый вид (минимальный зум)
  равен полному окну — карта не уезжает в пустоту (на скрине было X −5000..40000, Y −500..4000).
  Поведенчески проверено: попытка задать «уехавший» диапазон зажимается в `0..cols × 0..rows`.
  Тест `tests/test_group4_remarks.py::test_heatmap_view_limited_to_data`. **Полный pytest — 301 passed.**

- **#84 — секущие плоскости 3D режут данные от своего края до текущей позиции** (`awf/ui/view3d.py`):
  переработан `Waterfall3DView._clip_windows()` — теперь каждый слот плоскости режет независимо и
  односторонне: слот 0 задаёт нижнюю границу (отрезает от минимума оси до своей позиции), слот 1 —
  верхнюю (от максимума оси). Раньше (IV-R3/#22) обрезка включалась только при ВИДИМОЙ паре плоскостей
  оси; теперь достаточно одной — она прячет «пройденный» участок от своего края. Две плоскости оси
  по-прежнему дают окно между ними (обратная совместимость). Семантика «от края, прятать пройденное»
  выбрана оператором. Встречные плоскости (нижняя выше верхней, `i0>i1` или `j0>j1`) дают вырожденное
  окно — в `_rebuild_surface()` добавлена защита: поверхность снимается (`_surface=None`), пустой
  `GLSurfacePlotItem` не строится. Тесты `test_group4_remarks.py`: переписан
  `test_single_plane_clips_from_edge` (одиночный слот 0 режет), добавлены
  `test_single_plane_slot1_clips_upper`, `test_single_counts_plane_activates_clip`,
  `test_crossed_planes_empty_surface`; `test_disable_one_plane_restores_full` →
  `test_disable_one_plane_keeps_other_clip` (снятие слота 1 оставляет обрезку слота 0). **Полный pytest — 304 passed.**

- **#85 — маркеры изотопов только на секущих плоскостях** (`awf/ui/view3d.py`): прежние рёберные
  лучи энергий (Задача 15/IV-R2 — вертикальные отрезки у края данных `x=xmin`, рисовались для каждой
  выбранной линии вне зависимости от плоскостей) убраны. `_rebuild_energy_rays()` теперь только снимает
  ранее построенные лучи (`_ray_items`), ничего не рисует. Единственный механизм маркеров —
  `_rebuild_plane_nuclides()`: цветные вертикальные отрезки выбранных гамма-линий на гранях ВИДИМЫХ
  секущих плоскостей Времени (высота ∝ интенсивности, #67/#69). Без видимой плоскости маркеров нет
  совсем; чтобы увидеть линии нуклида — включить секущую плоскость Времени. Режим подсветки поверхности
  (Задача 18, отдельный тумблер) не затронут. Тесты `test_view3d_rays.py` переписаны под новую семантику
  (`test_no_edge_rays_markers_only_on_planes`, `test_plane_marker_y_matches_energy_channel`,
  `test_plane_markers_cleared_on_empty_and_replaced`, `test_plane_markers_rebuilt_on_zscale_change`);
  `test_highlight.py::test_view3d_highlight_enable_recolors_and_keeps_markers` проверяет, что маркеры
  плоскости переживают ре-рендер подсветки, а рёберных лучей нет. **Полный pytest — 304 passed.**

- **#86 — догрузка гамма-линий оставшихся изотопов из списков (медицинские в первую очередь)**
  (`scripts/fetch_missing_nuclides.py`, `awf/data/nuclides.json`): из 91 нуклида в карте категорий в
  библиотеке было только 21 (LSRM SpectraLine). Написан воспроизводимый скрипт (§16,
  `scripts/fetch_missing_nuclides.py`): читает разницу `nuclide_categories.json` − `nuclides.json`,
  тянет γ-линии каждого недостающего нуклида из IAEA LiveChart/ENSDF (`decay_rads`, `rad_types=g`,
  офлайн-кэш `awf/data/iaea_cache/`), фильтрует по интенсивности ≥ 0.1 %, кэп 30 линий/нуклид,
  T½ → value+unit, lifetime по порогу 60 сут. Метастабильные изомеры (Tc-99m / Ba-137m / Ag-110m)
  берутся из запроса основного состояния по `p_energy` уровня изомера, T½ — из `start_level_hl`
  (опробовано на физике: Tc-99m 140.5 кэВ @89 %, Ba-137m 661.66 кэВ @89.9 %). Порядок приоритета —
  medical → natural → technogenic → fission. Слияние `--merge` идемпотентно дописывает только
  отсутствующие имена и обновляет `_provenance`. Результат: **+62 нуклида (21 → 83), 124 → 871 линия**;
  4 категории заполнены (medical 10, natural 21, technogenic 33, fission 19; lifetime 44 long / 39 short).
  **8 нуклидов честно НЕ добавлены** (нет γ-линий ≥ 0.1 % — чистые α/β-излучатели): Po-210, Ni-63,
  Pu-241, Pm-147, Ru-106, Sr-89, Sr-90, Y-90 (для Ru-106 добавлена дочерняя γ-активная Rh-106). Все
  значения сверены с источником IAEA, ни одного из памяти (анти-галлюцинация §23); спот-чек 15
  изотопов против справочника совпал, «верхние» рентгеновские линии (Cr Kα, Hg Kα у Tl-201 — рабочая
  линия ОФЭКТ) — реальные строки таблицы `rad_types=g`, эталонные гамма (Cr-51 320 кэВ, Tl-201 167 кэВ)
  в записях присутствуют. Тесты панели обновлены под новый состав: `topLevelItemCount` 3 → 4 (появилась
  ветка «Медицинские»), `test_lifetime_filter_partitions_long_short` (фильтр разделяет long/short вместо
  прежнего «все long»), `test_add_nuclide_replaces_not_duplicates` использует Po-210 (нет в библиотеке).
  **Полный pytest — 304 passed.**

- **#88 — объяснение (без кода): панель «Срезы / Сечения / Выборки»** (`awf/ui/panels.py::SlicePanel`):
  верхний график — γ-спектр выбранного фрагмента по времени (срез/окно между плоскостями/прямоугольная
  выборка с 2D-карты), не интегральный спектр всего файла → видно короткие/слабые линии, теряющиеся в
  усреднении; селектор «Энергоокно» — узкая ROI вокруг фотопика (пресеты Cs-137 662 / K-40 1461 / U 609 /
  U 1764 / Th 2614 или ручные границы); нижний график — скорость счёта по времени, две кривые: «полоса
  ROI» (малиновая, широкая полоса каналов с 2D-карты) и «энергоокно» (жёлтая, узкая полоса у линии).
  Ценность: временной профиль конкретной линии (внос/вынос/спад источника), отделение линии от фона
  (узкое окно vs широкая полоса), накопление статистики по «горячей» выборке. Изменений кода/тестов нет,
  счётчик 304.

- **#89 — графики дока «Срезы/Сечения/Выборки» уезжали при зуме — привязка к окну данных**
  (`awf/ui/panels.py::SlicePanel._lock_views_to_data`): аналог #87 для двух графиков среза. После
  `set_spectrogram` X-домен каждого ViewBox ограничивается экстентом данных через
  `setLimits(xMin, xMax, maxXRange=…)`: верхний график — энергия `[emin..emax]`, нижний — время
  `[tmin..tmax]`. Панорама не выходит за домен, минимальный зум (макс. отдаление) = полный домен.
  Y намеренно не ограничиваем — у спектра среза бывает лог-шкала (#43), у профиля масштаб счёта/cps
  меняется (#44/#53). Тест `tests/test_group4_remarks.py::test_slice_views_locked_to_data`.
  **Полный pytest — 305 passed.**

- **#90 — сглаживание: SMA по каналам в каждом временно́м срезе** (`awf/ui/view3d.py:237`,
  `awf/ui/zscale.py::smooth_counts`): проверка-фиксация. Запрошенное поведение уже реализовано —
  `z_counts = smooth_counts(z_counts, self._smooth, axis=1)`, где `z_counts` имеет форму
  `(nt, nc)` = (временны́е срезы × каналы), `axis=1` — канальная (энергетическая) ось. `smooth_counts`
  это box-SMA окном `2·r+1` с краевым паддингом; применяется к каждому ряду-срезу независимо (по
  времени не смешивает). Регулятор «Сглаживание» — диапазон 0–15, дефолт 0 (`knobs.py:246`), 0 = bypass
  (`DEFAULT_SMOOTH==0`). Изменений в логике нет; добавлен регрессионный тест
  `test_smooth_per_time_slice_independent_axis1` (декомпозиция: 2D-сглаживание axis=1 == одномерный SMA
  каждого среза). **Полный pytest — 306 passed.**

- **#91 — убрать нефункциональную кнопку «Регулировки: ВКЛ/ВЫКЛ»** (`awf/ui/knobs.py`,
  `awf/ui/style.py`): общий мастер-выключатель `AdjustPanel._global` удалён. Он был
  нефункционален по сути — по дефолту стоял ВЫКЛ (#60) и в этом состоянии `values()` отдавал
  ВСЕ дефолты, заглушая ряды; чтобы любая регулировка заработала, пользователь должен был ещё и
  флипнуть мастер, иначе включение отдельного ряда не давало эффекта. Теперь bypass — только
  per-row (тумблер «вкл/выкл» на каждой ручке, как и был): `values()` = `{k: r.effective_value()}`,
  ряд выкл → его дефолт. Удалены `_on_global`/`is_global_on`/`KnobRow.set_global_enabled` и
  QSS-правила `#knobGlobal`; `reset_all` упрощён (гашение ручек делает `_on_toggle` при
  `set_on(False)`). Тесты переписаны под per-row (`test_adjustpanel_per_row_bypass`,
  `test_mainwindow_rows_off_revert_all`, правки tbin/light/bypass-тестов). **Полный pytest — 306 passed.**

- **#92 — рукоятки Регулировок сбрасывали зум 3D** (`awf/ui/view3d.py`): каждый ход ручки
  (`set_smoothing`/`set_contrast`/`set_light_intensity`/`set_time_bins`/`set_z_scale`/`set_colormap`/
  `set_unit_mode`) перерисовывает поверхность через `set_spectrogram(self._sg, …)`, а тот безусловно
  звал `setCameraPosition(distance=span*1.6)` — сбрасывая пользовательский зум/панораму на каждое
  движение. Исправлено: в начале `set_spectrogram` фиксируем `is_new = sg is not self._sg` (новый файл
  — другой объект; ре-рендеры передают тот же `self._sg`), и кадрирование камеры выполняем только
  `if is_new`. Зум сохраняется при любой регулировке/смене палитры/Z-шкалы/единиц; при загрузке нового
  спектра камера по-прежнему кадрируется под размер данных. Тест `test_adjust_setters_preserve_camera_zoom`
  (зум держится через set_smoothing/set_contrast/set_colormap; новый sg — кадрирует заново).
  **Полный pytest — 307 passed.**

- **#93 — шрифт тулбара «Вид» = как в главном меню** (`awf/ui/style.py`): после #62 тулбар и его
  контролы были подняты до 15px, крупнее главного меню (`QMenuBar`/`QMenu` — 14px). Приведено к
  14px: `font-size: 15px`→`14px` во всех 5 правилах тулбара (`QToolBar` и потомки
  `QLabel`/`QCheckBox`/`QComboBox`/`QPushButton`); высота контролов (`min-height`/`padding` от #62)
  не тронута. Тест #62 `test_toolbar_and_statusbar_sized_up` обновлён: проверяет `font-size: 14px`
  присутствует и `font-size: 15px` больше нет. **Полный pytest — 307 passed** (нового теста нет,
  счётчик без изменений).

- **#94 — убрать нерабочий пункт «— вручную —» из комбобокса «Энергоокно»** (`awf/ui/panels.py`):
  пункт был пустышкой — его выбор уходил в ранний `return` (`idx <= 0`), ничего не делая. Удалён;
  комбо `_ewin_preset` теперь содержит только пресеты нуклидов (`DEFAULT_WINDOWS`), индексы сдвинуты
  на −1 (`_on_ewin_preset`: `DEFAULT_WINDOWS[idx]`, guard `idx < 0 or idx >= len`). «Ручное» состояние
  (правка спинбоксов границ) выражено штатной идиомой Qt — пустой выбор `setCurrentIndex(-1)` (в
  `__init__` старт тоже −1, как прежняя «вручную»: на старте пресет не активен). `_on_ewin_spin` ставит
  −1 вместо 0. Тесты: новый `test_ewin_preset_has_no_manual_item` + правки `test_preset_combobox_sets_window`
  (пресет 0 = Cs-137) и `test_manual_spin_edit_resets_preset` (ручное → idx −1). **Полный pytest — 308 passed.**

- **#95 — маркеры изотопов не должны просвечивать сквозь рельеф 3D** (`awf/ui/view3d.py`):
  вертикальные маркеры нуклидов (`_add_plane_nuclide_line`) создавались `GLLinePlotItem` с дефолтными
  glOptions, а дефолт у `GLLinePlotItem` в pyqtgraph 0.14.0 — `'additive'` (`GL_DEPTH_TEST` ВЫКЛ),
  поэтому они всегда рисовались поверх и были видны «за массивом». Передал `glOptions="opaque"` (depth-тест
  ВКЛ) + `antialias=False` (без blend сглаживание линии не применяется) — рельеф теперь перекрывает
  маркеры, оказавшиеся позади него; `'opaque'` корректен при любом порядке отрисовки. Профиль сечения
  (#41, `_PROFILE_GL`) намеренно остаётся поверх — его не трогал. Тест `test_plane_marker_depth_tested_not_additive`
  (проверка `GL_DEPTH_TEST is True` у построенного маркера). **Полный pytest — 309 passed.**

- **#96 — фон и вычитание; меню «Анализ»: выбор/наложение/вычет фона** (`awf/model/background.py` —
  новый; `awf/ui/background_dialog.py` — новый; `awf/ui/panels.py::SlicePanel`; `awf/ui/main_window.py`):
  фон — поканальная скорость счёта (cps, нормировка по живому времени). Источник — ЛЮБОЙ из двух:
  (а) диапазон срезов текущего измерения — `background_from_range` = `Σcounts[t_lo:t_hi]/Σlive_time`;
  (б) отдельный файл (.aswf/.rcspg/.n42) — `background_from_spectrogram`: при иной калибровке фон
  переводится в спектральную плотность (cps/кэВ через `|∇E|`), интерполируется по энергии цели
  (`np.interp`, вне диапазона → 0, без экстраполяции) и умножается на ширину каналов цели —
  метрологически корректно, при совпадающей калибровке сводится к поканальному cps. **Наложение фона:**
  `SlicePanel._bg_curve` (оранжевый пунктир) поверх спектра среза в текущих единицах (cps — фон как
  есть; counts — `bg_cps·live_time` окна). **Вычет фона = весь водопад (3D+2D+срез):** `subtract_background`
  строит производную `Spectrogram` `clip(counts[t,ch] − bg_cps[ch]·live_time[t], 0)` (отрицательное → 0),
  раздаётся через `MainWindow._redistribute`/`_active_spectrogram` в 3D-рельеф, 2D-карту и панель срезов;
  вкладка «Аналитика» — ВСЕГДА на исходных данных. Меню «Анализ»: «Выбор фона…» (диалог
  `BackgroundDialog`), «Наложение фона» и «Вычет фона» (checkable, активны только после выбора фона);
  загрузка нового файла сбрасывает фон (`_reset_background`). Тесты: `tests/test_background.py` (10 —
  математика: диапазон-cps, взвешивание по живому времени, клип отрицательных, масштаб по live_time,
  файл-фон та же/иная калибровка `[2,2,2,2,2,0]`), `tests/test_bg_ui.py` (10 — наложение скрыто до
  включения, единичное масштабирование cps/counts, очистка по None, вычет уменьшает водопад и
  восстанавливается, аналитика на исходных, сброс при перезагрузке, диалог range/file/default) + правка
  #75 `test_top_menus_skeleton_present` (меню «Анализ» теперь наполнено). **Полный pytest — 329 passed.**

- **#97 — стиль диалога «Выбор фона» не соответствует тёмной теме** (`awf/ui/style.py`):
  `BackgroundDialog` (#96) — дочерний к `MainWindow`, поэтому тёмный фон темы (`APP_QSS`) на него
  каскадировал, НО в теме не было правил для `QRadioButton` (рисовался дефолтным синим индикатором
  Windows) и для кнопок-стрелок спинбоксов `QSpinBox::up/down-button` (светлые системные, выбивались
  из тёмного диалога). Добавлены в `APP_QSS`: `QDialog` (явный тёмный фон top-level), `QRadioButton` +
  `::indicator` (тёмный кружок, checked — зелёный акцент `#4a7d4a`, как у выделения меню/вкладок),
  `QSpinBox/QDoubleSpinBox ::up/down-button` (тёмные `#45484d` + hover) и `::up/down-arrow`
  (CSS-треугольники светлым `#cfd2d6`). Правила общие — чинят радиокнопки и спинбоксы во всём
  приложении. Тесты: `test_bg_dialog_theme_covers_radio_and_spinbox` (QSS покрывает виджеты),
  `test_bg_dialog_inherits_window_theme` (диалог — child окна, наследует градиентную тему).
  **Полный pytest — 331 passed.**

- **#99 — наложение фона в окне среза выглядит странно (лог-шкала, ВЭ-хвост)** (`awf/ui/panels.py`):
  оранжевая кривая фона (`_bg_curve`) в высокоэнергетическом хвосте имеет много каналов с фоном
  `== 0` (в фоновом временно́м окне там нулевая сумма отсчётов). На лог-шкале Y `log10(0) → −∞`,
  pyqtgraph рисовал «частокол» вертикальных пунктиров до ~1e-10. Исправлено в `_render_background`:
  при включённой лог-шкале (`_spec_log`) неположительный фон маскируется в `nan`, кривая рисуется
  с `connect="finite"` — рвётся по сегментам, нули просто не отображаются (на линейной шкале
  маскирования нет — нули остаются точками). `set_spectrum_log` теперь вызывает `_render_spectrum`,
  чтобы маска пересчитывалась при переключении лог/лин. Тест:
  `test_slice_bg_log_masks_nonpositive`. **Полный pytest — 334 passed.**
- **#100 — сброс зума на окнах среза и времени** (`awf/ui/panels.py`): в `SlicePanel` добавлена
  кнопка «Сброс зума» (рядом с «лог Y») и метод `reset_zoom()` — `autoRange()` на обоих ViewBox
  (спектр среза + временной ряд) возвращает полный вид данных; X-домен при этом не выходит за окно
  данных (ограничен `_lock_views_to_data`, #89). Тесты: `test_slice_has_reset_zoom_button`,
  `test_slice_reset_zoom_restores_full_view`. **Полный pytest — 334 passed.**
- **#98 — фоновый массив не отображается на 3D-спектрограмме** (`awf/ui/view3d.py` +
  `awf/ui/main_window.py`): выбранный фон (поканальный cps) теперь виден на 3D-водопаде как
  «тусклая постоянная простыня» (решение оператора) — полупрозрачный сине-серый лист
  (`GLSurfacePlotItem`, alpha 0.16, translucent), натянутый по всей оси времени на высоте рельефа,
  соответствующей `bg(энергия)`. Высота берётся интерполяцией `bg` в реализованное отображение
  значение→высота текущего рельефа (`z_counts`→`z_surface`, `np.interp` по отсортированным
  значениям) — согласовано с любой Z-шкалой/контрастом без повторного применения лог-преобразования
  с data-зависимым полом. Перевод cps→counts (если рельеф в counts) — через медианное живое время
  среза (`_bg_unit_factor`). Простыня пересобирается на каждом `set_spectrogram` и при смене
  фона/видимости. Один пункт меню «Наложение фона» теперь управляет и кривой среза (1D), и
  простынёй (3D): `set_background_sheet` / `set_background_sheet_visible`, разводка в
  `_on_bg_select` / `_on_bg_overlay_toggled` / `_reset_background`. Тест:
  `test_view3d_bg_sheet_toggle`. **Полный pytest — 335 passed.**
- **#101 — 0 по оси Y не зафиксирован (окно среза/выборки)** (`awf/ui/panels.py`): `_lock_views_to_data`
  (#89) ограничивал только X, оставляя нижнюю границу Y свободной — при зуме/панораме «0» уезжал.
  Добавлен `_lock_spectrum_y()` (вызывается из `_render_spectrum`, т.е. на каждую перерисовку, смену
  единиц, сглаживание, переключение лог/лин и фон): в линейном режиме `setLimits(yMin=0)` (счёт ≥ 0 —
  ноль прибит к низу), в логарифмическом `setLimits(yMin=log10(min>0))` — пол по минимальному
  положительному отображаемому значению (вид pyqtgraph в лог-режиме = координаты log10, поэтому floor
  переводится через `log10`). Это убирает «уезжание» дна и пустой провал лог-оси до ~1e-10 в ВЭ-хвосте.
  Тесты: `test_slice_spectrum_y_floor_linear`, `test_slice_spectrum_y_floor_log`.
  **Полный pytest — 337 passed.**
- **#102 — добавить палитры в окно «Цветовая палитра»** (`awf/ui/colormaps.py` + новый `awf/ui/palette_dialog.py`
  + `awf/ui/main_window.py`): было 3 палитры (Insight/Inferno/Viridis), стало 15 — Insight (фирменная, дефолт)
  + 14 со скриншота-макета оператора: Inferno, Magma, Plasma, Viridis, Cividis, Turbo, Jet, Hot, Ocean,
  Parula, Cubehelix, Spectral, Cool, Grayscale. `COLORMAPS` теперь `(ключ, подпись, описание)`. `get_colormap`
  чинён: пробует pyqtgraph-источник, затем `source="matplotlib"` (jet/hot/ocean/cubehelix/cool/gray/Spectral
  резолвятся только так — раньше молча падали бы на Insight); `parula` отсутствует в pyqtgraph/matplotlib
  (лицензия MathWorks) → задан кастомными контрольными точками (как Insight), `Spectral` — только с заглавной.
  Новый модальный диалог `PaletteDialog` (тёмная тема): список строк с превью-градиентом (рендер из
  `getLookupTable` → скруглённый QPixmap), названием и описанием; клик = выбор + подсветка текущей + сигнал
  `selected(key)` для живого применения к 2D/3D. В тулбаре кнопка-цикл палитр (#74) заменена на кнопку,
  открывающую окно; `_apply_colormap` единообразит 2D-карту и 3D-поверхность и переразмещает сечения.
  Тесты: `test_registry_has_screenshot_palettes`, `test_added_palettes_resolve_distinct_from_insight`
  (доказывает отсутствие молчаливого fallback), `test_palette_dialog_lists_all_and_selects`.
  **Полный pytest — 340 passed.**

- **#103 — `.rcspg` RadiaCode не загружался (`JSONDecodeError`)** (`awf/io/rcspg_loader.py` + `awf/ui/main_window.py`
  + новый `tests/test_rcspg_android_text.py`): причина — `load_rcspg` делал `json.load`, но **Android**-экспорт
  RadiaCode это табулированный ТЕКСТ, а не JSON (JSON — это **iOS**-экспорт; оператор подтвердил: нужны оба
  варианта). `load_rcspg` переписан в диспетчер по формату: первый непустой символ `{` → ветка `_load_rcspg_json`
  (iOS, прежняя логика без изменений), иначе → новая `_load_rcspg_text` (Android). Текст-формат: строка-заголовок
  `Spectrogram: …` (tab-разделённые `Channels`/`Timestamp`=Windows-FILETIME/`Accumulation time`/`Device serial`);
  строка `Spectrum: <hex>` → калибровка `[a0,a1,a2]` из 3×float32 LE после 4 служебных байт (преднакопленный
  спектр как временно́й срез НЕ используется); далее по строке на интервал `<FILETIME>\t<живых_сек>\t<отсчёты>`
  с опущенными хвостовыми нулями → паддинг до `Channels`. `live_time_s` = поле «живых секунд» (Σ = Accumulation
  time ТОЧНО — проверено), `time_offsets_s`/`real_time_s` из разностей FILETIME-меток (целочисленно по тикам,
  деление на 1e7). FILETIME трактуется как **UTC** (заголовочный `Time` — локальное время прибора, отличие = зона).
  Проверено на реальном файле оператора: 1236 срезов × 1024 канала, калибровка [3.94, 2.37, 3.8e-4], `live_total`
  = 1624 c. Заголовок диалога ошибки «Ошибка загрузки N42» сделан общим «Ошибка загрузки» (`main_window.py:550`).
  9 Android-тестов; iOS/JSON-тест `test_rcspg_loader.py` не тронут. **Полный pytest — 349 passed.**

**Задача 26 — Вкладка «Аналитика»** (`awf/ui/analytics_panel.py` + `main_window.py`): `AnalyticsPanel`
(2D-скаттер проекций, по одному `ScatterPlotItem` на кластер для легенды; каждая точка несёт индекс
среза). Сигнал `sliceClicked(i)` → `MainWindow._on_analytics_slice` → `SlicePanel.show_time_slice` +
поднятие дока срезов. Контролы: выбор проекции/кластеризации, k, нормировка, «Пересчитать»;
авто-пересчёт только для записей ≤ `AUTORUN_SLICE_CAP=2000` срезов. Тесты: `tests/test_analytics.py`
(7) — скаттер строится, точки покрывают все срезы, клик эмитит индекс, недоступный метод не падает.

**Итог Группы V:** новые модули `gradient/peakmap/decomposition/deconvolve/cluster` + UI (`SlicePanel`
энергоокно, `HeatmapPanel` изолинии, `AnalyticsPanel`). Тестов добавлено: 40 (analysis) + 23 (UI) = 63.
**Полный прогон pytest — 222 passed.** Smoke-launch `MainWindow` (offscreen): изолинии, аналитика
(PCA+KMeans, 3 кластера), клик→срез, энергоокно — все разведены без ошибок. **Не пушено**
(публикация — по явному решению оператора).

---

# Группа VI. Пространственная ось / GPS (ТЗ-B.5,7,10) — отложено

> **Решение оператора 2026-06-25:** «GPS — следующий этап». В backlog для полноты. Hard-предпосылки:
> Задачи 27 и 28 (без них пространственные методы нереализуемы).

## Задача 27 — Рефактор модели: ось координат/трек 🟣 · Объём L
**Цель:** дать `Spectrogram` опциональную пространственную ось (координата/GPS на срез).

**Подзадачи:**
- 27.1. Добавить опциональное поле `track` (массив координат/время на срез) в `Spectrogram`.
- 27.2. Обратная совместимость: записи без трека работают как сейчас (трек = None).
- 27.3. Методы выборки по координате (аналог временных срезов).

**Где:** `awf/model/spectrogram.py`.
**Тесты:** модель несёт трек; существующие тесты не ломаются (трек опционален).
**Зависимости:** numpy. **Предпосылки:** —

## Задача 28 — Извлечение GPS-трека из загрузчиков 🟣 · Объём M
**Цель:** парсить координаты/время из `.rcspg` и `.n42`.

**Подзадачи:**
- 28.1. `.rcspg` (JSON — содержит координаты/время): извлечь трек.
- 28.2. `.n42` (может содержать GPS): извлечь, если присутствует.
- 28.3. Заполнить `track` модели; запись без GPS грузится как прежде.

**Где:** `awf/io/rcspg_loader.py`, `awf/io/n42_loader.py`.
**Тесты:** трек извлекается на файле с GPS; файл без GPS грузится без регресса.
**Зависимости:** lxml/json. **Предпосылки:** 27.
**⚠ Опсек:** координаты — приватные данные; не коммитить образцы (`.gitignore` уже исключает `*.rcspg`/`*.n42`).

## Задача 29 — Карта пиков координата→активность 🟣 · Объём M · *ТЗ-B.7 (пространственный)*
**Цель:** карта активности энергетических окон вдоль маршрута (радиометрическая съёмка).

**Подзадачи:**
- 29.1. Расширить `peakmap` (Задача 21) на ось координат вместо времени.
- 29.2. Привязка нетто-активности окна к координате трека.
- 29.3. UI-карта (координата→цвет=активность).

**Где:** `awf/analysis/peakmap.py`, UI-карта.
**Тесты:** карта координата→активность строится на записи с треком.
**Зависимости:** numpy/pyqtgraph. **Предпосылки:** 21, 27, 28.

## Задача 30 — Volume Rendering 🟣🟡 · Объём L · *ТЗ-B.5*
**Цель:** куб данных X=энергия, Y=координата, Z=время; интенсивность→прозрачность.

**Подзадачи:**
- 30.1. Оценка движка: VTK / Mayavi / Plotly Volume / дооснащение pyqtgraph (тяжёлые зависимости — отдельное решение).
- 30.2. Построение объёма из матрицы с треком; перенос-функция (intensity→opacity).
- 30.3. Интерактивность: вращение, пороги прозрачности, выделение кластеров.

**Где:** новый модуль рендера.
**Тесты:** интерактивный объёмный рендер на записи с треком (smoke на синтетике).
**Зависимости:** VTK или Plotly (TBD). **Предпосылки:** 27, 28.

## Задача 31 — 3D-карта местности + спектры 🟣🟡 · Объём L · *ТЗ-B.10*
**Цель:** X=долгота, Y=широта, Z=высота, цвет=активность/содержание нуклида — наложение спектров на рельеф.

**Подзадачи:**
- 31.1. Источник высоты (из GPS-трека/DEM); привязка спектра к точке маршрута.
- 31.2. Раскраска по суммарной активности или по содержанию выбранного нуклида (Задача 11/29).
- 31.3. Движок карты/рельефа (TBD), интеграция GPS+высота+спектр.

**Где:** новый модуль карты.
**Тесты:** карта местности с цветовой активностью по треку (smoke).
**Зависимости:** картографический/3D-движок (TBD). **Предпосылки:** 27, 28, 29.

---

# Сводки

## Зависимости пакетов по группам
| Группа | Задачи | Новые пакеты | Примечание |
|---|---|---|---|
| I. Фундамент | 1 | — | Qt-free каркас |
| II. Фон и пики | 2–8 | — (numpy) | ТЗ-A.7,8,9 + площадь/MDA/FWHM |
| III. Библиотека/идентификация | 9–12 | — (stdlib) | ТЗ-A.10 |
| IV. Визуал iZotope | 13–18 | — (pyqtgraph ≥0.13 для `GLTextItem`) | ТЗ-A.1…6 |
| V. Аналитика по времени | 19–26 | scipy, scikit-learn (опц.), umap-learn/hdbscan (опц.) | ТЗ-B.3,4,6,8,9 |
| VI. Пространство/GPS | 27–31 | VTK/Plotly (TBD) | ТЗ-B.5,7,10 — отложено |

## Граф зависимостей между задачами
```
1 ─┬─ 2 ─┬─ 6 ─┬─ 7
   │      │     └─ 11 ─ 12
   │      ├─ 18
   │      └─ 23
   ├─ 3 ──┘
   ├─ 4 ── 5 ── 21 ─┬─ 25 ── 26
   ├─ 8 ──(2,11,23)  └─ 29(VI)
   ├─ 19   24 ──┴─ 26
   └─ 22
9 ── 12        10 ── 11
13,14,15,16,17 — независимы (UI);  18 ← (2,17)
27 ── 28 ─┬─ 29 ─ 31
          ├─ 30
```

## Оценка объёма (грубо)
- **S (≤полдня):** 1, 5, 7, 17, 22 — 5 задач.
- **M (1–2 дня):** 2, 3, 4, 6, 8, 9, 10, 12, 14, 15, 16, 19, 20, 21, 24, 26, 28, 29 — 18 задач.
- **L (3+ дня):** 11, 13, 23, 25, 27, 30, 31 — 7 задач.

## Рекомендуемая последовательность (на согласование — порядок за оператором)
**MVP-first:** Группа I (1) → II (2,3,4,5,8,6,7) → III (10,9,11,12) → IV (16,17,13,14,15,18) →
V (19,20,21,22 → 23,24,25,26) → VI (27,28,29,30,31).
Каждый зелёный сквозной прогон фиксируется тестами; после первого — `graphify install` (§21 доктрины).
